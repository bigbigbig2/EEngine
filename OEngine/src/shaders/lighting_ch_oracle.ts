/**
 * lighting_ch_oracle：定义对应渲染阶段使用的 WGSL 着色器代码。
 */

export const LIGHTING_CH_VERTEX_WGSL = "\nconst pos = array< vec2<f32>, 3 >(\n    vec2<f32>(-1.0, -1.0),\n    vec2<f32>( 3.0, -1.0),\n    vec2<f32>(-1.0,  3.0)\n);\n\nstruct FrameAllocatorNative{\n    @builtin(position) pos : vec4<f32>,\n    @location(0) uv: vec2<f32>,\n}\n\n@vertex\nfn main(\n  @builtin(vertex_index) VertexIndex : u32\n) -> FrameAllocatorNative {\n    var out:FrameAllocatorNative;\n\n    let ndc = pos[VertexIndex];\n\n    out.pos = vec4<f32>(ndc, 0.0, 1.0);\n    out.uv = fma(ndc, vec2<f32>(0.5, -0.5), vec2(0.5));\n\n    return out;\n}\n";

export const LIGHTING_CH_FRAGMENT_WGSL = "\nfn receive_instance_bounds(traced_harmonics:f32) -> f32{\n    return select(1.0, -1.0, traced_harmonics < 0.0);\n}\n\n\nfn uv_octahedral_unit_decode( traced_harmonics: vec2<f32> ) -> vec3<f32>{\n    var shader_sdf_distance_sqr = fma(traced_harmonics, vec2(2.0), vec2(-1.0));\n\n    var optimized_move_x = vec3<f32>(shader_sdf_distance_sqr, 1.0 - abs(shader_sdf_distance_sqr.x) - abs(shader_sdf_distance_sqr.y));\n\n    let j = max(-optimized_move_x.z,0.0);\n    \n    optimized_move_x.x += select( j, -j, optimized_move_x.x > 0.0);\n    optimized_move_x.y += select( j, -j, optimized_move_x.y > 0.0);\n\n    return normalize( optimized_move_x );\n}\n\n\nfn decode_g_buffer_normal(traced_harmonics: vec2<u32>) -> vec3<f32>{\n    const shader_sdf_distance_sqr = 1.0 / 65535.0;\n    \n    let optimized_move_x = vec2f(traced_harmonics) * shader_sdf_distance_sqr;\n\n    return uv_octahedral_unit_decode(optimized_move_x.xy);\n}\n\n\nfn v3_matrix4_project(traced_harmonics: vec3<f32>, shader_sdf_distance_sqr: mat4x4<f32>) -> vec3<f32>{\n   \n    let optimized_move_x = shader_sdf_distance_sqr * vec4<f32>(traced_harmonics, 1.0);\n    \n\n    return optimized_move_x.xyz / optimized_move_x.w ;\n    \n}\n\n\nfn uv_to_ndc(traced_harmonics: vec2<f32>)->vec2<f32>{\n    return fma(traced_harmonics, vec2(2.0f, -2.0f), vec2(-1.0f, 1.0f));\n}   \n    \n\nfn project_position_from_depth(\n    traced_harmonics: vec2<f32>,\n    shader_sdf_distance_sqr: f32,\n    optimized_move_x: mat4x4<f32>\n) -> vec3<f32> {\n\n    let j = uv_to_ndc(traced_harmonics);\n\n    return v3_matrix4_project(\n        vec3<f32>(j, shader_sdf_distance_sqr),\n        optimized_move_x\n    );\n}\n\n\nfn mat4_extract_position(traced_harmonics: mat4x4<f32>) -> vec3<f32>{\n    return traced_harmonics[3].xyz;\n}\n    \n\nconst MIN_DIELECTRICS_F0 = 0.04;\n    \n\nfn metalness_to_specular_color(traced_harmonics:f32, shader_sdf_distance_sqr: vec3<f32>) -> vec3<f32>{\n    const optimized_move_x = vec3(MIN_DIELECTRICS_F0);\n    \n    return mix(optimized_move_x, shader_sdf_distance_sqr, traced_harmonics);\n}\n\n\nfn decode_g_buffer_roughness(traced_harmonics: vec4f) -> f32{\n    return traced_harmonics.y;   \n}\n    \n\nfn decode_g_buffer_metalness(traced_harmonics: vec4f) -> f32{\n    return traced_harmonics.x;\n}\n    \n\nfn rgbe9995_decode(traced_harmonics:u32) -> vec3<f32>{\n\n    let shader_sdf_distance_sqr = vec4<f32>(\n        (vec4(traced_harmonics) >> vec4<u32>(0, 9, 18, 27)) \n        & vec4<u32>(0x1FF, 0x1FF, 0x1FF, 0x1F)\n    );\n    \n    return shader_sdf_distance_sqr.rgb * exp2(shader_sdf_distance_sqr.a - 15.0 - 9.0);\n}\n    \n\nfn rgbe_write_code(traced_harmonics:vec3<u32>) -> vec3<u32>{\n    var shader_sdf_distance_sqr = traced_harmonics * 1664525u + 1013904223u;\n\n    shader_sdf_distance_sqr.x += shader_sdf_distance_sqr.y * shader_sdf_distance_sqr.z;\n    shader_sdf_distance_sqr.y += shader_sdf_distance_sqr.z * shader_sdf_distance_sqr.x;\n    shader_sdf_distance_sqr.z += shader_sdf_distance_sqr.x * shader_sdf_distance_sqr.y;\n\n    shader_sdf_distance_sqr = shader_sdf_distance_sqr ^ (shader_sdf_distance_sqr >> vec3(16u));\n\n    shader_sdf_distance_sqr.x += shader_sdf_distance_sqr.y * shader_sdf_distance_sqr.z;\n    shader_sdf_distance_sqr.y += shader_sdf_distance_sqr.z * shader_sdf_distance_sqr.x;\n    shader_sdf_distance_sqr.z += shader_sdf_distance_sqr.x * shader_sdf_distance_sqr.y;\n\n    return shader_sdf_distance_sqr;\n}\n    \nstruct UvUnwrapper{\n    diffuse : vec3<f32>,\n    roughness : f32,\n    occlusion : f32,\n    specularF0 : vec3<f32>,\n    specularF90 : f32,\n    emissive : vec3<f32>,\n    opacity : f32,\n}\nstruct ExternalTextureContext{\n    diffuse : vec3<f32>,\n    specular : vec3<f32>,\n}\nconst RECIPROCAL_PI : f32 = 0.318309886183790671537767526745028724;\n\nfn F_Hauber( traced_harmonics: vec3<f32>, shader_sdf_distance_sqr: f32, optimized_move_x: f32 ) -> vec3<f32> {\n\tlet j = 1.0 - optimized_move_x;\n    let cursor = j * j;\n    let t3 = cursor * cursor;\n\t\n\tlet gi_radiance = vec3(shader_sdf_distance_sqr - optimized_move_x);\n\t\n\treturn mix( traced_harmonics, gi_radiance, t3 );\n}\n    \nconst EPSILON  :f32 = 1e-6;\n\nfn get_bent_noh_squared(\n    traced_harmonics: f32,\n    shader_sdf_distance_sqr: f32,\n    optimized_move_x: f32,\n    j: f32 \n) -> f32{\n\n    let cursor = inverseSqrt(1.0 + j * j);\n    \n    let t3 = 2.0 * traced_harmonics * shader_sdf_distance_sqr - optimized_move_x;\n    if (t3 >= cursor){\n        return 1.0;\n    }\n    if (t3 <= -cursor) {\n\n        let gi_radiance = traced_harmonics + shader_sdf_distance_sqr;\n        return saturate(gi_radiance * gi_radiance / max(2.0 + 2.0 * optimized_move_x, 1e-6));\n    }\n\n\n    let needs_destructor_signature = cursor * j / sqrt(max(1.0 - t3 * t3, 1e-6));\n    \n    var raw_destructor_signature = needs_destructor_signature * (shader_sdf_distance_sqr - t3 * traced_harmonics);\n    var seed_budget_ms = needs_destructor_signature * (2.0 * shader_sdf_distance_sqr * shader_sdf_distance_sqr - 1.0 - t3 * optimized_move_x);\n\n\n    let texture = sqrt(saturate(1.0 - traced_harmonics * traced_harmonics - shader_sdf_distance_sqr * shader_sdf_distance_sqr - optimized_move_x * optimized_move_x + 2.0 * traced_harmonics * shader_sdf_distance_sqr * optimized_move_x));\n\n    let format = needs_destructor_signature * texture;\n    let dst = needs_destructor_signature * (2.0 * texture * shader_sdf_distance_sqr);\n\n    let message = traced_harmonics * cursor + shader_sdf_distance_sqr + raw_destructor_signature;\n    let color_texture = optimized_move_x * cursor + 1.0 + seed_budget_ms;\n\n    let redundant = format * color_texture;\n    let bucket_index_count = message * color_texture;\n    let local_total_indirect_diffuse = dst * message;\n\n    let num_occluded = bucket_index_count * ( 0.25 * local_total_indirect_diffuse - 0.5 * redundant );\n    let u8array = redundant * redundant + local_total_indirect_diffuse * (local_total_indirect_diffuse - 2.0 * redundant) + message * ((traced_harmonics * cursor + shader_sdf_distance_sqr) * color_texture * color_texture -\n       bucket_index_count * (0.5 * (color_texture + optimized_move_x * cursor) + 0.5));\n\n\n    let meshlet_buckets = 2.0 * num_occluded / (u8array * u8array + num_occluded * num_occluded + 1e-8);\n    let result = meshlet_buckets * u8array;\n    let allocator_textures = 1.0 - meshlet_buckets * num_occluded;\n\n    raw_destructor_signature = allocator_textures * raw_destructor_signature + result * format;\n    seed_budget_ms = allocator_textures * seed_budget_ms + result * dst;\n\n\n    let meshlet = traced_harmonics * cursor + raw_destructor_signature;\n    let filter_mitchell = optimized_move_x * cursor + seed_budget_ms;\n    \n    let rel_name = shader_sdf_distance_sqr + meshlet;\n    let chunk_scene_bounding_box = 2.0 * filter_mitchell + 2.0;\n    \n    return saturate(rel_name * rel_name / max(chunk_scene_bounding_box, 1e-6));\n    \n}\n    \n\nfn oren_nayar_fujii_diffuse_dir_albedo(traced_harmonics: f32, shader_sdf_distance_sqr: f32, optimized_move_x: f32) -> f32 {\n    let j = 1.0 - traced_harmonics;\n    \n\n    const cursor = 0.0571085289;\n    const t3 = 0.491881867;\n    const gi_radiance = -0.332181442;\n    const needs_destructor_signature = 0.0714429953;\n\n\n    let raw_destructor_signature = fma(j, needs_destructor_signature, gi_radiance);\n    let seed_budget_ms = fma(j, raw_destructor_signature, t3);\n    let texture = fma(j, seed_budget_ms, cursor);\n    let format = j * texture;\n\n\n    return optimized_move_x * fma(shader_sdf_distance_sqr, format, 1.0);\n}    \n    \n\nfn oren_nayar_fujii_diffuse_avg_albedo(roughness: f32, A: f32) -> f32 {\n    return A * fma(0.07248821245692394, roughness, 1.0);\n}\n    \n\nfn oren_nayar_compensated_diffuse(\n    NoV: f32, \n    NoL: f32, \n    LoV: f32, \n    roughness: f32, \n    color: vec3f\n) -> vec3f {\n\n\n    let A = 1.0 / fma(0.2877934092108062, roughness, 1.0);\n                            \n\n    let dirAlbedoV = oren_nayar_fujii_diffuse_dir_albedo(NoV, roughness, A);\n    let dirAlbedoL = oren_nayar_fujii_diffuse_dir_albedo(NoL, roughness, A);\n    let avgAlbedo = oren_nayar_fujii_diffuse_avg_albedo(roughness, A);\n\n    let s = LoV - NoL * NoV;\n    let stinv = select(s, s / max(1e-7, max(NoL, NoV)), s > 0.0);\n    \n    let lobeSingleScatter = color * A * fma(roughness, stinv, 1.0);\n    \n    let color2 = color * color;\n    let colorMultiScatter = color2 * avgAlbedo /\n                            (vec3f(1.0) - color * max(0.0, 1.0 - avgAlbedo));\n   \n    const M_FLOAT_EPS = 1e-8;\n    \n    let lobeMultiScatter = colorMultiScatter *\n                           max(M_FLOAT_EPS, 1.0 - dirAlbedoV) *\n                           max(M_FLOAT_EPS, 1.0 - dirAlbedoL) /\n                           max(M_FLOAT_EPS, 1.0 - avgAlbedo);\n\n\n    return lobeSingleScatter + lobeMultiScatter;\n}\n    \n\nfn cluster_depth_to_z_slice(\n    traced_harmonics: f32,\n    shader_sdf_distance_sqr: vec3<f32>,\n    optimized_move_x: f32,\n) -> f32 {\n\n    let j = max(0,log2(fma(traced_harmonics, shader_sdf_distance_sqr.x, shader_sdf_distance_sqr.y)) * shader_sdf_distance_sqr.z);\n\n    let cursor = min(j, optimized_move_x);\n\n    return cursor;\n\n}\n    \n\nfn cluster_from_fragment_coord(\n    coord: vec3<f32>,\n) -> vec3<u32> {\n    \n    let scene_depth = coord.z;\n\n    let cluster_z = cluster_depth_to_z_slice(scene_depth, cluster_parameters, 23 );\n\n\n    let flipped_y = f32(view.height) - coord.y;\n\n    return vec3<u32>(\n        u32( coord.x / 32 ),\n        u32( flipped_y / 32 ),\n        u32( cluster_z )\n   );\n}\n    \n\nfn grid3d_to_index( traced_harmonics: vec3<u32>, shader_sdf_distance_sqr: vec2<u32> ) -> u32{\n    return traced_harmonics.x + ( traced_harmonics.y  + traced_harmonics.z * shader_sdf_distance_sqr.y ) * shader_sdf_distance_sqr.x;\n}\n    \n\nfn divide_round_up_vec2u( traced_harmonics:vec2u, shader_sdf_distance_sqr:vec2u ) -> vec2u{\n    return (traced_harmonics + shader_sdf_distance_sqr - 1u) / shader_sdf_distance_sqr;\n}\n\n\nfn light_cluster_resolution_xy_from_screen(screen_resolution: vec2u) -> vec2u{\n    return divide_round_up_vec2u( screen_resolution, vec2(32) );\n}\n\n\nfn light_cluster_count_point(traced_harmonics: u32) -> u32 {\n    return traced_harmonics & 0xFFu;\n}\n\nfn light_cluster_count_spot(traced_harmonics: u32) -> u32 {\n    return (traced_harmonics >> 8u) & 0xFFu;\n}\n\nfn light_cluster_pack_counts(traced_harmonics: u32, shader_sdf_distance_sqr: u32) -> u32 {\n    return (traced_harmonics & 0xFFu) | ((shader_sdf_distance_sqr & 0xFFu) << 8u);\n}\n    \nstruct OrthographicCameraManager{\n    shading_normal : vec3<f32>,\n    geometric_normal : vec3<f32>,\n    position : vec3<f32>,\n    view_direction : vec3<f32>,\n}\n\nfn directional_lights_iteration_mask(traced_harmonics: ptr<storage, array<u32>, read>) -> u32 {\n    let shader_sdf_distance_sqr = traced_harmonics[4096u];\n    if (shader_sdf_distance_sqr == ~0u) {\n        return 0u;\n    }\n\n    return traced_harmonics[shader_sdf_distance_sqr + 1u];\n}\n    \nstruct AtlasPacker{\n    position : vec3<f32>,\n    distance : f32,\n    color : vec3<f32>,\n    radius : f32,\n    flags : u32,\n    near_clip_distance : f32,\n    shadow_id : u32,\n}\nfn database_read_AtlasPacker_0_2723_element(database: ptr<storage, array<u32>>, index: u32) -> AtlasPacker {\n    let page_index = index / 2723u;\n    let page_offset = index % 2723u;\n    let page_lookup_index = page_index + 0u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 87u + page_offset * 12u;\n    return AtlasPacker(\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 0]), \n            bitcast<f32>(database[base_offset + 1]), \n            bitcast<f32>(database[base_offset + 2])),\n        bitcast<f32>(database[base_offset + 3]),\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 4]), \n            bitcast<f32>(database[base_offset + 5]), \n            bitcast<f32>(database[base_offset + 6])),\n        bitcast<f32>(database[base_offset + 7]),\n        u32(database[base_offset + 8]),\n        bitcast<f32>(database[base_offset + 9]),\n        u32(database[base_offset + 10])\n    );\n}\nstruct Struct_2{\n    position : vec3<f32>,\n    distance : f32,\n    direction : vec3<f32>,\n    radius : f32,\n    color : vec3<f32>,\n    coneCos : f32,\n    penumbraCos : f32,\n    flags : u32,\n    near_clip_distance : f32,\n    shadow_id : u32,\n}\n\nfn light_get_spot_attenuation( traced_harmonics:f32, shader_sdf_distance_sqr:f32,  optimized_move_x:f32 ) -> f32{\n    return smoothstep( traced_harmonics, shader_sdf_distance_sqr, optimized_move_x );\n}\n    \n\nfn pow2(traced_harmonics:f32)->f32{\n    return traced_harmonics*traced_harmonics;\n}\n\n\nfn pow4(traced_harmonics:f32)->f32{\n    let shader_sdf_distance_sqr = traced_harmonics*traced_harmonics;\n    return shader_sdf_distance_sqr*shader_sdf_distance_sqr;\n}\n\n\nfn light_sphere_distance_attenuation(\n    distance_to_center: f32,\n    radius: f32,\n    cutoff_distance: f32\n) -> f32 {\n\n\n    const MIN_RADIUS = 1.0e-2;\n\n    let r_eff = max(radius, MIN_RADIUS);\n    let d_eff = max(distance_to_center, r_eff);\n\n    var attenuation = 1.0 / pow(d_eff, 2);\n\n    if (cutoff_distance > 0.0) {\n        let d_surface = max(0.0, distance_to_center - radius);\n        attenuation *= pow2(saturate(1.0 - pow4(d_surface / cutoff_distance)));\n    }\n\n    return attenuation;\n}\n\nstruct GpuSceneManager{\n    direction : vec3<f32>,\n    disk_radius : f32,\n    color : vec3<f32>,\n    flags : u32,\n    near_clip_distance : f32,\n    shadow_id : u32,\n}\nstruct GpuPrimitiveTypeTable{\n    direction : vec3<f32>,\n    radius : f32,\n    color : vec3<f32>,\n    distance : f32,\n}\n\nfn get_directional_light_info(  \n    traced_harmonics:GpuSceneManager \n) -> GpuPrimitiveTypeTable {\n\n    var shader_sdf_distance_sqr:GpuPrimitiveTypeTable;\n\n    shader_sdf_distance_sqr.color = traced_harmonics.color;\n    shader_sdf_distance_sqr.direction = -traced_harmonics.direction;\n    shader_sdf_distance_sqr.radius = traced_harmonics.disk_radius;\n    \n    shader_sdf_distance_sqr.distance = 1.496e11;\n\n    return shader_sdf_distance_sqr;\n\n}\n    \nstruct Struct_3{\n    index_0 : u32,\n    index_1 : u32,\n    blend : f32,\n}\nfn database_read_vec4__f32___3_8127_element(database: ptr<storage, array<u32>>, index: u32) -> vec4<f32> {\n    let page_index = index / 8127u;\n    let page_offset = index % 8127u;\n    let page_lookup_index = page_index + 12288u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 257u + page_offset * 4u;\n    return vec4<f32>(\n        bitcast<f32>(database[base_offset + 0]), \n        bitcast<f32>(database[base_offset + 1]), \n        bitcast<f32>(database[base_offset + 2]), \n        bitcast<f32>(database[base_offset + 3]));\n}\nconst PI : f32 = 3.1415926535897932384626433832795;\n\nfn build_orthonormal_matrix_n( traced_harmonics : vec3<f32> ) -> mat3x3<f32>{\n    var shader_sdf_distance_sqr: vec3<f32>;\n    var optimized_move_x: vec3<f32>;\n    \n    if(traced_harmonics.z < 0.0){\n    \n        let j = 1.0 / (1.0 - traced_harmonics.z);\n        let cursor = traced_harmonics.x * traced_harmonics.y * j;\n        \n        shader_sdf_distance_sqr = vec3(1.0 - traced_harmonics.x * traced_harmonics.x * j, -cursor, traced_harmonics.x);\n        optimized_move_x = vec3(cursor, traced_harmonics.y * traced_harmonics.y * j - 1.0, -traced_harmonics.y);\n        \n    }else{\n        let j = 1.0 / (1.0 + traced_harmonics.z);\n        let cursor = -traced_harmonics.x * traced_harmonics.y * j;\n        \n        shader_sdf_distance_sqr = vec3(1.0 - traced_harmonics.x * traced_harmonics.x * j, cursor, -traced_harmonics.x);\n        optimized_move_x = vec3(cursor, 1.0 - traced_harmonics.y * traced_harmonics.y * j, -traced_harmonics.y);\n        \n    }\n    \n    return mat3x3(\n        shader_sdf_distance_sqr,\n        optimized_move_x,\n        traced_harmonics\n    );\n}\n\n\nfn cone_sample_direction(\n    traced_harmonics: vec3<f32>, \n    shader_sdf_distance_sqr: f32, \n    optimized_move_x: vec2<f32>\n) -> vec3<f32> {\n\n    const j = PI*2.0;\n    \n\n    let cursor = optimized_move_x.x * j;\n    let t3 = sqrt(optimized_move_x.y);\n\n\n    let gi_radiance = t3 * cos(cursor);\n    let needs_destructor_signature = t3 * sin(cursor);\n\n\n    let raw_destructor_signature = shader_sdf_distance_sqr;\n    let seed_budget_ms = gi_radiance * raw_destructor_signature;\n    let texture = needs_destructor_signature * raw_destructor_signature;\n\n\n    let format = build_orthonormal_matrix_n(traced_harmonics);\n    \n\n\n\n    return normalize(format[2] + (format[0] * seed_budget_ms) + (format[1] * texture));\n}\n    \n\nfn contact_harden_pcf_kernel(\n    occluders: f32,\n    occluderDistSum: f32,\n    lightDistanceNormalized: f32\n) -> f32 {\n    let occluderAvgDist: f32 = occluderDistSum * (1.0 / occluders);\n\n\n    const w: f32 = 1.0 / 8;\n\n\n    let pcfWeight: f32 = saturate(occluderAvgDist * (1.0 / lightDistanceNormalized));\n    \n\n    var percentageOccluded: f32 = saturate(occluders * w);\n\n\n    percentageOccluded = 2.0 * percentageOccluded - 1.0;\n    \n    let occludeSign: f32 = sign(percentageOccluded);\n    \n    percentageOccluded = 1.0 - ( occludeSign * percentageOccluded );\n\n    percentageOccluded = mix(\n        (percentageOccluded * percentageOccluded * percentageOccluded),\n        percentageOccluded,\n        pcfWeight\n    );\n\n    percentageOccluded = 1.0 - percentageOccluded;\n    percentageOccluded *= occludeSign;\n    percentageOccluded = 0.5 * percentageOccluded + 0.5;\n\n    return 1.0 - percentageOccluded;\n}\n    \n\nfn max_penumbra_percentage(traced_harmonics: f32, shader_sdf_distance_sqr: f32) -> f32 {\n    let optimized_move_x = max(traced_harmonics, shader_sdf_distance_sqr);\n    \n    const j: f32 = 0.001953125;\n    const cursor: f32 = 0.5;\n    const t3: f32 = 0.70710678118654752440084436210485;\n\n    let gi_radiance = max(optimized_move_x, cursor);\n    let needs_destructor_signature = sqrt(gi_radiance) / t3;\n    let raw_destructor_signature = needs_destructor_signature * j;\n\n    return raw_destructor_signature;\n}\n    \n\nfn read_node_capacity( traced_harmonics:u32 ) -> f32{\n    return bitcast<f32>(0x3f800000 | (traced_harmonics >> 9)) - 1.0f;\n}\n    \n\nfn store_uint4(traced_harmonics:vec2<f32>) -> vec2<f32>{\n    return select( vec2(1.0), vec2(-1.0), traced_harmonics < vec2(0.0));\n}\n\n\nfn uv_octahedral_unit_encode( traced_harmonics : vec3<f32> ) -> vec2<f32>\n{\n\n    let shader_sdf_distance_sqr = abs( traced_harmonics.x ) + abs( traced_harmonics.y ) + abs( traced_harmonics.z );\n\n    var optimized_move_x = traced_harmonics.xy / shader_sdf_distance_sqr;\n\n    if(traced_harmonics.z < 0.0){\n    \n        optimized_move_x = ( 1.0 - abs(optimized_move_x.yx) ) * store_uint4( optimized_move_x.xy );\n    }\n\n    return 0.5 + 0.5 * optimized_move_x.xy;\n}\n\n\n    var<private> rnd_state : u32 = 2891336453u;\n    \n\nfn offset_wge16(traced_harmonics: u32) -> u32 {\n\n  let shader_sdf_distance_sqr = traced_harmonics * 747796405u + 2891336453u;\n  let optimized_move_x = ((shader_sdf_distance_sqr >> ((shader_sdf_distance_sqr >> 28u) + 4u)) ^ shader_sdf_distance_sqr) * 277803737u;\n  \n  return (optimized_move_x >> 22u) ^ optimized_move_x;\n  \n}\n    \n\nfn initialize() -> u32{\n  rnd_state = offset_wge16(rnd_state);\n  \n  return rnd_state;\n}\n    \n\nfn random_vec2() -> vec2<f32>{\n    \n    let traced_harmonics = initialize();\n    \n    return unpack2x16unorm(traced_harmonics);\n    \n}\n    \nstruct WgslJavaScriptCompiler{\n    atlas : vec4<f32>,\n    projection : mat4x4<f32>,\n}\nfn database_read_WgslJavaScriptCompiler_4_1635_element(database: ptr<storage, array<u32>>, index: u32) -> WgslJavaScriptCompiler {\n    let page_index = index / 1635u;\n    let page_offset = index % 1635u;\n    let page_lookup_index = page_index + 16384u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 53u + page_offset * 20u;\n    return WgslJavaScriptCompiler(\n        vec4<f32>(\n            bitcast<f32>(database[base_offset + 0]), \n            bitcast<f32>(database[base_offset + 1]), \n            bitcast<f32>(database[base_offset + 2]), \n            bitcast<f32>(database[base_offset + 3])),\n        mat4x4<f32>(\n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 4]), \n                bitcast<f32>(database[base_offset + 5]), \n                bitcast<f32>(database[base_offset + 6]), \n                bitcast<f32>(database[base_offset + 7])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 8]), \n                bitcast<f32>(database[base_offset + 9]), \n                bitcast<f32>(database[base_offset + 10]), \n                bitcast<f32>(database[base_offset + 11])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 12]), \n                bitcast<f32>(database[base_offset + 13]), \n                bitcast<f32>(database[base_offset + 14]), \n                bitcast<f32>(database[base_offset + 15])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 16]), \n                bitcast<f32>(database[base_offset + 17]), \n                bitcast<f32>(database[base_offset + 18]), \n                bitcast<f32>(database[base_offset + 19])))\n    );\n}\n\nfn ndc_to_uv( traced_harmonics: vec2<f32> ) -> vec2<f32> {\n    return fma( traced_harmonics, vec2(0.5, -0.5), vec2(0.5) );\n}     \n    \n\nfn uv_to_texel_coordinate(\n    traced_harmonics: vec2<f32>,\n    shader_sdf_distance_sqr: vec2<u32>\n) -> vec2<f32> {\n    return fma(traced_harmonics, vec2<f32>(shader_sdf_distance_sqr), vec2(-0.5));\n}\n\n\nfn get_shadow_offsets( traced_harmonics: vec3f,  shader_sdf_distance_sqr: vec3f) -> vec2f{\n    let optimized_move_x = saturate(dot(traced_harmonics, shader_sdf_distance_sqr));\n    let j = sqrt(1.0 - optimized_move_x * optimized_move_x);\n    let cursor = j / optimized_move_x;\n    return vec2f(j, min(2.0, cursor));\n}\n    \n\nfn correct_u_vs(traced_harmonics:vec4<f32>, shader_sdf_distance_sqr: vec2<f32>) -> f32{\n\n    let optimized_move_x = mix(traced_harmonics.x, traced_harmonics.y, shader_sdf_distance_sqr.x);\n    let j = mix(traced_harmonics.z, traced_harmonics.w, shader_sdf_distance_sqr.x);\n  \n    return mix(optimized_move_x, j, shader_sdf_distance_sqr.y);\n}\n    \n\nfn rgb_to_corners(\n    traced_harmonics: vec2<f32>,\n    shader_sdf_distance_sqr: vec2f,\n    optimized_move_x: f32,\n) -> f32 {\n    let j = traced_harmonics * shader_sdf_distance_sqr;\n\n    let cursor = textureGatherCompare(\n        pass_descriptor,\n        u_int,\n        j,\n        optimized_move_x,\n    );\n    \n    let t3 = fract(traced_harmonics + 0.5);\n    \n    return correct_u_vs(cursor.wzxy, t3);\n}\n    \n\nfn shadowmap_sample_5(\n    traced_harmonics: vec2f,\n    shader_sdf_distance_sqr: vec2f,\n    optimized_move_x: f32,\n) -> f32{\n    var j = floor(traced_harmonics + 0.5);\n    \n    let cursor = (traced_harmonics.x + 0.5 - j.x);\n    let t3 = (traced_harmonics.y + 0.5 - j.y);\n    \n    j -= 0.5;\n\n    let gi_radiance:f32 = (4 - 3 * cursor);\n    let needs_destructor_signature:f32 = 7;\n    let raw_destructor_signature:f32 = (1 + 3 * cursor);\n\n    let seed_budget_ms = (3 - 2 * cursor) / gi_radiance - 2;\n    let texture = (3 + cursor) / needs_destructor_signature;\n    let format = cursor / raw_destructor_signature + 2;\n\n    let dst:f32 = (4 - 3 * t3);\n    let message:f32 = 7;\n    let color_texture:f32 = (1 + 3 * t3);\n\n    let redundant = (3 - 2 * t3) / dst - 2;\n    let bucket_index_count = (3 + t3) / message;\n    let local_total_indirect_diffuse = t3 / color_texture + 2;\n\n    var num_occluded = 0.0;\n\n    num_occluded += gi_radiance * dst * rgb_to_corners(( j + vec2( seed_budget_ms, redundant)), shader_sdf_distance_sqr, optimized_move_x);\n    num_occluded += needs_destructor_signature * dst * rgb_to_corners(( j + vec2( texture, redundant)), shader_sdf_distance_sqr, optimized_move_x);\n    num_occluded += raw_destructor_signature * dst * rgb_to_corners(( j + vec2( format, redundant)), shader_sdf_distance_sqr, optimized_move_x);\n\n    num_occluded += gi_radiance * message * rgb_to_corners(( j + vec2( seed_budget_ms, bucket_index_count)), shader_sdf_distance_sqr, optimized_move_x);\n    num_occluded += needs_destructor_signature * message * rgb_to_corners(( j + vec2( texture, bucket_index_count)), shader_sdf_distance_sqr, optimized_move_x);\n    num_occluded += raw_destructor_signature * message * rgb_to_corners(( j + vec2( format, bucket_index_count)), shader_sdf_distance_sqr, optimized_move_x);\n\n    num_occluded += gi_radiance * color_texture * rgb_to_corners(( j + vec2( seed_budget_ms, local_total_indirect_diffuse)), shader_sdf_distance_sqr, optimized_move_x);\n    num_occluded += needs_destructor_signature * color_texture * rgb_to_corners(( j + vec2( texture, local_total_indirect_diffuse)), shader_sdf_distance_sqr, optimized_move_x);\n    num_occluded += raw_destructor_signature * color_texture * rgb_to_corners(( j + vec2( format, local_total_indirect_diffuse)), shader_sdf_distance_sqr, optimized_move_x);\n\n\n    num_occluded *= 1.0f / 144;\n    \n    return num_occluded;\n}\n    \n\nfn m4_projection_size(traced_harmonics: mat4x4<f32>) -> vec2f{\n\n    let shader_sdf_distance_sqr = length(traced_harmonics[0]);\n    let optimized_move_x = length(traced_harmonics[1]);\n\n    return vec2f(2.0 / shader_sdf_distance_sqr, 2.0 / optimized_move_x);\n}\n    \n\nfn sample_shadowmap_atlas_for_light(\n    projection_matrix: mat4x4f,\n    position_ws: vec3f,\n    surface_normal: vec3f,\n    light: GpuPrimitiveTypeTable,\n    atlas_aabb: vec4f,\n) -> f32{\n\n    const NORMAL_BIAS_SCALE = 0.5;\n    const DEPTH_OFFSET_BIAS = 0.0001;\n\n    let projection_scale = m4_projection_size(projection_matrix);\n    let world_pixel_size = max(projection_scale.x, projection_scale.y) / atlas_aabb.z;\n\n    let shadowmap_bias = get_shadow_offsets(surface_normal, light.direction);\n    let normal_offset = world_pixel_size * shadowmap_bias.x * surface_normal * NORMAL_BIAS_SCALE;\n\n\n    let clip_light = projection_matrix * vec4(position_ws + normal_offset, 1.0f);\n    let pos_light = clip_light.xyz / clip_light.w;\n\n    let texel_size = 1.0 / vec2f(textureDimensions(pass_descriptor).xy);\n\n    let light_uvw = vec3f(ndc_to_uv(pos_light.xy), pos_light.z);\n    let light_coords = uv_to_texel_coordinate(light_uvw.xy, vec2u(atlas_aabb.zw));\n\n    let depth_bias = DEPTH_OFFSET_BIAS * shadowmap_bias.y;\n    let depth = light_uvw.z + depth_bias;\n\n\n    let shadowmap_sample_texel = clamp(light_coords.xy, vec2(2.5), atlas_aabb.zw - 2.5) + atlas_aabb.xy;\n\n    return shadowmap_sample_5(\n        shadowmap_sample_texel,\n        texel_size,\n        depth,\n    );\n}\n    \n\nfn shadowmap_sample_spot(\n    database: ptr<storage, array<u32>>,\n    shadow_id: u32,\n    position_ws: vec3f,\n    surface_normal_ws: vec3f,\n    light: GpuPrimitiveTypeTable,\n) -> f32 {\n    let view_meta = database_read_WgslJavaScriptCompiler_4_1635_element(database, shadow_id);\n\n    return sample_shadowmap_atlas_for_light(\n        view_meta.projection,\n        position_ws,\n        surface_normal_ws,\n        light,\n        view_meta.atlas,\n    );\n}\n    \nstruct Struct_1{\n    counts : u32,\n    offset : u32,\n}\nstruct PipelineCacheKey{\n    projection_matrix : mat4x4<f32>,\n    upscale_ratio : vec2<f32>,\n    jitter : vec2<f32>,\n    width : u32,\n    height : u32,\n    frame_index : u32,\n}\nstruct CommandEncoder{\n    transform : mat4x4<f32>,\n    transform_inverse : mat4x4<f32>,\n    view_matrix : mat4x4<f32>,\n    view_matrix_inverse : mat4x4<f32>,\n    projection_matrix : mat4x4<f32>,\n    projection_matrix_inverse : mat4x4<f32>,\n    view_projection_matrix : mat4x4<f32>,\n    view_projection_matrix_inverse : mat4x4<f32>,\n    frustum : array< vec4<f32>, 6 >,\n    device_depth_to_view_space : vec4<f32>,\n}\n@group(0) @binding(0) var yz : texture_2d<f32>;\n@group(0) @binding(1) var light : texture_2d<f32>;\n@group(0) @binding(2) var ag_x : texture_2d<u32>;\n@group(0) @binding(3) var nzb : texture_2d<f32>;\n@group(0) @binding(4) var input_texture : texture_2d<u32>;\n@group(0) @binding(5) var segment_height : sampler;\n@group(1) @binding(0) var<storage, read> node : array< u32 >;\n@group(1) @binding(1) var sec_radix_passes : texture_2d<f32>;\n@group(1) @binding(2) var<uniform> cluster_parameters : vec3<f32>;\n@group(1) @binding(3) var<storage, read> cluster_lookup : array< Struct_1 >;\n@group(1) @binding(4) var<storage, read> cluster_data : array< u32 >;\n@group(1) @binding(5) var pass_descriptor : texture_depth_2d;\n@group(1) @binding(6) var u_int : sampler_comparison;\n@group(2) @binding(0) var<uniform> view : PipelineCacheKey;\n@group(2) @binding(1) var<uniform> camera : CommandEncoder;\n\nfn read_gBuffer_material(\n    i_coord: vec2<u32>\n) -> UvUnwrapper{\n    \n    let texel_pbr = textureLoad(\n        light,\n        i_coord,\n        0\n    );\n    \n    let albedo_ao_sample = textureLoad(\n        nzb,\n        i_coord,\n        0\n    );\n\n    let albedo = albedo_ao_sample.rgb;\n    \n    let emissive = rgbe9995_decode( textureLoad(\n        input_texture,\n        i_coord,\n        0\n    ).r );\n\n\n\n    let f_metalness = decode_g_buffer_metalness(texel_pbr);\n    let f_roughness = decode_g_buffer_roughness(texel_pbr);\n        \n    var material : UvUnwrapper;\n\n    material.diffuse = albedo * (1.0 - f_metalness);\n    material.occlusion = albedo_ao_sample.a;\n\n    material.roughness = max(f_roughness, 0.02);\n\n    material.specularF0 = metalness_to_specular_color(f_metalness, albedo);\n    material.specularF90 = 1.0;\n    \n    material.emissive = emissive;\n    \n    return material;\n}\n    \n\nfn get_view_space_depth( \n    traced_harmonics: f32,\n    shader_sdf_distance_sqr: CommandEncoder \n)-> f32 {\n\n    let optimized_move_x = shader_sdf_distance_sqr.device_depth_to_view_space;\n    \n    let j = optimized_move_x.y;\n    let cursor = optimized_move_x.x;\n    \n    return j / (traced_harmonics + cursor);\n}\n    \n\nfn random_initialize(traced_harmonics : vec3<u32>, shader_sdf_distance_sqr: vec3<u32>) {\n    let optimized_move_x = rgbe_write_code(traced_harmonics + shader_sdf_distance_sqr * 37u);\n\n    rnd_state = optimized_move_x.x ^ optimized_move_x.y ^ optimized_move_x.z;\n}\n    \n\nfn D_GGX( traced_harmonics:f32,  shader_sdf_distance_sqr:f32 ) -> f32{\n\n\tlet optimized_move_x =  shader_sdf_distance_sqr * (traced_harmonics - 1.0) + 1.0;\n\n\treturn traced_harmonics / (PI * optimized_move_x * optimized_move_x);\n\n}\n\n\nfn V_GGX_SmithCorrelated( \n    traced_harmonics:f32,\n    shader_sdf_distance_sqr:f32,\n    optimized_move_x:f32\n) -> f32{\n\n\tlet j = pow2( traced_harmonics );\n\n\n\tlet cursor = shader_sdf_distance_sqr * sqrt( fma( pow2( optimized_move_x ), ( 1.0 - j ), j ) );\n\tlet t3 = optimized_move_x * sqrt( fma( pow2( shader_sdf_distance_sqr ), ( 1.0 - j ), j ) );\n\n\treturn 0.5 / max( cursor + t3, EPSILON );\n\t\n}\n\n\nfn BRDF_GGX(\n\n    traced_harmonics: f32,\n    shader_sdf_distance_sqr: f32,\n    optimized_move_x: f32,\n    j: f32,\n    \n    cursor: vec3<f32>,\n    t3: f32,\n    \n    gi_radiance: f32\n  \n) -> vec3<f32> {\n\n\n\tlet needs_destructor_signature = F_Hauber( cursor, t3, j );\n\n\n\tlet raw_destructor_signature = V_GGX_SmithCorrelated( gi_radiance, traced_harmonics, shader_sdf_distance_sqr );\n\t\n\n\tlet seed_budget_ms = D_GGX( gi_radiance * gi_radiance, optimized_move_x );\n\n\treturn needs_destructor_signature * raw_destructor_signature * seed_budget_ms ;\n}\n    \n\nfn re_direct_physical( \n    traced_harmonics: GpuPrimitiveTypeTable,\n    shader_sdf_distance_sqr: OrthographicCameraManager,\n    optimized_move_x: UvUnwrapper,\n    j: ptr<function, ExternalTextureContext>\n) {\n\n\n\n    let cursor = shader_sdf_distance_sqr.shading_normal;\n    let t3 = traced_harmonics.direction;\n    let gi_radiance = shader_sdf_distance_sqr.view_direction;\n\n\tlet needs_destructor_signature = normalize( t3 + gi_radiance );\n\n\tlet raw_destructor_signature = dot( cursor, t3 );\n\tlet seed_budget_ms = saturate( raw_destructor_signature );\n\tlet texture = saturate( dot( cursor, gi_radiance ) );\n\tlet format = saturate( dot( gi_radiance, needs_destructor_signature ) );\n\tlet dst = saturate( dot( gi_radiance, t3 ) );\n\n\tlet message = optimized_move_x.roughness * optimized_move_x.roughness;\n\n\n\tlet color_texture = clamp(traced_harmonics.radius, 0.0, 0.999);\n\tlet redundant = sqrt(1.0 - color_texture * color_texture);\n\tlet bucket_index_count = color_texture / redundant;\n\tlet local_total_indirect_diffuse = get_bent_noh_squared(seed_budget_ms, texture, dst, bucket_index_count);\n\n\tlet num_occluded = saturate(message + color_texture / 3.0);\n\tlet u8array = (message / num_occluded) * (message / num_occluded);\n\n\n\tlet meshlet_buckets = saturate((raw_destructor_signature + color_texture) / (1.0 + color_texture));\n\tlet result = meshlet_buckets * traced_harmonics.color;\n\n\tlet allocator_textures = BRDF_GGX(\n\t    meshlet_buckets,\n\t    texture,\n\t    local_total_indirect_diffuse,\n\t    format,\n\t    optimized_move_x.specularF0,\n\t    optimized_move_x.specularF90,\n\t    num_occluded,\n\t);\n\n    (*j).specular += result * allocator_textures * u8array;\n\n    let meshlet = oren_nayar_compensated_diffuse(\n        texture,\n        meshlet_buckets,\n        dst,\n        optimized_move_x.roughness,\n        optimized_move_x.diffuse,\n    );\n    \n\t(*j).diffuse += result * meshlet * RECIPROCAL_PI;\n\t\n}\n    \n\nfn light_cluster_metadata_by_position(\n\n    traced_harmonics: vec2f,\n    shader_sdf_distance_sqr: f32,\n    optimized_move_x: vec2u\n    \n) -> Struct_1{\n\n    let j = light_cluster_resolution_xy_from_screen( optimized_move_x );\n\n    let cursor = cluster_from_fragment_coord( vec3(traced_harmonics, shader_sdf_distance_sqr) );\n    let t3 = grid3d_to_index( cursor, j );\n        \n    return cluster_lookup[t3];\n        \n}\n    \n\nfn get_point_light_info(\n    traced_harmonics:AtlasPacker,\n    shader_sdf_distance_sqr: vec3<f32>,\n) -> GpuPrimitiveTypeTable {\n\n    let optimized_move_x = traced_harmonics.position.xyz - shader_sdf_distance_sqr;\n\n    var j : GpuPrimitiveTypeTable;\n\n    j.direction = normalize(optimized_move_x);\n\n    let cursor = length(optimized_move_x);\n\n    let t3 = light_sphere_distance_attenuation(cursor, traced_harmonics.radius, traced_harmonics.distance);\n\n    j.color = traced_harmonics.color * t3;\n\n    j.radius = traced_harmonics.radius / cursor;\n\n    j.distance = max(0.0, cursor - traced_harmonics.radius);\n\n    return j;\n\n}\n    \n\nfn get_point_light_info_by_index(\n    database: ptr<storage, array<u32>>,\n    index: u32,\n    position_ws: vec3<f32>\n) -> GpuPrimitiveTypeTable {\n    let light = database_read_AtlasPacker_0_2723_element(database, index);\n    return get_point_light_info(light, position_ws);\n}\n    \nfn database_read_Struct_2_2_2043_element(database: ptr<storage, array<u32>>, index: u32) -> Struct_2 {\n    let page_index = index / 2043u;\n    let page_offset = index % 2043u;\n    let page_lookup_index = page_index + 8192u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 65u + page_offset * 16u;\n    return Struct_2(\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 0]), \n            bitcast<f32>(database[base_offset + 1]), \n            bitcast<f32>(database[base_offset + 2])),\n        bitcast<f32>(database[base_offset + 3]),\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 4]), \n            bitcast<f32>(database[base_offset + 5]), \n            bitcast<f32>(database[base_offset + 6])),\n        bitcast<f32>(database[base_offset + 7]),\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 8]), \n            bitcast<f32>(database[base_offset + 9]), \n            bitcast<f32>(database[base_offset + 10])),\n        bitcast<f32>(database[base_offset + 11]),\n        bitcast<f32>(database[base_offset + 12]),\n        u32(database[base_offset + 13]),\n        bitcast<f32>(database[base_offset + 14]),\n        u32(database[base_offset + 15])\n    );\n}\nfn database_read_GpuSceneManager_1_2723_element(database: ptr<storage, array<u32>>, index: u32) -> GpuSceneManager {\n    let page_index = index / 2723u;\n    let page_offset = index % 2723u;\n    let page_lookup_index = page_index + 4096u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 87u + page_offset * 12u;\n    return GpuSceneManager(\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 0]), \n            bitcast<f32>(database[base_offset + 1]), \n            bitcast<f32>(database[base_offset + 2])),\n        bitcast<f32>(database[base_offset + 3]),\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 4]), \n            bitcast<f32>(database[base_offset + 5]), \n            bitcast<f32>(database[base_offset + 6])),\n        u32(database[base_offset + 7]),\n        bitcast<f32>(database[base_offset + 8]),\n        u32(database[base_offset + 9])\n    );\n}\n\nfn get_spot_light_info(\n    traced_harmonics: Struct_2,\n    shader_sdf_distance_sqr: vec3<f32>,\n) -> GpuPrimitiveTypeTable{\n\n    var optimized_move_x : GpuPrimitiveTypeTable;\n\n    let j = traced_harmonics.position - shader_sdf_distance_sqr;\n\n    optimized_move_x.direction = normalize( j );\n\n    let cursor = length( j );\n\n    optimized_move_x.distance = cursor;\n    optimized_move_x.radius = traced_harmonics.radius / cursor;\n\n    let t3 = dot( optimized_move_x.direction, -traced_harmonics.direction );\n\n    let gi_radiance = light_get_spot_attenuation( traced_harmonics.coneCos, traced_harmonics.penumbraCos, t3 );\n\n\n    if ( gi_radiance > 0.0 ) {\n\n        optimized_move_x.color = traced_harmonics.color * gi_radiance;\n        optimized_move_x.color *= light_sphere_distance_attenuation( cursor, traced_harmonics.radius, traced_harmonics.distance );\n\n    } else {\n\n        optimized_move_x.color = vec3( 0.0 );\n\n    }\n\n\n\n    return optimized_move_x;\n}\n    \nfn database_read__5_545_element(database: ptr<storage, array<u32>>, index: u32) -> array< WgslJavaScriptCompiler, 3 > {\n    let page_index = index / 545u;\n    let page_offset = index % 545u;\n    let page_lookup_index = page_index + 20480u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 19u + page_offset * 60u;\n    return array< WgslJavaScriptCompiler, 3 >(\n        WgslJavaScriptCompiler(\n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 0]), \n                bitcast<f32>(database[base_offset + 1]), \n                bitcast<f32>(database[base_offset + 2]), \n                bitcast<f32>(database[base_offset + 3])),\n            mat4x4<f32>(\n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 4]), \n                    bitcast<f32>(database[base_offset + 5]), \n                    bitcast<f32>(database[base_offset + 6]), \n                    bitcast<f32>(database[base_offset + 7])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 8]), \n                    bitcast<f32>(database[base_offset + 9]), \n                    bitcast<f32>(database[base_offset + 10]), \n                    bitcast<f32>(database[base_offset + 11])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 12]), \n                    bitcast<f32>(database[base_offset + 13]), \n                    bitcast<f32>(database[base_offset + 14]), \n                    bitcast<f32>(database[base_offset + 15])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 16]), \n                    bitcast<f32>(database[base_offset + 17]), \n                    bitcast<f32>(database[base_offset + 18]), \n                    bitcast<f32>(database[base_offset + 19])))\n        ),\n        WgslJavaScriptCompiler(\n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 20]), \n                bitcast<f32>(database[base_offset + 21]), \n                bitcast<f32>(database[base_offset + 22]), \n                bitcast<f32>(database[base_offset + 23])),\n            mat4x4<f32>(\n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 24]), \n                    bitcast<f32>(database[base_offset + 25]), \n                    bitcast<f32>(database[base_offset + 26]), \n                    bitcast<f32>(database[base_offset + 27])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 28]), \n                    bitcast<f32>(database[base_offset + 29]), \n                    bitcast<f32>(database[base_offset + 30]), \n                    bitcast<f32>(database[base_offset + 31])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 32]), \n                    bitcast<f32>(database[base_offset + 33]), \n                    bitcast<f32>(database[base_offset + 34]), \n                    bitcast<f32>(database[base_offset + 35])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 36]), \n                    bitcast<f32>(database[base_offset + 37]), \n                    bitcast<f32>(database[base_offset + 38]), \n                    bitcast<f32>(database[base_offset + 39])))\n        ),\n        WgslJavaScriptCompiler(\n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 40]), \n                bitcast<f32>(database[base_offset + 41]), \n                bitcast<f32>(database[base_offset + 42]), \n                bitcast<f32>(database[base_offset + 43])),\n            mat4x4<f32>(\n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 44]), \n                    bitcast<f32>(database[base_offset + 45]), \n                    bitcast<f32>(database[base_offset + 46]), \n                    bitcast<f32>(database[base_offset + 47])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 48]), \n                    bitcast<f32>(database[base_offset + 49]), \n                    bitcast<f32>(database[base_offset + 50]), \n                    bitcast<f32>(database[base_offset + 51])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 52]), \n                    bitcast<f32>(database[base_offset + 53]), \n                    bitcast<f32>(database[base_offset + 54]), \n                    bitcast<f32>(database[base_offset + 55])), \n                vec4<f32>(\n                    bitcast<f32>(database[base_offset + 56]), \n                    bitcast<f32>(database[base_offset + 57]), \n                    bitcast<f32>(database[base_offset + 58]), \n                    bitcast<f32>(database[base_offset + 59])))\n        )\n    );\n}\n\nfn shadowmap_csm_compute_cascade_blended(\n    position_ws: vec3f,\n    view_direction_ws: vec3f,\n    cascades: array<WgslJavaScriptCompiler, 3>,\n) -> Struct_3 {\n\n    const BLEND_BORDER_FRACTION: f32 = 0.07;\n\n    for(var i = 0u; i < 3; i++){\n        let m = cascades[i];\n\n\n        var cascade_position = (m.projection * vec4(position_ws, 1.0f)).xyz;\n\n\n        cascade_position.z = cascade_position.z * 2.0 - 1.0;\n\n        let texture_size = m.atlas.zw;\n        let projection_bias = texture_size / (texture_size + vec2(5));\n        let valid_extents = vec3(projection_bias, 1.0);\n\n        if(all(abs(cascade_position) < valid_extents)){\n            var info = Struct_3(\n    i,\n    i,\n    0.0,\n);\n\n\n            if (i < 2) {\n\n                let next_index = i + 1u;\n\n\n                let m1 = cascades[next_index];\n\n\n                var cascade_position1 = (m1.projection * vec4(position_ws, 1.0f)).xyz;\n\n\n                cascade_position1.z = cascade_position1.z * 2.0 - 1.0;\n\n                let texture_size1 = m1.atlas.zw;\n                let projection_bias1 = texture_size1 / (texture_size1 + vec2(5));\n                let valid_extents1 = vec3(projection_bias1, 1.0);\n\n                if(all(abs(cascade_position1) < valid_extents1)){\n\n                    let pos = cascade_position;\n                    let dir = normalize((m.projection * vec4(-view_direction_ws, 0.0)).xyz);\n\n\n                    let inv_dir = 1.0 / dir;\n\n                    let t1 = (-valid_extents - pos) * inv_dir;\n                    let t2 = ( valid_extents - pos) * inv_dir;\n\n                    let tmin = min(t1, t2);\n                    let tmax = max(t1, t2);\n\n\n                    let t_enter = max(max(tmin.x, tmin.y), tmin.z);\n                    let t_exit  = min(min(tmax.x, tmax.y), tmax.z);\n\n\n                    let total_span = t_exit - t_enter;\n\n\n                    let dist_remaining_normalized = t_exit / total_span;\n\n\n                    if (dist_remaining_normalized < BLEND_BORDER_FRACTION) {\n\n                        let weight = 1.0 - (dist_remaining_normalized / BLEND_BORDER_FRACTION);\n\n                        info.blend = smoothstep(0.0, 1.0, weight );\n                        info.index_1 = next_index;\n                    }\n                }\n            }\n\n            return info;\n        }\n\n    }\n\n\n    return Struct_3(\n    ~0u,\n    ~0u,\n    0.0,\n);\n}\n    \n\nfn shadowmap_sample_directional(\n    database: ptr<storage, array<u32>>,\n    shadow_id: u32,\n    position_ws: vec3f,\n    view_direction_ws: vec3f,\n    surface_normal_ws: vec3f,\n    light: GpuPrimitiveTypeTable,\n) -> f32 {\n    let cascades = database_read__5_545_element(database, shadow_id);\n\n    let cascade_info = shadowmap_csm_compute_cascade_blended(\n        position_ws,\n        view_direction_ws,\n        cascades,\n    );\n\n    if(cascade_info.index_0 == ~0u){\n\n        return 1.0;\n    }\n\n    let cascade_meta_0 = cascades[cascade_info.index_0];\n\n    let visibility_0 = sample_shadowmap_atlas_for_light(\n        cascade_meta_0.projection,\n        position_ws,\n        surface_normal_ws,\n        light,\n        cascade_meta_0.atlas,\n    );\n\n    if(cascade_info.blend <= 0.0){\n        return visibility_0;\n    }\n\n    let cascade_meta_1 = cascades[cascade_info.index_1];\n\n    let visibility_1 = sample_shadowmap_atlas_for_light(\n        cascade_meta_1.projection,\n        position_ws,\n        surface_normal_ws,\n        light,\n        cascade_meta_1.atlas,\n    );\n\n    return mix(visibility_0, visibility_1, cascade_info.blend);\n}\n    \n\nfn random() -> f32{\n    \n    let traced_harmonics = initialize();\n    \n    return read_node_capacity( traced_harmonics );\n\n}\n    \n\nfn random_round_vec2(traced_harmonics: vec2<f32>) -> vec2<f32>{\n    \n    let shader_sdf_distance_sqr = select( vec2(0.0), vec2(1.0),  fract(traced_harmonics) > random_vec2() );\n    \n    return floor(traced_harmonics) + shader_sdf_distance_sqr;\n    \n}\n    \n\nfn shadowmap_sample_point(\n    database: ptr<storage, array<u32>>,\n    shadow_id: u32,\n    position_ws: vec3f,\n    surface_normal_ws: vec3f,\n    light_position_ws: vec3f,\n    light_max_distance: f32,\n    light_radius: f32,\n) -> f32 {\n    const NORMAL_BIAS_SCALE = 2.0;\n    const DEPTH_OFFSET_BIAS = 0.0001;\n\n    let atlas_aabb = database_read_vec4__f32___3_8127_element(database, shadow_id);\n\n\n    let to_light_unbiased = light_position_ws - position_ws;\n    let dist_to_light_unbiased = length(to_light_unbiased);\n    let light_direction = to_light_unbiased / max(dist_to_light_unbiased, 1e-6);\n\n    let world_pixel_size = 2.0 * dist_to_light_unbiased / max(atlas_aabb.z, 1.0);\n    let shadowmap_bias = get_shadow_offsets(surface_normal_ws, light_direction);\n    let normal_offset = world_pixel_size * shadowmap_bias.x * surface_normal_ws * NORMAL_BIAS_SCALE;\n\n\n    let to_surface = (position_ws + normal_offset) - light_position_ws;\n    let distance_to_center = length(to_surface);\n\n\n    let dir_light_to_surface = to_surface / max(distance_to_center, 1e-6);\n\n    let face_size = vec2u(atlas_aabb.zw);\n\n\n    let radial = distance_to_center / max(light_max_distance, 1e-6);\n    let depth_bias = DEPTH_OFFSET_BIAS * shadowmap_bias.y;\n    let reference_depth = (1.0 - saturate(radial)) + depth_bias;\n\n\n    let cone_aperture = 2.0 * max_penumbra_percentage(light_radius, light_radius);\n\n    var occluders: f32 = 0.0;\n    var occluder_dist_sum: f32 = 0.0;\n\n    for (var tap: u32 = 0u; tap < 8u; tap++) {\n\n        let xi = random_vec2();\n\n        let perturbed_dir = cone_sample_direction(dir_light_to_surface, cone_aperture, xi);\n\n        let octahedral_uv = uv_octahedral_unit_encode(perturbed_dir);\n        let local_texel = uv_to_texel_coordinate(octahedral_uv, face_size);\n\n\n        let rounded_local_texel = random_round_vec2(local_texel);\n\n        let sample_texel = vec2<u32>(rounded_local_texel) + vec2<u32>(atlas_aabb.xy);\n\n        let stored = textureLoad(pass_descriptor, sample_texel, 0);\n        let dist = stored - reference_depth;\n\n\n        let occluded = step(0.0, dist);\n\n        occluders += occluded;\n        occluder_dist_sum += dist * occluded;\n    }\n\n    return contact_harden_pcf_kernel(\n        occluders,\n        occluder_dist_sum,\n        reference_depth,\n\n    );\n}\n    \n\nfn shadowmap_get_point_light_visibility(\n    database: ptr<storage, array<u32>>,\n    point_index: u32,\n    position_ws: vec3f,\n    surface_normal_ws: vec3f,\n) -> f32 {\n    let light = database_read_AtlasPacker_0_2723_element(database, point_index);\n\n    if ((light.flags & 1u) == 0u) {\n        return 1.0;\n    }\n\n    let to_light = light.position - position_ws;\n    if (dot(to_light, surface_normal_ws) < 0.0) {\n        return 0.0;\n    }\n\n    return shadowmap_sample_point(\n        database,\n        light.shadow_id,\n        position_ws,\n        surface_normal_ws,\n        light.position,\n        light.distance,\n        light.radius,\n    );\n}\n\nfn shadowmap_get_spot_light_visibility(\n    database: ptr<storage, array<u32>>,\n    spot_index: u32,\n    position_ws: vec3f,\n    surface_normal_ws: vec3f,\n) -> f32 {\n    let light = database_read_Struct_2_2_2043_element(database, spot_index);\n\n    if ((light.flags & 1u) == 0u) {\n        return 1.0;\n    }\n\n    let incident = get_spot_light_info(light, position_ws);\n\n    if (dot(incident.direction, surface_normal_ws) < 0.0) {\n        return 0.0;\n    }\n\n    return shadowmap_sample_spot(\n        database,\n        light.shadow_id,\n        position_ws,\n        surface_normal_ws,\n        incident,\n    );\n}\n\nfn shadowmap_get_directional_light_visibility(\n    database: ptr<storage, array<u32>>,\n    directional_index: u32,\n    position_ws: vec3f,\n    view_direction_ws: vec3f,\n    surface_normal_ws: vec3f,\n) -> f32 {\n    let light = database_read_GpuSceneManager_1_2723_element(database, directional_index);\n\n    if ((light.flags & 1u) == 0u) {\n        return 1.0;\n    }\n\n    let incident = get_directional_light_info(light);\n\n    if (dot(incident.direction, surface_normal_ws) < 0.0) {\n        return 0.0;\n    }\n\n    return shadowmap_sample_directional(\n        database,\n        light.shadow_id,\n        position_ws,\n        view_direction_ws,\n        surface_normal_ws,\n        incident,\n    );\n}\n    \n\nfn get_spot_light_info_by_index(\n    database: ptr<storage, array<u32>>,\n    index: u32,\n    position_ws: vec3<f32>\n) -> GpuPrimitiveTypeTable {\n    let light = database_read_Struct_2_2_2043_element(database, index);\n    return get_spot_light_info(light, position_ws);\n}\n    \n\nfn get_directional_light_info_by_index(\n    database: ptr<storage, array<u32>>,\n    index: u32\n) -> GpuPrimitiveTypeTable {\n    let light = database_read_GpuSceneManager_1_2723_element(database, index);\n    return get_directional_light_info(light);\n}\n    \n\nfn shade_standard_material_direct(\n    traced_harmonics: UvUnwrapper,\n    shader_sdf_distance_sqr: OrthographicCameraManager,\n    optimized_move_x: vec2<f32>,\n    cursor: f32,\n) -> vec3<f32>{\n\n    var t3 : ExternalTextureContext;\n\n\n    var gi_radiance = directional_lights_iteration_mask(&node);\n    while (gi_radiance != 0u) {\n        let needs_destructor_signature = countTrailingZeros(gi_radiance);\n        gi_radiance &= ~(1u << needs_destructor_signature);\n\n        var raw_destructor_signature = get_directional_light_info_by_index(&node, needs_destructor_signature);\n\n        let seed_budget_ms = shadowmap_get_directional_light_visibility(\n            &node,\n            needs_destructor_signature,\n            shader_sdf_distance_sqr.position,\n            shader_sdf_distance_sqr.view_direction,\n            shader_sdf_distance_sqr.shading_normal\n        );\n\n        raw_destructor_signature.color *= seed_budget_ms;\n\n        re_direct_physical(raw_destructor_signature, shader_sdf_distance_sqr, traced_harmonics, &t3);\n    }\n\n\n    let texture = light_cluster_metadata_by_position(\n        optimized_move_x.xy,\n        cursor,\n        vec2(view.width, view.height)\n    );\n\n    let format = texture.offset;\n    let dst = light_cluster_count_point(texture.counts);\n    let message = light_cluster_count_spot(texture.counts);\n\n    for (var color_texture = 0u; color_texture < dst; color_texture++) {\n        let redundant = cluster_data[format + color_texture];\n\n        var raw_destructor_signature = get_point_light_info_by_index(&node, redundant, shader_sdf_distance_sqr.position);\n\n        let seed_budget_ms = shadowmap_get_point_light_visibility(\n            &node,\n            redundant,\n            shader_sdf_distance_sqr.position,\n            shader_sdf_distance_sqr.shading_normal\n        );\n\n        raw_destructor_signature.color *= seed_budget_ms;\n\n        if (all(raw_destructor_signature.color == vec3(0.0))) {\n            continue;\n        }\n\n        re_direct_physical(raw_destructor_signature, shader_sdf_distance_sqr, traced_harmonics, &t3);\n    }\n\n    for (var bucket_index_count = 0u; bucket_index_count < message; bucket_index_count++) {\n        let redundant = cluster_data[format + dst + bucket_index_count];\n\n        var raw_destructor_signature = get_spot_light_info_by_index(&node, redundant, shader_sdf_distance_sqr.position);\n\n        let seed_budget_ms = shadowmap_get_spot_light_visibility(\n            &node,\n            redundant,\n            shader_sdf_distance_sqr.position,\n            shader_sdf_distance_sqr.shading_normal\n        );\n\n        raw_destructor_signature.color *= seed_budget_ms;\n\n        if (all(raw_destructor_signature.color == vec3(0.0))) {\n            continue;\n        }\n\n        re_direct_physical(raw_destructor_signature, shader_sdf_distance_sqr, traced_harmonics, &t3);\n    }\n\n    var local_total_indirect_diffuse = vec3(traced_harmonics.emissive);\n\n    let num_occluded = t3.diffuse + t3.specular + local_total_indirect_diffuse;\n\n    return num_occluded;\n}\n    \n\n@fragment\nfn main(\n\n    @builtin(position) coord : vec4<f32>,\n    @location(0) uv: vec2<f32>,\n  \n) -> @location(0) vec4<f32> {\n\n    let i_coord = vec2<u32>(coord.xy);\n    \n\n    random_initialize( vec3u(i_coord, view.frame_index), vec3u( 0xEE6B2807, 7, 0xD0974829 ) );\n        \n    let texel_depth = textureLoad(yz,\n        i_coord,\n        0\n    ).r;\n    \n    let view_depth = get_view_space_depth(texel_depth, camera);\n    \n    let position_ws = project_position_from_depth(uv, texel_depth, camera.view_projection_matrix_inverse );\n           \n    let camera_position_ws = mat4_extract_position(camera.transform);\n    \n    let view_direction = normalize(camera_position_ws - position_ws);\n    \n    let g_buffer_sample_normal = textureLoad(ag_x, i_coord, 0);\n    \n    let shading_normal = decode_g_buffer_normal(g_buffer_sample_normal.xy);\n    let geometric_normal = decode_g_buffer_normal(g_buffer_sample_normal.zw);\n                       \n    let geometry = OrthographicCameraManager(\n        shading_normal, \n        geometric_normal,\n        position_ws,\n        view_direction,\n    );\n    \n    let material = read_gBuffer_material(i_coord);\n            \n    let result = shade_standard_material_direct(\n        material,\n        geometry,\n        coord.xy,\n        view_depth,\n    );\n    \n    return vec4(result, 1.0);\n}\n";
