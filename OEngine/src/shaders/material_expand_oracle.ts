/**
 * material_expand_oracle：定义对应渲染阶段使用的 WGSL 着色器代码。
 */

export const MATERIAL_EXPAND_VERTEX_WGSL = "struct EventDispatcher{\n    albedo_color : vec4<f32>,\n    emissive_factor : vec3<f32>,\n    id : u32,\n    ambient_factors : vec2<f32>,\n    metallic_factor : f32,\n    roughness_factor : f32,\n    transmission_factor : f32,\n    ior_factor : f32,\n    transparency_mode : u32,\n    draw_mode : u32,\n    draw_side : u32,\n}\n\nconst pos : array< vec2<f32>, 3 > = array< vec2<f32>, 3 >(\n  vec2<f32>(-1.0, -1.0),\n  vec2<f32>(3.0, -1.0),\n  vec2<f32>(-1.0, 3.0)\n);\n\n@group(0) @binding(0) var<uniform> material_info: EventDispatcher;\n\nstruct FrameAllocatorNative {\n    @builtin(position) position : vec4<f32>,\n}\n\n@vertex\nfn main(\n  @builtin(vertex_index) traced_harmonics : u32\n) -> FrameAllocatorNative {\n  \n  var shader_sdf_distance_sqr: FrameAllocatorNative;\n  \n  let optimized_move_x = pos[traced_harmonics];\n  \n  shader_sdf_distance_sqr.position = vec4<f32>(optimized_move_x, f32(material_info.id)/16777216 , 1.0);\n\n  return shader_sdf_distance_sqr;\n}\n";

export const MATERIAL_EXPAND_FRAGMENT_WGSL = "\nfn mat4_extract_position(traced_harmonics: mat4x4<f32>) -> vec3<f32>{\n    return traced_harmonics[3].xyz;\n}\n    \n\nfn store_uint4(traced_harmonics:vec2<f32>) -> vec2<f32>{\n    return select( vec2(1.0), vec2(-1.0), traced_harmonics < vec2(0.0));\n}\n\n\nfn uv_octahedral_unit_encode( traced_harmonics : vec3<f32> ) -> vec2<f32>\n{\n\n    let shader_sdf_distance_sqr = abs( traced_harmonics.x ) + abs( traced_harmonics.y ) + abs( traced_harmonics.z );\n\n    var optimized_move_x = traced_harmonics.xy / shader_sdf_distance_sqr;\n\n    if(traced_harmonics.z < 0.0){\n    \n        optimized_move_x = ( 1.0 - abs(optimized_move_x.yx) ) * store_uint4( optimized_move_x.xy );\n    }\n\n    return 0.5 + 0.5 * optimized_move_x.xy;\n}\n\n\nfn encode_g_buffer_normal(traced_harmonics: vec3<f32>) -> vec2<u32>{\n    let shader_sdf_distance_sqr = uv_octahedral_unit_encode(traced_harmonics);\n\n    return vec2<u32>(shader_sdf_distance_sqr * 65535.0);\n}\n    \n\nfn interpolate_attribute_3f32(\n    traced_harmonics:vec3<f32>,\n    shader_sdf_distance_sqr:vec3<f32>,\n    optimized_move_x:vec3<f32>,\n    j:vec3<f32>\n) -> vec3<f32>{\n\n    return vec3<f32>(\n        traced_harmonics[0] * j.x + shader_sdf_distance_sqr[0] * j.y + optimized_move_x[0] * j.z,\n        traced_harmonics[1] * j.x + shader_sdf_distance_sqr[1] * j.y + optimized_move_x[1] * j.z,\n        traced_harmonics[2] * j.x + shader_sdf_distance_sqr[2] * j.y + optimized_move_x[2] * j.z,\n    );\n\n}\nstruct ShadeMaterialInfo{\n    uv : vec2<f32>,\n    ddx : vec2<f32>,\n    ddy : vec2<f32>,\n}\nstruct BufferAllocator{\n    lambda : vec3<f32>,\n    ddx : vec3<f32>,\n    ddy : vec3<f32>,\n}\n\nfn interpolate_with_derivatives(\n    traced_harmonics: BufferAllocator,\n    shader_sdf_distance_sqr:f32,\n    optimized_move_x:f32,\n    j:f32\n) -> vec3<f32>{\n\n        let cursor = vec3<f32>(shader_sdf_distance_sqr, optimized_move_x, j);\n        \n        return vec3<f32>(\n                dot(cursor, traced_harmonics.lambda),\n                dot(cursor, traced_harmonics.ddx),\n                dot(cursor, traced_harmonics.ddy)\n        );\n        \n}\n\nfn barycentric_interpolate_uv(\n    bary: BufferAllocator,\n    uv0: vec2<f32>,\n    uv1: vec2<f32>,\n    uv2: vec2<f32> \n) -> ShadeMaterialInfo{\n\n    let m_0 = interpolate_with_derivatives(bary, uv0.x, uv1.x, uv2.x);\n    let m_1 = interpolate_with_derivatives(bary, uv0.y, uv1.y, uv2.y);\n\n    return ShadeMaterialInfo (\n        vec2<f32>(m_0.x, m_1.x),\n        vec2<f32>(m_0.y, m_1.y),\n        vec2<f32>(m_0.z, m_1.z),\n    );\n}\n\n\nfn compute_normal_matrix_from_m4( traced_harmonics: mat4x4<f32> ) -> mat3x3<f32>{\n    let shader_sdf_distance_sqr = traced_harmonics[0].xyz;\n    let optimized_move_x = traced_harmonics[1].xyz;\n    let j = traced_harmonics[2].xyz;\n    \n    return mat3x3<f32>(\n        cross(optimized_move_x, j),\n        cross(j, shader_sdf_distance_sqr),\n        cross(shader_sdf_distance_sqr, optimized_move_x)\n    );\n}\n\n\nfn anti_alias_roughness_kaplanyan (\n        traced_harmonics:f32, \n        shader_sdf_distance_sqr:vec3<f32>, \n        optimized_move_x: vec3<f32>,\n        j: vec3<f32>\n) -> f32{\n    \n    const cursor = 0.50;\n    const t3 = 0.18;\n    \n    const gi_radiance = cursor * cursor;\n    \n\n    let needs_destructor_signature = shader_sdf_distance_sqr;\n    \n    let raw_destructor_signature = vec2(dot(needs_destructor_signature, optimized_move_x), dot(needs_destructor_signature, j));\n    \n\n    let seed_budget_ms = fwidth(raw_destructor_signature);\n    \n    let texture = clamp( max(seed_budget_ms.x , seed_budget_ms.y), 1e-3, 0.3);\n    \n    let format =  gi_radiance * texture * texture;\n    \n    let dst = min( t3, format * 2.0 );\n    \n\n    let message = sqrt(traced_harmonics * traced_harmonics + dst);          \n\n    return message;\n}\n\nfn build_orthonormal_matrix_nt(\n    traced_harmonics:vec3<f32>,\n    shader_sdf_distance_sqr:vec4<f32>\n) -> mat3x3<f32>{\n    let optimized_move_x = shader_sdf_distance_sqr.xyz;\n    \n\n    let j = normalize(optimized_move_x - traced_harmonics * dot(traced_harmonics, optimized_move_x));\n    \n    let cursor = normalize(cross(traced_harmonics, j) * shader_sdf_distance_sqr.w);\n\n    return mat3x3(\n        j,\n        cursor,\n        traced_harmonics\n    );\n    \n}\n\nstruct GpuAnimationManager{\n    position : vec3<f32>,\n    normal : vec3<f32>,\n    tangent : vec4<f32>,\n    uv : vec2<f32>,\n    uv1 : vec2<f32>,\n    color : vec3<f32>,\n}\n\nfn meshlet_compute_attribute_section_offset(\n    traced_harmonics: Struct_0, \n) -> u32{\n    \n\n    \n    let shader_sdf_distance_sqr = ( traced_harmonics.primitive_count * 3u  + 3u) >> 2u;\n    \n    return traced_harmonics.address + shader_sdf_distance_sqr;\n    \n}\n    \n\nfn read_meshlet_attribute_vec3f(traced_harmonics:u32) -> vec3f{    \n    return vec3(\n        history_raw_type[traced_harmonics],\n        history_raw_type[traced_harmonics + 1],\n        history_raw_type[traced_harmonics + 2],\n    );\n}\n    \n\nfn read_meshlet_attribute_vec2f(traced_harmonics:u32) -> vec2f{    \n    return vec2(\n        history_raw_type[traced_harmonics],\n        history_raw_type[traced_harmonics+1],\n    );\n}\n    \n\nfn receive_instance_bounds(traced_harmonics:f32) -> f32{\n    return select(1.0, -1.0, traced_harmonics < 0.0);\n}\n\n\nfn uv_octahedral_unit_decode( traced_harmonics: vec2<f32> ) -> vec3<f32>{\n    var shader_sdf_distance_sqr = fma(traced_harmonics, vec2(2.0), vec2(-1.0));\n\n    var optimized_move_x = vec3<f32>(shader_sdf_distance_sqr, 1.0 - abs(shader_sdf_distance_sqr.x) - abs(shader_sdf_distance_sqr.y));\n\n    let j = max(-optimized_move_x.z,0.0);\n    \n    optimized_move_x.x += select( j, -j, optimized_move_x.x > 0.0);\n    optimized_move_x.y += select( j, -j, optimized_move_x.y > 0.0);\n\n    return normalize( optimized_move_x );\n}\n\n\nfn decode_vertex_tangent(traced_harmonics : u32) -> vec4<f32>{\n    let shader_sdf_distance_sqr = f32( (traced_harmonics & 1) ) * 2.0 - 1.0;\n    \n    let optimized_move_x = (vec2(traced_harmonics) >> vec2(1u, 16u)) & vec2(0x7FFF, 0xFFFFu);\n    \n\n    let j = vec2<f32>(optimized_move_x) / vec2(32767.0,65535.0);\n    \n    let cursor = uv_octahedral_unit_decode(j);\n    \n    return vec4(cursor, shader_sdf_distance_sqr);\n}\n    \n\nfn decode_vertex_color(traced_harmonics : u32) -> vec3<f32>{\n    return unpack4x8unorm(traced_harmonics).xyz;\n}\n    \n\nfn read_meshlet_attribute_u32(traced_harmonics:u32) -> u32{    \n    return bitcast<u32>( history_raw_type[traced_harmonics] );\n}\n    \n\nfn decode_meshlet_element(traced_harmonics: u32) -> vec2u{\n    return vec2u(\n        (traced_harmonics >> 8),\n        (traced_harmonics & 0xFF)\n    );\n}\n    \n\nfn compute_triangle_face_normal(\n    traced_harmonics: vec3<f32>,\n    shader_sdf_distance_sqr: vec3<f32>,\n    optimized_move_x: vec3<f32>,\n) -> vec3<f32> {\n\n    let j = optimized_move_x - shader_sdf_distance_sqr;\n    let cursor = traced_harmonics - shader_sdf_distance_sqr;\n    \n    let t3 = cross(j, cursor);\n\n    return normalize(t3);\n    \n}\n    \n\nfn max_v3(traced_harmonics: vec3<f32>) -> f32{\n    return max(traced_harmonics.x, max(traced_harmonics.y, traced_harmonics.z));\n}\n    \n\nfn rgbe9995_encode(traced_harmonics:vec3<f32>) -> u32{\n    const shader_sdf_distance_sqr = bitcast<f32>(0x477F8000u);\n    const optimized_move_x = bitcast<f32>(0x37800000u);\n    \n    let j = clamp(traced_harmonics, vec3(0.0), vec3(shader_sdf_distance_sqr));\n    \n    let cursor = max(optimized_move_x, max_v3(j));\n    \n    let t3 = bitcast<f32>((bitcast<u32>(cursor) + 0x07804000u) & 0x7F800000u);\n    \n    let gi_radiance = bitcast<vec3<u32>>(j + t3);\n    let needs_destructor_signature = (bitcast <u32>(t3) << 4) + 0x10000000u;\n    return needs_destructor_signature | (gi_radiance.b << 18) | (gi_radiance.g << 9) | (gi_radiance.r & 0x1FF);\n}\n    \n\nfn rgbe_read_multiple_tap( traced_harmonics: vec2<f32> ) -> f32{\n\n    var shader_sdf_distance_sqr  = fract(traced_harmonics * vec2(5.3987, 5.4421));\n    shader_sdf_distance_sqr += dot(shader_sdf_distance_sqr.yx, shader_sdf_distance_sqr.xy + vec2(21.5351, 14.3137));\n\n    let optimized_move_x = shader_sdf_distance_sqr.x * shader_sdf_distance_sqr.y;\n\n    return fract(optimized_move_x * 95.4307) + fract(optimized_move_x * 75.04961) - 1.0;\n}\n    \n\nfn dither_color_8bit_triangle_noise(traced_harmonics:vec2<f32>) -> f32{\n    let shader_sdf_distance_sqr = rgbe_read_multiple_tap(traced_harmonics);\n    \n    return shader_sdf_distance_sqr / 255.0;\n}\n    \nstruct CascadedSceneShadowmap{\n    bounding_sphere : vec4<f32>,\n    bounding_box : array< f32, 6 >,\n    geometry : u32,\n    material : u32,\n    node : u32,\n}\nfn database_read_CascadedSceneShadowmap_0_2043_element(database: ptr<storage, array<u32>>, index: u32) -> CascadedSceneShadowmap {\n    let page_index = index / 2043u;\n    let page_offset = index % 2043u;\n    let page_lookup_index = page_index + 0u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 65u + page_offset * 16u;\n    return CascadedSceneShadowmap(\n        vec4<f32>(\n            bitcast<f32>(database[base_offset + 0]), \n            bitcast<f32>(database[base_offset + 1]), \n            bitcast<f32>(database[base_offset + 2]), \n            bitcast<f32>(database[base_offset + 3])),\n        array< f32, 6 >(\n            bitcast<f32>(database[base_offset + 4]),\n            bitcast<f32>(database[base_offset + 5]),\n            bitcast<f32>(database[base_offset + 6]),\n            bitcast<f32>(database[base_offset + 7]),\n            bitcast<f32>(database[base_offset + 8]),\n            bitcast<f32>(database[base_offset + 9])\n        ),\n        u32(database[base_offset + 10]),\n        u32(database[base_offset + 11]),\n        u32(database[base_offset + 12])\n    );\n}\nstruct Struct_10{\n    local_rotation : vec4<f32>,\n    global : mat4x4<f32>,\n    prev_global : mat4x4<f32>,\n    local_translation : vec3<f32>,\n    parent : u32,\n    local_scale : vec3<f32>,\n}\nfn database_read_Struct_10_1_744_element(database: ptr<storage, array<u32>>, index: u32) -> Struct_10 {\n    let page_index = index / 744u;\n    let page_offset = index % 744u;\n    let page_lookup_index = page_index + 4096u;\n    let page_address = database[page_lookup_index];\n    let base_offset = page_address + 25u + page_offset * 44u;\n    return Struct_10(\n        vec4<f32>(\n            bitcast<f32>(database[base_offset + 0]), \n            bitcast<f32>(database[base_offset + 1]), \n            bitcast<f32>(database[base_offset + 2]), \n            bitcast<f32>(database[base_offset + 3])),\n        mat4x4<f32>(\n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 4]), \n                bitcast<f32>(database[base_offset + 5]), \n                bitcast<f32>(database[base_offset + 6]), \n                bitcast<f32>(database[base_offset + 7])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 8]), \n                bitcast<f32>(database[base_offset + 9]), \n                bitcast<f32>(database[base_offset + 10]), \n                bitcast<f32>(database[base_offset + 11])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 12]), \n                bitcast<f32>(database[base_offset + 13]), \n                bitcast<f32>(database[base_offset + 14]), \n                bitcast<f32>(database[base_offset + 15])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 16]), \n                bitcast<f32>(database[base_offset + 17]), \n                bitcast<f32>(database[base_offset + 18]), \n                bitcast<f32>(database[base_offset + 19]))),\n        mat4x4<f32>(\n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 20]), \n                bitcast<f32>(database[base_offset + 21]), \n                bitcast<f32>(database[base_offset + 22]), \n                bitcast<f32>(database[base_offset + 23])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 24]), \n                bitcast<f32>(database[base_offset + 25]), \n                bitcast<f32>(database[base_offset + 26]), \n                bitcast<f32>(database[base_offset + 27])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 28]), \n                bitcast<f32>(database[base_offset + 29]), \n                bitcast<f32>(database[base_offset + 30]), \n                bitcast<f32>(database[base_offset + 31])), \n            vec4<f32>(\n                bitcast<f32>(database[base_offset + 32]), \n                bitcast<f32>(database[base_offset + 33]), \n                bitcast<f32>(database[base_offset + 34]), \n                bitcast<f32>(database[base_offset + 35]))),\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 36]), \n            bitcast<f32>(database[base_offset + 37]), \n            bitcast<f32>(database[base_offset + 38])),\n        u32(database[base_offset + 39]),\n        vec3<f32>(\n            bitcast<f32>(database[base_offset + 40]), \n            bitcast<f32>(database[base_offset + 41]), \n            bitcast<f32>(database[base_offset + 42]))\n    );\n}\n\nfn scene_read_mesh(\n    database: ptr<storage, array<u32>>,\n    i: u32,\n) -> CascadedSceneShadowmap {\n    return database_read_CascadedSceneShadowmap_0_2043_element(database, i);\n}\n\nfn scene_read_node(\n    database: ptr<storage, array<u32>>,\n    node_id: u32,\n) -> Struct_10 {\n    return database_read_Struct_10_1_744_element(database, node_id);\n}\n\nstruct EventDispatcher{\n    albedo_color : vec4<f32>,\n    emissive_factor : vec3<f32>,\n    id : u32,\n    ambient_factors : vec2<f32>,\n    metallic_factor : f32,\n    roughness_factor : f32,\n    transmission_factor : f32,\n    ior_factor : f32,\n    transparency_mode : u32,\n    draw_mode : u32,\n    draw_side : u32,\n}\nstruct PipelineCacheKey{\n    projection_matrix : mat4x4<f32>,\n    upscale_ratio : vec2<f32>,\n    jitter : vec2<f32>,\n    width : u32,\n    height : u32,\n    frame_index : u32,\n}\nstruct CommandEncoder{\n    transform : mat4x4<f32>,\n    transform_inverse : mat4x4<f32>,\n    view_matrix : mat4x4<f32>,\n    view_matrix_inverse : mat4x4<f32>,\n    projection_matrix : mat4x4<f32>,\n    projection_matrix_inverse : mat4x4<f32>,\n    view_projection_matrix : mat4x4<f32>,\n    view_projection_matrix_inverse : mat4x4<f32>,\n    frustum : array< vec4<f32>, 6 >,\n    device_depth_to_view_space : vec4<f32>,\n}\nstruct SceneBundle{\n    bounding_sphere : vec4<f32>,\n    bounding_box : array< f32, 6 >,\n    index_count : u32,\n    meshlets_address : u32,\n    meshlets_count : u32,\n}\nstruct Struct_0{\n    bounds_box : array< f32, 6 >,\n    address : u32,\n    primitive_count : u32,\n    vertex_count : u32,\n    flags : u32,\n}\n@group(0) @binding(0) var<uniform> material_info : EventDispatcher;\n@group(0) @binding(1) var normal : texture_2d<f32>;\n@group(0) @binding(2) var lookup : sampler;\n@group(0) @binding(3) var transmitted_energy_factor : texture_2d<f32>;\n@group(0) @binding(4) var screen_st : sampler;\n@group(0) @binding(5) var xyz : texture_2d<f32>;\n@group(0) @binding(6) var elements_per_texel_depth : sampler;\n@group(0) @binding(7) var bb_dim : texture_2d<f32>;\n@group(0) @binding(8) var normals : sampler;\n@group(1) @binding(0) var clamped : texture_2d<u32>;\n@group(1) @binding(1) var total_fraction : texture_2d<u32>;\n@group(1) @binding(2) var<uniform> view : PipelineCacheKey;\n@group(1) @binding(3) var<uniform> camera : CommandEncoder;\n@group(2) @binding(0) var<storage, read> scene_database : array< u32 >;\n@group(2) @binding(1) var<storage, read> geometries : array< SceneBundle >;\n@group(2) @binding(2) var<storage, read> camera_icon : array< Struct_0 >;\n@group(2) @binding(3) var<storage, read> history_raw_type : array< f32 >;\n\nfn textureSampleBarycentric(\n        traced_harmonics:texture_2d<f32>,\n        shader_sdf_distance_sqr:sampler,  \n        optimized_move_x:ShadeMaterialInfo \n) -> vec4<f32> {\n\n     return textureSampleGrad(\n        traced_harmonics,\n        shader_sdf_distance_sqr,\n        optimized_move_x.uv,\n        optimized_move_x.ddx ,\n        optimized_move_x.ddy ,\n     );\n\n}\n\nfn barycentric_full(\n    pt0: vec4<f32>,\n    pt1: vec4<f32>,\n    pt2: vec4<f32>,\n    pixel: vec2<f32>\n) -> BufferAllocator {\n    let invW:vec3<f32> = 1.0 / vec3<f32>(pt0.w, pt1.w, pt2.w);\n\n    let ndc0:vec2<f32> = pt0.xy * invW.x;\n    let ndc1:vec2<f32> = pt1.xy * invW.y;\n    let ndc2:vec2<f32> = pt2.xy * invW.z;\n\n    let det_multiplier:vec3<f32> = invW / (determinant(mat2x2(ndc2 - ndc1, ndc0 - ndc1)));\n\n    let m_ddx = vec3<f32>(ndc1.y - ndc2.y, ndc2.y - ndc0.y, ndc0.y - ndc1.y) * det_multiplier;\n    let m_ddy = vec3<f32>(ndc2.x - ndc1.x, ndc0.x - ndc2.x, ndc1.x - ndc0.x) * det_multiplier;\n\n    let ddxSum:f32 = dot(m_ddx, vec3<f32>(1,1,1));\n    let ddySum:f32 = dot(m_ddy, vec3<f32>(1,1,1));\n\n    let deltaVec:vec2<f32> = pixel - ndc0;\n\n    let interpInvW:f32 = invW.x + deltaVec.x * ddxSum + deltaVec.y * ddySum;\n    let interpW:f32 = 1.0 / interpInvW;\n\n    let m_lambda = vec3<f32>(\n        interpW * (invW.x  + deltaVec.x * m_ddx.x + deltaVec.y * m_ddy.x),\n        interpW * (0.0f    + deltaVec.x * m_ddx.y + deltaVec.y * m_ddy.y),\n        interpW * (0.0f    + deltaVec.x * m_ddx.z + deltaVec.y * m_ddy.z)\n    );\n\n    let interpW_ddx:f32 = 1.0f / (interpInvW + ddxSum);\n    let interpW_ddy:f32 = 1.0f / (interpInvW + ddySum);\n\n    let liw = m_lambda * interpInvW;\n\n    return BufferAllocator(\n        m_lambda,\n        interpW_ddx * (liw + m_ddx) - m_lambda,\n        interpW_ddy * (liw + m_ddy) - m_lambda\n    );\n}\n\nstruct Struct_35{\n    a : GpuAnimationManager,\n    b : GpuAnimationManager,\n    c : GpuAnimationManager,\n}\n\nfn decode_vertex_normal(traced_harmonics: u32) -> vec3<f32>{\n    \n    let shader_sdf_distance_sqr = (vec2(traced_harmonics) >> vec2(0u, 16u)) & vec2(0xFFFFu, 0xFFFFu);\n    \n    let optimized_move_x = vec2<f32>(shader_sdf_distance_sqr) / vec2(65535.0, 65535.0);\n    \n    return uv_octahedral_unit_decode(optimized_move_x);\n}    \n    \n\nfn read_meshlet_vertex(\n    header: Struct_0,  \n    vertex_id: u32,\n) -> GpuAnimationManager{\n    \n    let attribute_offset = meshlet_compute_attribute_section_offset( header );\n    \n    let clamped_vertex_id = min(vertex_id, header.vertex_count - 1u);\n    \n    var output: GpuAnimationManager;\n    \n    var offset = attribute_offset;\n    {\n        output.position = read_meshlet_attribute_vec3f(offset + clamped_vertex_id * 3u);\n    \n        offset += header.vertex_count * 3u;\n    }\n    \n    {\n        let attribute_compressed = (header.flags & 1) != 0u;\n        let local_offset = select(clamped_vertex_id, 0u, attribute_compressed);\n        \n        output.normal = decode_vertex_normal( read_meshlet_attribute_u32(offset + local_offset) );\n        \n        offset += select(header.vertex_count, 1u, attribute_compressed);\n    }\n    \n    {\n        let attribute_compressed = (header.flags & 2) != 0u;\n        let local_offset = select(clamped_vertex_id, 0u, attribute_compressed);\n        \n        output.tangent = decode_vertex_tangent( read_meshlet_attribute_u32(offset + local_offset) );\n        \n        offset += select(header.vertex_count, 1u, attribute_compressed);\n    }\n    \n    {\n        let attribute_compressed = (header.flags & 4) != 0u;\n        let local_offset = select(clamped_vertex_id, 0u, attribute_compressed);\n        \n        output.color = decode_vertex_color( read_meshlet_attribute_u32(offset + local_offset) );\n        \n        offset += select(header.vertex_count, 1u, attribute_compressed);\n    }\n    \n    {\n        let attribute_compressed = (header.flags & 8) != 0u;\n        let local_offset = select(clamped_vertex_id, 0u, attribute_compressed);\n        \n        output.uv = read_meshlet_attribute_vec2f(offset + local_offset * 2u);\n        \n        offset += select(header.vertex_count, 2u, attribute_compressed);\n    }\n    \n    {\n        let attribute_compressed = (header.flags & 16) != 0u;\n        let local_offset = select(clamped_vertex_id, 0u, attribute_compressed);\n        \n        output.uv1 = unpack2x16unorm( read_meshlet_attribute_u32(offset + local_offset) );\n    }\n    \n    return output;\n}\n    \n\nfn read_meshlet_resolved_index(\n    traced_harmonics: Struct_0, \n    shader_sdf_distance_sqr: u32,\n) -> u32{\n    \n    let optimized_move_x = shader_sdf_distance_sqr >> 2u;\n    \n\n    let j = ( shader_sdf_distance_sqr & 0x03u ) << 3;\n    \n    let cursor = read_meshlet_attribute_u32(traced_harmonics.address + optimized_move_x);\n    \n\n    return (cursor >> j) & 0xFF;    \n}\n    \n\nfn read_meshlet_triangle_vertices(\n    meshlet_id: u32,\n    triangle_id: u32\n) -> Struct_35{\n    let header = camera_icon[meshlet_id];\n    \n    let index_offset = triangle_id * 3u;\n    \n    let index_a = read_meshlet_resolved_index(header, index_offset);\n    let index_b = read_meshlet_resolved_index(header, index_offset + 1);\n    let index_c = read_meshlet_resolved_index(header, index_offset + 2);\n    \n    let a = read_meshlet_vertex( header, index_a );\n    let b = read_meshlet_vertex( header, index_b );\n    let c = read_meshlet_vertex( header, index_c );\n    \n    return Struct_35(a, b, c);\n}\n    \n\nstruct ShaderModuleCompiler {\n  @location(0) pbr : vec2<f32>,\n  @location(1) normal : vec4<u32>,\n  @location(2) albedo : vec4<f32>,\n  @location(3) emissive : u32,\n}\n\n\nconst MIP_BIAS = 0.75187970;\n\n@fragment\nfn main(\n  @builtin(position) traced_harmonics : vec4<f32>,\n) -> ShaderModuleCompiler {\n    var shader_sdf_distance_sqr : ShaderModuleCompiler;\n\n    let optimized_move_x = textureLoad(\n        clamped,\n        vec2<i32>(traced_harmonics.xy),\n        0\n    ).r;\n    \n    let j = decode_meshlet_element(optimized_move_x);\n    let cursor = j[0];\n    let t3 = j[1];\n\n    let gi_radiance = textureLoad(\n        total_fraction,\n        vec2<i32>(traced_harmonics.xy),\n        0\n    ).r;\n\n    let needs_destructor_signature = scene_read_mesh(&scene_database, gi_radiance);\n    let raw_destructor_signature = scene_read_node(&scene_database, needs_destructor_signature.node);\n\n\n    let seed_budget_ms:SceneBundle = geometries[needs_destructor_signature.geometry];\n\n\n    let texture = read_meshlet_triangle_vertices(cursor, t3);\n\n    let format = texture.a;\n    let dst = texture.b;\n    let message = texture.c;\n\n    let color_texture:vec3<f32> = format.position;\n    let redundant:vec3<f32> = dst.position;\n    let bucket_index_count:vec3<f32> = message.position;\n    \n    let local_total_indirect_diffuse = compute_triangle_face_normal(\n        color_texture,\n        redundant,\n        bucket_index_count,\n    );\n    \n    let num_occluded = raw_destructor_signature.global;\n\n    let u8array:vec4<f32> = num_occluded * vec4<f32>(color_texture, 1.0);\n    let meshlet_buckets:vec4<f32> = num_occluded * vec4<f32>(redundant, 1.0);\n    let result:vec4<f32> = num_occluded * vec4<f32>(bucket_index_count, 1.0);\n\n    let allocator_textures = view.projection_matrix;\n\n    let meshlet = allocator_textures * u8array;\n    let filter_mitchell = allocator_textures * meshlet_buckets;\n    let rel_name = allocator_textures * result;\n\n\n    let chunk_scene_bounding_box = barycentric_full(\n        meshlet,\n        filter_mitchell,\n        rel_name,\n        traced_harmonics.xy   \n    );\n    \n    let ve = interpolate_attribute_3f32(\n        u8array.xyz / u8array.w,\n        meshlet_buckets.xyz / meshlet_buckets.w,\n        result.xyz / result.w,\n        chunk_scene_bounding_box.lambda\n    );\n\n    let raw_type:vec2<f32> = format.uv;\n    let fields:vec2<f32> = dst.uv;\n    let probe_volume_scattering:vec2<f32> = message.uv;\n\n    var distinct = barycentric_interpolate_uv(chunk_scene_bounding_box, raw_type, fields, probe_volume_scattering);\n    \n\n    let m_base = MIP_BIAS / view.upscale_ratio;\n    \n\n    distinct.ddx *= m_base.x;\n    distinct.ddy *= m_base.y;\n    \n    let instance_vertex_position = textureSampleBarycentric(xyz, elements_per_texel_depth, distinct);\n\n\n    var chunk_ss_trace_mip = instance_vertex_position.g * material_info.roughness_factor;\n    let prim_children = instance_vertex_position.b * material_info.metallic_factor;\n    \n    let no_l = fma(instance_vertex_position.r, material_info.ambient_factors.x, material_info.ambient_factors.y);\n\n    let perturbed_dir = format.normal;\n    let flat = dst.normal;\n    let mipmap_shader = message.normal;\n\n    let camera_previous_sum = interpolate_attribute_3f32(\n        perturbed_dir,\n        flat,\n        mipmap_shader,\n        chunk_scene_bounding_box.lambda\n    );\n\n    let moment_scale_y = interpolate_attribute_3f32(\n        format.tangent.xyz,\n        dst.tangent.xyz,\n        message.tangent.xyz,\n        chunk_scene_bounding_box.lambda\n    );\n    \n\n    let ffx_brixelizer_scratch_a = format.tangent.w;\n    \n    let vector = interpolate_attribute_3f32(\n        format.color,\n        dst.color,\n        message.color,\n        chunk_scene_bounding_box.lambda\n    );\n\n    let ai_store = mat4_extract_position( camera.transform );\n    let scene = normalize( ai_store - ve );\n    \n    var v4 = compute_normal_matrix_from_m4(num_occluded);\n\n    var new_index = normalize( v4 * camera_previous_sum );\n    \n\n    var dup = normalize( v4 * moment_scale_y );\n    \n\n    var lib = normalize( v4 * local_total_indirect_diffuse );\n    \n    let target_q_high = dot(lib, scene) < 0.0;\n    \n    if( target_q_high ) {\n\n        new_index = -new_index;\n        dup = -dup;\n        lib = -lib;\n    }\n\n    let chunk_convert_to_write = build_orthonormal_matrix_nt(new_index, vec4(dup, ffx_brixelizer_scratch_a));\n\n    let remapped_dispatch_thread_id = textureSampleBarycentric(transmitted_energy_factor, screen_st, distinct).rgb * 2.0 - 1.0;\n\n\n    let mie:vec3<f32> = normalize( chunk_convert_to_write * remapped_dispatch_thread_id );\n     \n   \n\n    let code = anti_alias_roughness_kaplanyan(chunk_ss_trace_mip, mie, chunk_convert_to_write[0], chunk_convert_to_write[1]);\n\n\n    let file_map = code + dither_color_8bit_triangle_noise(traced_harmonics.xy);\n    \n    let pvec = textureSampleBarycentric(normal, lookup, distinct);\n\n\n    let buckets = vec3(pvec.rgb / pvec.a);\n\n    shader_sdf_distance_sqr.albedo = vec4<f32>( \n        buckets * vector * material_info.albedo_color.rgb,\n        no_l \n    );\n    \n    shader_sdf_distance_sqr.normal = vec4u(\n        encode_g_buffer_normal(mie),\n        encode_g_buffer_normal(lib), \n    );\n    \n    \n    shader_sdf_distance_sqr.pbr = vec2(prim_children, file_map);\n\n    let children = textureSampleBarycentric(bb_dim, normals, distinct).rgb * material_info.emissive_factor;   \n\n    shader_sdf_distance_sqr.emissive = rgbe9995_encode(children);\n\n    return shader_sdf_distance_sqr;\n}";

