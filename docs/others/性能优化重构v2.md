对，这 4 点都成立，而且会直接改变我前面那版“性能重构”的思路。

前一版还是过于把问题看成：

> Geometry 慢 → 改 Geometry
> Material 慢 → 改 Material
> Surface 带宽大 → 压 Surface

这种“按 Pass 优化”的思维。

你现在真正应该做的其实是：

> **把 EEngine 从“运行时读取通用资产并直接渲染”的引擎，重构成“Asset Cook + Streaming + Compact GPU Runtime + GPU-driven Rendering”的完整系统。**

性能链路应该从文件开始看：

```text
磁盘 / CDN
   ↓
传输数据量
   ↓
解压 / 解码
   ↓
CPU RAM
   ↓
数据转换 / 重排
   ↓
GPU Upload
   ↓
VRAM Resident Set
   ↓
GPU Cache / Memory Bandwidth
   ↓
Culling / Raster
   ↓
Texture Sampling
   ↓
Shading
   ↓
Surface Bandwidth
   ↓
Effects
   ↓
Present
```

**前面任何一层设计得差，后面的 GPU Renderer 都会跟着付代价。**

而且你第 4 点也对：我前面说“大架构没有 10% 总收益就不要做”太机械了。以后不能拿一个固定 FPS 百分比当所有优化的门槛。

------

# 一、先看一个非常关键的问题：EEngine 现在资源侧确实还有很大空间

我重新看了你现在的资源代码。

## 1. 贴图现在最终基本还是 `RGBA8`

你现在的 `TextureResidency`：

```ts
format: "rgba8unorm"
```

而且所有 Texture Bank：

```text
256
512
1024
2048
4096
```

最终都是 `rgba8unorm` texture array。

具体 Bank 配置：

```ts
GPU_TEXTURE_BANK_SIZES =
[256, 512, 1024, 2048, 4096]
```

这意味着：

即使用户下载的是：

```text
WebP
AVIF
JPEG
PNG
```

文件很小，

上传 GPU 后还是：

```text
RGBA8
4 bytes / pixel
```

所以：

> **文件压缩 ≠ 显存压缩。**

这是两个完全不同的问题。

------

# 二、一个 4K 贴图到底有多夸张

4096 × 4096：

```text
16,777,216 pixels
```

RGBA8：

```text
16,777,216 × 4
≈ 64 MiB
```

加完整 mip chain：

```text
≈ 85.3 MiB
```

也就是说：

> 一张 4K RGBA8 texture 就可能接近 **85 MiB VRAM**。

10 张：

```text
≈ 853 MiB
```

20 张：

```text
≈ 1.67 GiB
```

而你现在 `TextureResidency` 的逻辑预算甚至设成了：

```ts
2 * 1024 * 1024 * 1024
```

也就是：

```text
2 GiB
```

------

# 三、如果换 GPU 原生 Block Compression 呢？

以桌面 GPU 为例。

RGBA8：

```text
4 bytes / pixel
```

BC7：

```text
1 byte / pixel
```

那么同一张 4K：

```text
RGBA8
≈ 85.3 MiB

BC7
≈ 21.3 MiB
```

直接约：

# 4 倍显存差距

如果某些贴图使用 BC1：

```text
≈ 0.5 byte / pixel
```

则可以到：

```text
≈ 10.7 MiB
```

约：

# 8 倍

而且 GPU 压缩贴图不只是：

```text
VRAM ↓
```

同时还会：

```text
Texture Cache 压力 ↓

显存带宽 ↓

读取数据量 ↓

更高概率 cache hit

更少 memory pressure
```

MDN 当前 WebGPU 已经明确支持 BC、ETC2、ASTC GPU 压缩格式 feature；包括 BC1～BC7、BC4/5、BC6H 等。([MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/API/GPUSupportedFeatures?utm_source=chatgpt.com))

所以我现在会把：

> **GPU 原生 texture compression**

提高到 EEngine 核心性能架构级别，而不是 loader 小优化。

------

# 四、你现在的 glTF Texture Loader 其实也说明了这个问题

现在主要处理：

```text
普通 source
+
EXT_texture_webp
```

但缺少非常重要的：

```text
KHR_texture_basisu
KTX2
```

而 KTX2 正好是为这种情况设计的。

它允许：

```text
                  KTX2
                    │
           Basis Universal
                    │
        ┌───────────┼────────────┐
        ▼           ▼            ▼
       BC7        ASTC          ETC2
     Desktop      Mobile        Mobile
```

KTX2 本身支持 mip、streaming、Basis Universal supercompression；glTF 的 `KHR_texture_basisu` 就是为了让 glTF 使用这套机制。([Khronos Registry](https://registry.khronos.org/KTX/specs/2.0/ktxspec.v2.html?utm_source=chatgpt.com))

所以这里我会直接改设计。

------

# 五、以后不应该是“glTF → Runtime”

而应该变成：

# EEngine Asset Pipeline

```text
                 Authoring Assets

         GLTF / GLB / FBX / USD / OBJ
         PNG / JPG / WebP / EXR / HDR
                    │
                    ▼
             OEngine Asset Cooker
                    │
     ┌──────────────┼───────────────┐
     │              │               │
     ▼              ▼               ▼
 Geometry        Texture         Animation
 Processor       Processor       Processor
     │              │               │
     ▼              ▼               ▼
 Quantize        Compress         Quantize
 Reorder         Mipmap           Compress
 Meshlet         Transcode        Resample
 LOD             Streaming        GPU Layout
 Hierarchy       Metadata
     │              │               │
     └──────────────┼───────────────┘
                    ▼
             OEngine Runtime Asset

               .oasset / .opack
                    │
           ┌────────┴────────┐
           ▼                 ▼
        Metadata          Data Pages
                        64K / 128K...
                    │
                    ▼
              Runtime Streaming
                    │
                    ▼
                GPU Runtime
```

也就是说：

## glTF 以后是 Import Format

而不是：

## GPU Runtime Format

这两者必须分开。

------

# 六、这个改变我认为非常重要

类似：

```text
Blender
 ↓
FBX
 ↓
Unity Runtime
```

Unity 并不是每一帧按照 FBX 原始文件布局去渲染。

同理 EEngine 不应该：

```text
GLTF accessor
 ↓
GPU shader
```

直接建立永久对应关系。

应该：

```text
GLTF accessor
          ↓
      Offline Cook
          ↓
EEngine GPU-native format
```

------

# 七、而你现在 Geometry 其实存在一个很有意思的问题

当前 glTF loader 已经做了一部分：

```text
glTF attribute
 ↓
Engine Attribute
```

但其中：

```ts
Position:
Float32 × 3

Color:
Float32 × 3
```

也就是说一个原始 glTF Color 可能本来是：

```text
RGBA8
4 bytes / vertex
```

结果你可能转换成：

```text
Float32 × 3
12 bytes / vertex
```

直接：

# 3 倍

而 Position：

```text
float32 × 3
= 12 B
```

对于很多静态 mesh 实际未必需要这么高精度长期存储。

更关键的是另一方面：

其他 attribute 又可能保留：

```text
Uint8
Uint16
Float32
normalized
non-normalized
arbitrary stride
```

于是 Runtime shader 又需要：

```text
判断数据类型
判断 normalized
计算 stride
计算 byte offset
decode
```

所以当前实际上有点：

# 两边成本都吃了

```text
部分数据
→ Float32 膨胀

另一部分数据
→ Runtime 通用 decode
```

------

# 八、我要把这个彻底改成 Canonical GPU Vertex Format

例如静态模型：

```text
OEngineVertexV2

Position
----------------
局部 AABB quantization
3 × u16
= 6 B

Normal
----------------
octahedral
2 × i16
= 4 B

Tangent
----------------
octahedral + sign
≈ 4 B

UV0
----------------
f16 × 2
= 4 B

UV1
----------------
f16 × 2
= 4 B

Color
----------------
rgba8
= 4 B
```

不是每个 mesh 都必须全部字段。

Cooker 根据实际使用：

```text
有没有 normal map？
有没有 color？
有没有 UV1？
有没有 skin？
```

生成：

```text
Vertex Layout Class
```

------

# 九、例如一个常见 Static Mesh Vertex

传统：

```text
Position     float3    12
Normal       float3    12
Tangent      float4    16
UV           float2     8
Color        float4    16
-------------------------
64 bytes / vertex
```

我们可以做到大约：

```text
Position        6~8
Normal          4
Tangent         4
UV              4
Color           4
-----------------
≈ 20~24 bytes
```

也就是说：

# Vertex Resident Data 可以减少到约 1/2～1/3

而且不只是显存。

GPU 每次：

```text
fetch vertex
```

搬的数据也少了。

------

# 十、这时候 2026 WebGPU 新能力就应该真正用起来了

你第 1 点说：

> 可以依赖较新的 2026 能力。

那我会改变之前的“最低公分母”设计。

现在 2026 WebGPU feature 集已经包含：

```text
shader-f16

subgroups

subgroup-size-control

primitive-index

texture-formats-tier1

texture-formats-tier2

texture-component-swizzle

texture-compression-bc

texture-compression-astc

texture-compression-etc2

timestamp-query
```

当前 WebGPU/WGSL Editor's Draft 都已经列出这些能力。([GPU Web](https://gpuweb.github.io/types/types/GPUFeatureName.html?utm_source=chatgpt.com))

所以 EEngine 可以明确提出：

# `OEngine WebGPU 2026 High Profile`

------

# 十一、我甚至建议不要再把“老 WebGPU”当第一目标

可以分：

```text
OEngine High 2026
```

作为主要开发目标：

```text
required:
subgroups
shader-f16
primitive-index
texture-formats-tier1

desktop:
texture-compression-bc
```

然后才是：

```text
Portable fallback
```

如果你的目标本身就是：

> 高级 WebGPU GPU-driven Engine

那不应该为了照顾很老的 implementation，把核心 GPU Pipeline 永远限制住。

------

# 十二、`primitive-index` 对你尤其有意义

2026 WGSL 已经有：

```wgsl
@builtin(primitive_index)
```

Fragment shader 可以直接知道当前 primitive index。([GPU Web](https://gpuweb.github.io/gpuweb/wgsl/?utm_source=chatgpt.com))

这可能直接影响我们刚才讨论的：

```text
VisibilityKey V2
```

以前我要想办法：

```text
vertex_index
 ↓
triangle ID
```

以后可以更直接地做：

```text
instance_index
     +
primitive_index
     ↓
Meshlet Work Slot
+
Local Primitive
```

这对于你的 Visibility Buffer 非常适合。

------

# 十三、Subgroup 更重要

你现在很多 GPU queue 操作还是类似：

```text
thread
 ↓
atomic reserve
 ↓
append
```

以后可以：

```text
32 threads / subgroup

cull
 ↓
subgroupBallot
 ↓
subgroup count
 ↓
只做一次 global atomic
 ↓
每个 lane 计算自己的 offset
```

也就是：

```text
以前：

thread 0 → atomic
thread 1 → atomic
thread 2 → atomic
...
thread 31 → atomic


以后：

32 lanes
    ↓
subgroup compact
    ↓
1 global atomic
```

这可能同时改善：

```text
Hierarchy queue
Meshlet queue
Shade bin
SSR ray queue
Transparency queue
```

所以这个不是：

> “优化某个 shader”

而是：

# Queue Infrastructure V2

------

# 十四、`shader-f16` 也应该进入数据架构

不只是 shader ALU。

可以用于一部分：

```text
UV
normal intermediate
roughness
metallic
animation weights
部分 bounding 数据
material params
```

当然：

```text
world position
large world translation
depth-critical values
```

还是 f32。

目标是：

> **每种数据按照实际精度需要选择类型。**

而不是：

```text
全部 Float32
```

------

# 十五、模型文件压缩这一块，我也会正式做

这个不能只说：

```text
GLB gzip
```

我推荐主要走：

# Meshoptimizer

因为它和你的 GPU-driven 数据结构很匹配。

`EXT_meshopt_compression` 本身就是针对：

```text
vertex
index
morph
animation
instance transform
```

等 glTF binary data 的，而且它的设计目标之一就是高速解压、适合 GPU-ready layout。官方扩展文档还特别指出 WebAssembly SIMD decoder 在现代桌面可达到约 GB/s 级的解码吞吐。([GitHub](https://github.com/KhronosGroup/glTF/blob/main/extensions/2.0/Vendor/EXT_meshopt_compression/README.md?utm_source=chatgpt.com))

现在 meshoptimizer 甚至已经有新的：

```text
KHR_meshopt_compression
```

候选扩展以及更新的 codec v1；meshoptimizer v1.0 也明确说明新 vertex codec 改善了压缩率和解压性能。([GitHub](https://github.com/KhronosGroup/glTF/blob/main/extensions/2.0/Khronos/KHR_meshopt_compression/README.md?utm_source=chatgpt.com))

------

# 十六、但是我不会让 Runtime 依赖 glTF meshopt 的格式

Production pipeline 可以：

```text
GLTF
 ↓
meshoptimizer
 ↓
OEngine Cooker
 ↓
OAsset
```

然后 `.oasset` 里面直接使用我们自己的：

```text
Meshopt v1 compressed pages
```

这样就不用受：

```text
glTF extension version
```

限制。

------

# 十七、Draco 呢？

可以支持 Import。

但我不会把它作为 OEngine 主 Runtime Codec。

定位：

```text
KHR_draco_mesh_compression
       ↓
Import Compatibility

Meshopt
       ↓
Runtime Optimized Asset
```

原因是我们的目标不是：

> 单纯文件最小

而是：

```text
压缩率
+
解码速度
+
GPU layout
+
vertex locality
+
meshlet locality
+
runtime upload
```

整体最优。

------

# 十八、Cook 阶段应该做的不只是“压缩”

这个很关键。

我建议 Cooker 至少有以下过程：

```text
Raw Mesh
   ↓
Remove unused attributes
   ↓
Deduplicate vertices
   ↓
Deduplicate geometry
   ↓
Detect instancing
   ↓
Quantization
   ↓
Index optimization
   ↓
Vertex fetch optimization
   ↓
Meshlet generation
   ↓
Meshlet local reorder
   ↓
Cluster generation
   ↓
LOD simplification
   ↓
Cluster hierarchy
   ↓
Page packing
   ↓
Compression
```

meshoptimizer 本身也明确建议：

```text
quantization
+
vertex/index locality optimization
```

之后再压缩，因为这样压缩率和 GPU 访问局部性都会更好。([网格优化库](https://meshoptimizer.org/?utm_source=chatgpt.com))

------

# 十九、这里出现一个非常重要的“组合收益”

例如：

## 只做 Quantization

```text
Vertex 64 B
→
24 B
```

GPU fetch 降低。

------

再做：

## Vertex Reorder

```text
cache locality ↑
```

------

再做：

## Meshlet Locality

```text
连续 vertex
连续 triangle
```

------

再做：

## Meshopt Compression

下载：

```text
更小
```

------

再做：

## Meshlet Raster Work

运行时 queue：

```text
更小
```

------

于是你不能简单说：

```text
Quantization +8%
Meshlet +15%
Meshopt +0% FPS
```

然后：

```text
总收益 = 23%
```

根本不是这样的。

因为它们互相影响。

------

# 二十、真正应该这么理解

```text
Asset Quantization
       ↓
Resident Set ↓
       ↓
Cache locality ↑
       ↓
Vertex Fetch bandwidth ↓
       ↓
Meshlet raster 更便宜
       ↓
Material Resolve random fetch 更便宜
       ↓
GPU bandwidth pressure ↓
       ↓
其它 pass 更容易获得 bandwidth
```

这是：

# Synergy

组合效应。

------

# 二十一、而且还有 Bottleneck Shift

例如原来：

```text
Geometry 4 ms
Material 2 ms
Lighting 2 ms
```

Geometry 重构后：

```text
Geometry 1.5 ms
Material 2 ms
Lighting 2 ms
```

现在最大瓶颈变成：

```text
Material
```

于是你之前对 Material 做一个：

```text
0.3 ms
```

的小优化，

原来可能根本看不出来。

现在就很重要。

这就是为什么：

> **不能拿“这个小优化有没有让总 FPS +10%”作为判断依据。**

------

# 二十二、所以我修改之前的优化准则

以后分成 4 类。

## A. Architecture Enabler

比如：

```text
Canonical Vertex Format
VisibilityKey V2
Asset Page ABI
GPU Texture Compression
Shade Bin
```

这种本身就是未来架构前提。

不要求：

```text
单独 +10 FPS
```

------

## B. Hot-path Optimization

例如：

```text
subgroup queue compaction
vertex fetch reduction
material resolve optimization
```

主要看：

```text
对应 Stage 工作量
GPU time
bandwidth
queue size
```

不一定要求整个 Frame 有巨大变化。

------

## C. Residency / Memory Optimization

例如：

```text
BC7
geometry quantization
mip streaming
page cache
```

主要看：

```text
VRAM resident bytes

peak VRAM

RAM peak

upload bytes

streaming hitch

cache behavior
```

它即使当前场景：

```text
FPS +1
```

也完全可能值得做。

因为大型场景时会避免：

```text
VRAM 爆掉
↓
驱动换页
↓
突然卡几十 ms
```

------

## D. Micro Optimization

例如：

```text
shader 少两条 ALU

TS 少一个 Map lookup
```

这种才应该严格要求：

> 有测量收益才保留。

------

# 二十三、贴图这边我会直接设计成 Texture Pipeline V2

完整流程：

```text
Source Texture
PNG/JPG/WebP/EXR
        ↓
     Cooker
        ↓
Semantic Analysis
        │
        ├── BaseColor
        ├── Normal
        ├── ORM
        ├── Emissive
        └── HDR
        ↓
Generate Mips Offline
        ↓
Compression
```

Desktop：

```text
BaseColor
→ BC7 sRGB

Normal
→ BC5 / BC7

ORM
→ BC7 / BC4 family where appropriate

Emissive
→ BC7

HDR Environment
→ BC6H
```

Mobile：

```text
ASTC
```

Fallback：

```text
ETC2
```

WebGPU 当前已经具备对应格式 feature。([MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/API/GPUSupportedFeatures?utm_source=chatgpt.com))

------

# 二十四、然后 KTX2 主要解决“传输”

我会支持两套发布模式。

## Universal Web Asset

```text
KTX2
+
Basis Universal
```

客户端下载后：

```text
检查 adapter

NVIDIA / AMD / Intel
→ BC

Apple / ARM
→ ASTC

其它
→ ETC2
```

------

## Performance Asset Variant

如果：

```text
adapter 已经知道
```

应用甚至可以直接请求：

```text
texture.bc7.ktx2
```

而不是：

```text
Basis
 ↓
WASM transcode
 ↓
BC7
```

也就是说：

```text
首次进入页面：
requestAdapter()

 ↓

AssetProfile
```

然后：

```text
GET dungeon.desktop-bc.opack
```

或者：

```text
GET dungeon.mobile-astc.opack
```

这是更激进但合理的 Web 引擎方案。

------

# 二十五、Mip 也绝对不应该 Runtime 才生成

你现在 `TextureResidency.stage()` 里会：

```text
resize copy
 ↓
generateMipmap
```

Production asset 最好：

```text
Cooker
 ↓
提前生成 Mips
```

Runtime：

```text
copy compressed blocks
```

完事。

不应该每次加载资产：

```text
GPU resize
+
GPU mip generation
```

------

# 二十六、TextureResidency 本身也值得重构

你当前有：

```text
bank grow
```

而 grow 的思路本质上是：

```text
old bank
 ↓
new bigger bank
 ↓
copy old contents
```

代码里还专门维护了：

```text
bankGrowCount
bankCopyOperationCount
transactionPeakBytes
```

这已经说明这是一个实际资源生命周期成本。

------

# 二十七、我不想以后 Bank Grow 需要搬整栋楼

应该变：

```text
Texture Pool
│
├─ Page 0
├─ Page 1
├─ Page 2
├─ Page 3
└─ ...
```

不够：

```text
直接加 Page 4
```

而不是：

```text
创建一个更大的 TextureArray
 ↓
把 0~N 全 copy 过去
```

------

# 二十八、也就是 `TextureResidency V2`

例如：

```text
TextureRef V2

formatClass
sizeClass
page
layer
```

概念上：

```text
[ format | size | page | layer ]
```

而不是只有：

```text
bank + layer
```

于是：

```text
BC7 1024 Page0
BC7 1024 Page1

BC7 2048 Page0

ASTC ...
```

增长不再迁移旧数据。

------

# 二十九、但是这里有 WebGPU binding 限制

所以不能无限 Page。

这就需要设计：

```text
Resident Window
```

例如每一类保留固定 GPU-visible pages。

或者进一步走：

# Virtual Texture

```text
Material
 ↓
Virtual Texture ID
 ↓
Page Table
 ↓
Physical Texture Atlas
```

------

# 三十、Virtual Texture 我不会现在第一天就实现

但我会让 ABI **能够发展到这个方向**。

短期：

```text
compressed size-class arrays
+
LRU
+
resolution streaming
```

中期：

```text
mip residency
```

长期：

```text
physical tile cache
+
virtual texture
```

------

# 三十一、模型也应该做 Streaming，而不只是 Texture

这个更重要。

未来大型 scene：

```text
整个 geometry
```

也不应该：

```text
进入场景
↓
全部上传 GPU
```

而应该：

```text
             Geometry Package

      Root / Coarse LOD always resident
                   │
                   ▼
             Hierarchy traversal
                   │
       ┌───────────┴────────────┐
       │                        │
    resident                 missing
       │                        │
       ▼                        ▼
   render child          render parent LOD
                              +
                        streaming request
```

这就非常接近真正 Virtualized Geometry。

------

# 三十二、所以 Cluster Hierarchy 以后还承担 Streaming

现在：

```text
Cluster hierarchy
```

主要是：

```text
LOD + Cull
```

以后：

```text
Cluster hierarchy
```

同时可以承担：

```text
LOD
Cull
Streaming
Residency fallback
```

例如：

```text
Node
├─ bounds
├─ error
├─ child range
├─ page ID
└─ resident bit
```

GPU：

```text
想进入 child
 ↓
page resident?
```

是：

```text
继续
```

否：

```text
使用 parent
+
写 request bit
```

------

# 三十三、然后 Runtime 给 CPU 一个很小的 Streaming Feedback

比如：

```text
Page 14 required
Page 26 required
Page 881 required
```

CPU 不需要知道：

```text
哪些 triangle visible
```

只需要：

```text
哪些 Asset Page 要加载
```

这个 readback 是合理的。

因为它不是：

```text
GPU cull
 ↓
CPU rebuild draw list
```

所以不破坏 GPU-driven 原则。

------

# 三十四、`.opack` 可以按 Page 存

例如：

```text
Dungeon.opack

Header
Manifest

Geometry Page Table

Page 0000
Page 0001
Page 0002
...

Texture Table

Texture/Mip chunks

Animation chunks
```

然后 Web：

```text
HTTP Range Request
```

只拿：

```text
需要的 byte range
```

而不是：

```text
下载 500 MB
↓
再找里面 2 MB
```

------

# 三十五、Page 大小可以 benchmark

例如：

```text
64 KiB
128 KiB
256 KiB
```

这里我不会现在拍脑袋规定。

因为：

```text
page 太小
→ requests / metadata 多

page 太大
→ overfetch 多
```

这应该作为 Asset Streaming Benchmark 的一个变量。

------

# 三十六、Runtime 加载链路也要改

现在 production runtime 不应该：

```text
fetch
 ↓
parse glTF JSON
 ↓
parse accessor
 ↓
convert attribute
 ↓
meshlet build
 ↓
resize image
 ↓
generate mip
 ↓
upload
```

而应该：

```text
fetch OAsset page
 ↓
Worker decode
 ↓
GPU-ready bytes
 ↓
upload
```

Cook 的作用就是：

# 把所有能提前算的都提前算

------

# 三十七、Web Worker 应该成为 Asset Runtime 的一部分

```text
Main Thread
  │
  ├─ Renderer
  └─ WebGPU command encoding


Worker Pool
  │
  ├─ Meshopt decode
  ├─ KTX2 Basis transcode
  ├─ asset decompression
  ├─ scene metadata parse
  └─ streaming scheduler
```

主线程尽量不要：

```text
解析 200MB Mesh
```

不然即使平均 FPS 很好：

```text
P99
```

还是会炸。

------

# 三十八、RAM 也必须正式进入性能预算

以前我们主要讲：

```text
VRAM
```

但 Web 上很容易变成：

```text
Compressed file       100 MB

Decoded ArrayBuffer   300 MB

Geometry objects      300 MB

Packed copy           200 MB

GPU upload staging    200 MB

GPU Buffer            200 MB
```

一瞬间：

```text
1 GB+
```

这就是：

# Peak Memory

问题。

------

# 三十九、所以每种资源应该有 Ownership 生命周期

例如 geometry：

```text
Compressed Page
       ↓
Decode Buffer
       ↓
GPU Upload
       ↓
GPU Resident

上传完成后：

Compressed Page
必要时保留 cache

Decode Buffer
释放

CPU Geometry
如果 runtime 不需要
释放
```

不应该：

```text
SourceGeometry
+
Geometry
+
PackedGeometry
+
GpuAsset shadow
+
GPU Buffer
```

全部长期存在。

------

# 四十、这也是我为什么现在会主张一个非常明确的架构

```text
                    Asset World

Authoring
──────────────────────────────────

GLTF
FBX
USD
PNG
EXR
       ↓

OEngine Asset Cooker
──────────────────────────────────

Canonical IR

 ↓

Geometry Cook
Texture Cook
Animation Cook

 ↓

OAsset / OPack


Runtime
──────────────────────────────────

AssetStreamer
     ↓
AssetPageCache
     ↓
GpuUploadQueue
     ↓

GpuRuntime
──────────────────────────────────

GeometryPagePool
TextureResidency
MaterialStore
AnimationStore
InstanceStore

     ↓

GPU-driven Renderer
```

------

# 四十一、然后再接我们上一条谈的 Renderer V2

完整变成：

```text
                    OENGINE V2

                OFFLINE / BUILD TIME
────────────────────────────────────────

 Source Assets
 GLTF / USD / FBX / textures
              ↓
        Canonical Import IR
              ↓
      ┌───────┼──────────┐
      ▼       ▼          ▼
 Geometry   Texture   Animation
   Cook       Cook       Cook
      │       │          │
 Quantize   KTX2       Compress
 Meshopt    BC/ASTC    Quantize
 Meshlet    Mips
 LOD
 Hierarchy
      │       │          │
      └───────┼──────────┘
              ▼
         .oasset/.opack


                    RUNTIME
────────────────────────────────────────

        Streaming Scheduler
              ↓
      Asset Residency Manager
        │            │
        ▼            ▼
 Geometry Pages   Texture Pages
        │            │
        └─────┬──────┘
              ▼
          GPU Runtime
              ↓
       Instance / Scene Cull
              ↓
      Adaptive Work Selector
        │             │
      Flat         Hierarchy
        │             │
        └─────┬───────┘
              ▼
       Meshlet Work Queue
              ↓
        Visibility Raster
       primitive_index
              ↓
        VisibilityKey V2
              ↓
        Compute Shade Bin
        + subgroups
              ↓
      Material Compute Shading
              ↓
       Lighting Fusion
              ↓
         Surface Lite
              ↓
       SSR / AO / Shadow
              ↓
         Transparency
              ↓
          Temporal
              ↓
        Fused Post
              ↓
           Present
```

这才是我现在认为应该朝着走的方向。

------

# 四十二、性能要重新划成 7 个“大系统”

不是 Pass。

| 系统                  | 主要问题                               |
| --------------------- | -------------------------------------- |
| **Asset Encoding**    | 文件大小、quantization、compression    |
| **Loading**           | 网络、decode、主线程阻塞               |
| **CPU Memory**        | 重复对象、peak RAM                     |
| **GPU Residency**     | VRAM、page、texture compression        |
| **Geometry Pipeline** | culling/work amplification/raster      |
| **Shading Pipeline**  | material/texture/bandwidth             |
| **Frame Pipeline**    | effects/render targets/submit/temporal |

这样才能真正“整体优化”。

------

# 四十三、我现在给整个体系重新排优先级

这不是简单的：

```text
哪个 +20%
哪个 +10%
```

而是看它能影响多少系统。

## S 级：架构基础

### 1. `Asset Cooker + Runtime OAsset`

影响：

```text
下载
RAM
VRAM
GPU bandwidth
startup
streaming
shader layout
```

**系统杠杆极高。**

------

### 2. Canonical Compact GPU Geometry

影响：

```text
file
RAM
VRAM
vertex fetch
material resolve
cache
meshlet
```

**系统杠杆极高。**

------

### 3. GPU Compressed Texture Pipeline

影响：

```text
download
VRAM
texture bandwidth
cache
load time
streaming
```

**系统杠杆极高。**

------

### 4. Page-based Residency / Streaming

影响：

```text
large scene scalability
VRAM peak
RAM peak
loading
P95/P99
LOD
```

**系统杠杆极高。**

------

## A 级：核心 GPU Pipeline

### 5. Meshlet Work 替换 Triangle Work

解决：

```text
Geometry Work Amplification
```

------

### 6. Subgroup GPU Queue Infrastructure

同时影响：

```text
Visibility
LOD
Material binning
SSR
Transparency
```

------

### 7. Compute Material Binning

解决：

```text
Pixel Work Amplification
```

------

### 8. Surface / Lighting Fusion

解决：

```text
Framebuffer bandwidth
```

------

## B 级：稳定性和可扩展性

### 9. Geometry Budget

### 10. Texture Streaming

### 11. Shadow Budget

### 12. Dynamic Resolution

### 13. Software Shading Rate

------

## C 级：最后才做微优化

例如：

```text
WGSL ALU 几条指令
TS function
小 Map lookup
```

------

# 四十四、注意 Asset Cooker 我现在甚至会放在 Meshlet V2 前面

因为它会决定：

```text
Meshlet 里面到底是什么格式

Vertex 到底怎么排列

Cluster 怎么 Page 化

TextureRef 怎么设计

Streaming ID 怎么设计
```

如果我们先把 Runtime V2 全写完：

```text
又发现 Asset Format 不适合
```

又得返工。

所以正确顺序应该变成：

------

# Phase 0：Performance Truth

先确保能正确测：

```text
CPU Frame

GPU Frame Envelope

Geometry
Shading
Effects

RAM Peak
VRAM Logical Resident
Upload bytes/frame

Asset Download bytes
Decode ms
First Visible Frame
P95/P99 hitch
```

------

# Phase 1：Asset Architecture V2

设计：

```text
CanonicalAssetIR

OAsset
OPack

Chunk
Page
AssetID
PageID

Compression Profile
GPU Profile
```

只先定 ABI 和工具链。

------

# Phase 2：Geometry Cook V2

```text
quantization
vertex packing
index optimization
meshlet
LOD
hierarchy
page packing
meshopt compression
```

然后 Runtime 不再支持任意 layout 的 hot-path。

------

# Phase 3：Texture Pipeline V2

```text
KTX2

BC7 / BC6H
ASTC
ETC2

offline mips

compressed residency
page/chunk growth
```

这一阶段我预计甚至可能比某些 Renderer shader 改动更直观地降低显存。

------

# Phase 4：GPU Geometry Frontend V2

再做：

```text
subgroups
primitive-index
Meshlet Work
selective Exact
adaptive hierarchy
streaming residency
```

------

# Phase 5：Shading V2

```text
VisibilityKey V2
shade bin
compute shading
shader-f16
compact material
```

------

# Phase 6：Bandwidth V2

```text
Surface Lite
material-lighting fusion
compressed / compact attachments
effect classification
```

------

# Phase 7：Frame Budget System

```text
Geometry budget
Texture budget
Shadow budget
SSR budget
DRS
Shading rate
Streaming budget
```

------

# 四十五、这里“预算系统”也应该变成统一的

现在很多引擎会各玩各的：

```text
LOD 一个 threshold

Texture 一个 budget

Shadow 一个 setting

SSR 一个 quality
```

我反而想做：

# `FrameBudgetController`

输入：

```text
Target GPU frame = 16.67 ms

current GPU:
Geometry 3.0
Shading 5.2
Shadow 2.0
Effects 4.5
```

然后预算：

```text
GeometryBudget
ShadingBudget
ShadowBudget
EffectBudget
StreamingBudget
```

------

# 四十六、例如相机突然靠近模型

不是：

```text
SSE 要求高
 ↓
无限生成 meshlets
 ↓
GPU 40 FPS
```

而是：

```text
Need more geometry

 ↓

Geometry budget available?
     │
    yes
     ↓
refine

     │
    no
     ↓
keep coarser LOD
```

与此同时：

```text
Texture projected texel density ↑
 ↓
请求更高 mip
```

这时候 Geometry 和 Texture 甚至应该协同：

```text
低 LOD
→ 不需要超高 mip

高 LOD
→ 才值得加载高 mip
```

这又是一个典型：

# 组合优化

------

# 四十七、甚至 Streaming Priority 可以由 GPU 可见性决定

例如：

```text
Page A
屏幕 400,000 pixels

Page B
屏幕 200 pixels
```

那么：

```text
Priority A >> B
```

而不是：

```text
谁先发请求谁先下载
```

可以计算：

```text
priority =
screenCoverage
× importance
× LODError
× cameraVelocity prediction
```

这才是高级 Streaming system。

------

# 四十八、再举一个“资源优化为什么会让 Renderer 更快”的具体例子

假设 Dungeon：

```text
Texture Resident
500 MB

Geometry
300 MB

Intermediate
500 MB

其它
300 MB

≈ 1.6 GB
```

显存压力很高。

做完：

```text
Texture BC
500 → 130 MB

Geometry quantization
300 → 130 MB

Surface V2
500 → 300 MB
```

变成：

```text
≈ 860 MB
```

这不是只得到：

```text
VRAM -740 MB
```

而已。

还有：

```text
cache locality ↑

memory controller 压力 ↓

bandwidth contention ↓

驱动 paging 风险 ↓

texture sample throughput ↑

GPU temperature / power pressure 可能下降

frame-time tail 更稳定
```

所以：

> **显存容量优化和 GPU 运行性能不是独立问题。**

这点你提得对。

------

# 四十九、CPU RAM 同理

例如我们把：

```text
glTF decoded geometry
+
canonical geometry
+
GPU shadow
```

从三份减少成：

```text
Compressed Source Cache
+
temporary decode
+
GPU Runtime
```

那么：

```text
GC 压力 ↓

浏览器 memory pressure ↓

worker transfer ↓

加载 hitch ↓
```

这些最终都会影响真实体验。

------

# 五十、所以以后 EEngine 的性能评分表也应该改

不能只看：

```text
FPS
```

我会设计成：

| 类别      | 指标                                         |
| --------- | -------------------------------------------- |
| Frame     | FPS / CPU P50/P95/P99                        |
| GPU       | GPU Frame P50/P95                            |
| Geometry  | visible meshlets / vertex work / queue bytes |
| Shading   | shaded pixels / material work                |
| Texture   | sampled/resident bytes / format              |
| VRAM      | resident / transient / peak                  |
| RAM       | asset peak / runtime steady                  |
| Loading   | download MB / decode ms                      |
| Startup   | time-to-first-frame                          |
| Streaming | bytes/s / page requests / hitch              |
| Resource  | upload bytes/frame                           |
| Quality   | LOD error / texture mip deficit              |

这样以后你会看到：

```text
Optimization A:

FPS            +3%
VRAM          -70%
Download      -60%
Load time     -40%
P99 hitch     -50%
```

这种优化当然：

# 非常值得做

即使：

```text
FPS 只 +3%
```

------

# 五十一、还有 Multi-Draw，我也重新给它定位一下

你说可以依赖新能力。

当前 2026 CTS 已经可以看到：

```text
multiDrawIndirect
multiDrawIndexedIndirect
```

相关测试，但标准公开 Type/API surface 目前仍主要暴露单次 `drawIndirect/drawIndexedIndirect`；Chrome 早先提供过 experimental multi-draw。([GPU Web](https://gpuweb.github.io/gpuweb/?utm_source=chatgpt.com))

所以我会这样做：

```text
标准 2026 High Profile
----------------------
primitive-index
subgroups
shader-f16
compressed texture
texture format tiers


Experimental Ultra Profile
--------------------------
multiDrawIndirect
```

不要让整个引擎依赖一个还可能变化的 experimental API。

但：

> ABI 要允许以后直接接 MultiDraw。

------

# 五十二、最终我对 EEngine 的方向已经不只是：

> GPU-driven Renderer

而应该更准确叫：

# GPU-driven + GPU-native Asset Engine

两半：

```text
                EEngine

      Asset Driven          GPU Driven

           │                    │
           ▼                    ▼

      Asset Cook           GPU Visibility
      Quantization         GPU LOD
      Compression          GPU Queue
      Streaming            GPU Raster
      Residency            GPU Shading
      Paging               GPU Effects

           └──────────┬─────────┘
                      ▼

               Data-Oriented Runtime
```

这个架构会比单纯：

```text
“再优化一下 visibility”
```

完整得多。

------

# 我现在会正式修正上一版结论

之前我给前三：

```text
1 Meshlet Work
2 Compute Material
3 Canonical Vertex
```

现在如果考虑**整个引擎长期性能架构**，我会改成：

> **第一层：先设计 Runtime Asset Format / Cooker / Compact Data ABI。**
> **第二层：重构 Geometry + Texture Residency。**
> **第三层：重构 Geometry GPU Pipeline。**
> **第四层：重构 Material/Shading Pipeline。**
> **第五层：做 Surface/Effects/Frame Budget。**

因为：

```text
数据长什么样
```

决定了：

```text
GPU 到底要搬多少数据
```

而：

```text
GPU 搬多少数据
```

往往比：

```text
shader 多做几次乘法
```

重要得多。

------

## 我认为现在最值得做的是这一版完整架构

```text
OEngine Performance Architecture V2
│
├── 1. WebGPU 2026 High Profile
│   ├─ subgroups
│   ├─ shader-f16
│   ├─ primitive-index
│   ├─ texture formats tier1/2
│   └─ BC / ASTC / ETC2
│
├── 2. Asset Compiler
│   ├─ Canonical IR
│   ├─ .oasset
│   └─ .opack
│
├── 3. Geometry Pipeline
│   ├─ quantization
│   ├─ meshopt
│   ├─ meshlet
│   ├─ LOD
│   ├─ hierarchy
│   └─ page packing
│
├── 4. Texture Pipeline
│   ├─ KTX2
│   ├─ GPU block compression
│   ├─ offline mip
│   └─ streaming
│
├── 5. Memory / Residency
│   ├─ Geometry Page Cache
│   ├─ Texture Residency V2
│   ├─ CPU lifetime
│   └─ streaming feedback
│
├── 6. Geometry Rendering V2
│   ├─ subgroup queues
│   ├─ Meshlet Work
│   ├─ primitive-index
│   └─ selective Exact
│
├── 7. Shading V2
│   ├─ VisibilityKey V2
│   ├─ shade bins
│   ├─ compute shading
│   └─ f16
│
├── 8. Bandwidth V2
│   ├─ Surface Lite
│   └─ Lighting fusion
│
└── 9. Global Budget Controller
    ├─ geometry
    ├─ texture
    ├─ shadow
    ├─ effects
    ├─ streaming
    └─ resolution
```

这一次我认为已经更接近你真正想做的：**不是修“相机拉近掉帧”，而是把 EEngine 的数据、资源、加载、显存、GPU work 和 shading 全部围绕性能重新设计。**