# 约束有效性验证

约束文档也需要测试。

1. 选择一个会触发目标约束的小型真实任务。
2. 不直接告诉执行者要读哪个文件。
3. 要求最终列出读取文件、修改文件、关键决策和验证。
4. 检查是否经过 `CONTEXT-MAP`、Context、局部 `AGENTS` 和相关 ADR。
5. 如果未读到，调整根入口或路由；如果读到但行为错误，补决策规则和禁止边界。

典型场景：新增 GPU 队列、修改 Geometry ABI、增加渲染效果、声称性能提升、修改 three.js 参考目录。

