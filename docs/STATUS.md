# OEngine 当前状态

更新时间：2026-09-13。本文件只记录当前能力、开放风险和下一步；实施过程与旧结果从 Git 查询。

## 当前基线

- WebGPU Renderer、FrameGraph、FramePlan、Feature/Service 组合和公开入口已经存在。
- GPU-ready geometry package、`GpuAssetStore`、`GpuScene` 与 `GpuRenderWorld` 已形成资源边界。
- Packed source 与普通 Scene adapter 已汇入同一 GPU Render World；未注册 Scene 直接失败。
- Packed hierarchy/work generation、Hardware Visibility 的 `VisibilityKey + ShadingBinId` 双 MRT、Sparse Shading Bin classify/finalize、active-bin specialized material/fused direct lighting 与按需 compact SurfaceLite 已接入唯一生产主链。
- 旧 Pixel Queue、ShadeWork 和可见像素 scan/scatter 生产链已退出生产路径。
- direct lighting、CSM、GI、AO、SSR、MBOIT、Temporal 与 HDR post 接入同一 Renderer 主流程。
- Performance Inspector 仍是生产侧共享实时 Profiler/Timeline；旧综合质量与性能 fixture 已删除。
- [ADR-0012](./adr/0012-example-library-reset.md) 的过渡性重置已完成；`examples/` 当前是 Example Library V2 的 standalone Vite MPA + Storybook iframe catalog，包含 Basic Scene 与 Rendering Lab，但不承担 Browser Validation 或 formal benchmark。[ADR-0014](./adr/0014-browser-validation-and-performance-host.md) 的独立 `validation/` package 已建立 versioned protocol/registry/workload/artifact contract、内容 hash/new-run nonce、新鲜度与 dispose Gate、精确错误 allowlist、隔离 Chrome/Vite 生命周期及本机 raw artifact 策略；`protocol-self-test` 与 `webgpu-component` 已在 clean commit `30155aaaefe68e3ee6b04abb523f17cfeadda080` 上通过。后者实际选择 NVIDIA/Turing adapter，记录完整显式 limit、WebGPU 2026 API/WGSL probe、三类 error scope、bounded readback、one submit 与 intentional device destroy；两个 result artifact SHA-256 分别为 `153dd15aad0b3013139018ba061eb9996cacdba8dfece123a2eb4a5dd69efe61`、`f5959a569a45ff942cae6bcbd20dc718a94ef9ba7bda651381b4a48db014e472`。ADR-0013 `shading-bin-component`、`shading-resolve-component` 与 Step 6 `sparse-shading-candidate/correctness-v6` 的五个 correctness workload 及 resize retirement 已有各自范围内的 clean accepted evidence；fresh/stale/replay/crash/timeout 等协议故障矩阵、Step 6 其余 lifecycle workload、production cutover case 和 formal PERF runner 仍未完成。旧浏览器结果和既有 benchmark 仍只证明其冻结 commit。
- [ADR-0013](./adr/0013-sparse-shading-bin-pipeline.md) Step 0 为 `implementation-complete`（implementation commit `e15943f`）：已冻结 10 个旧→新迁移域、全部第 16 节删除 owner、9 个外部来源采用/拒绝状态，以及 Capability/Identity/Visibility/Queue/Classifier/Shading/Feature-off/Lifecycle/Diagnostics/Performance/Deletion 共 64 条稳定 requirement id。证据 owner 为 `OEngine/tests/fixtures/adr-0013-migration-manifest.json`（artifact id `adr-0013-step-0-migration-v1`）；当前设备政策已收敛为唯一阻塞 adapter `NVIDIA GeForce RTX 2060 SUPER`，其他设备只作非阻塞补充。L0 的 typecheck、build:test、shader source audit、文档系统与 targeted ownership/ABI tests 已通过。formal L5 baseline 因 ADR-0013 baseline/candidate workloads 和 PERF orchestration 尚未接入新宿主而保持 `open`；后续按用户要求以 implementation-first 推进到 Step 7.2，但没有豁免这项 Gate。Step 0 只冻结迁移合同，当前生产 owner 与开放项以 Step 7.2 状态条目为准。
- ADR-0013 Step 1 为 `implementation-complete`（implementation commit `08c02d4ce308a71123ff816a53fe670a8516af09`，tree `f3f66f2968abcd82742242f48f89e7b34a48de13`）：新增且仅新增内部 `GpuShadingProgramAbi`、`GpuShadingBinAbi` 和两组 targeted tests；冻结 versioned 64-entry CPU/WGSL dependency LUT、16 program/64 bin identity、全部 struct/heap/indirect offset 与 usage、checked extent/容量/binding/二维 dispatch preflight，以及 macro/microtile、all-or-nothing reservation、finalizer fail-closed 的唯一 CPU oracle。L0/L1 通过 `npm run typecheck`、`npm run build:test`、shader source audit、公开导出扫描、现有 ABI/ownership tests 和 `shading-bin-abi`/`shading-bin-reference` 的 exhaustive/property/boundary matrix；test artifacts SHA-256 为 `35af6eed9ebf584266d089df598fb2d385132fbaa1390e02dc8e67f6ea740e08`、`fe95fa23dc08469ecdc0363202e39f01cdd48431aa0616d7b3415fa852695074`。本 Step 按范围未创建 Renderer/GPU resource；真实 WGSL compile、L3 GPU、L4 browser 和 L5 PERF 均未声明通过。
- ADR-0013 Step 2 为 `implementation-complete`（implementation commit `277f1d8edc90c37528622a151603927b0a65ce79`，tree `95adf08988e555f3c2b198a85ea623704b38a9b3`；WebGPU 2026 location correction `6808b05`）：内部 capability plan 在 adapter 阶段精确验证 `core-features-and-limits`/`subgroups`/`texture-formats-tier1`、九项下限和从 `GPUAdapterInfo` 读取的 4..128 subgroup range，并冻结 post-device actual fingerprint；pipeline descriptor key 覆盖 program/set/output/capability/format，440 个合法 specialization 均从真实 binding declaration 推导 V2 budget，最宽变体精确为 4 groups、16 sampled textures、8 samplers、10 storage buffers、5 storage textures、4 uniforms，textureless/unlit/ColorOnly 无用 binding 物理缺席。L0–L2 静态门禁通过 typecheck、build:test、source/directive audit、BGL/WGSL/format descriptor oracle、每项 limit -1/equal/high table test 与 cache-key permutation；test artifact SHA-256 为 `8ff48bd4e7eb7cec82dbc928b3f89637aff6976a6a2d7db2c9a8a98ffa152811`、`b1e7a9558338a334b7ab7e7005431757103b440890ab3b1373e6dee9b12ee540`。Step 7 前未接入 `MainRenderPipeline`，所以没有改变旧 Renderer 设备集合；Step 4 component 已记录实际 capability，但 440 个 Step 5 specialization、L4 与 L5 仍为 open。
- ADR-0013 Step 3 为 `implementation-complete; production-publication-integrated`（基础 implementation commit `74c9cf91a0c546d20fef341c655279b780f9a73b`，production identity/summary commit `54e4a27`，association-specific material publication commit `fe021f8`，immutable production scene-source commit `23fa6da`）：内部 `GpuShadingPublicationPlan` 以唯一 candidate 事务覆盖 bulk、instance add/remove、Active/Transparency、material/geometry/association patch、TextureBindingSet relocation 与 extent/output/capability context；同一 immutable snapshot 原子冻结 64-bin refcount/mask、receiver/dependency truth、instance/MeshletWork bin、material/texture/geometry generation、dense sizing、pipeline descriptor 与逐组 bind-group publication。生产 `GpuRenderWorld` 现在也以 material × cooked geometry semantics × TextureBindingSet 的同一 LUT derivation 维护唯一增量 `ActiveShadingSummary`：bulk stage 直接把 6-bit `ShadingBinId` 写入 `GpuInstanceAbi V5 packedRasterFlags[13:8]`，既有 GPU work generation 原样复制到 MeshletWork；material 与 Active visibility patch 在一个 revision 内更新 64-bin/dependency/receiver truth，aborted command 恢复原 immutable summary 与 instance association，stable getter 不扫描 Scene/material registry。`GpuMaterialStore` 已停止按 `StandardShadeMaterial` 误去重，改为一个 RenderWorld publication 内按 `materialIndex × ShadingBinId` 发布有效 association slot；最多 16 种 geometry semantic profile 参与构建，运行时查询表固定为 `materialCount × 64`，不会随 geometry 数量做笛卡尔展开。同一材质的 `UnlitFactor/UnlitFactorColor` 或 `UnlitTexture/UnlitTextureColor` 因此拥有不同 record/handle，而不改变 program 的 PBR geometry profile 继续复用同一 record。唯一 272 B `OEngineShadingMaterialRecord` 与每槽四条 16 B texture route 共用一次非零 material/texture/publication generation；abort 不推进 generation，release-abort 恢复 ownership，slot 只在 submitted-work completion 后回收。Visibility、Shadow、Transparency、旧过渡 compute/debug consumer 与 candidate 均改读该唯一 record 的 `payload`，candidate 的并行 240 B visibility table 已删除。旧 `opaqueKernelClassCounts`、`activeKernelMask(s)` 与 3-bit instance helper 已删除；`fe021f8` 命中的 OEngine typecheck/build:test、103 个 targeted tests、validation typecheck 与 `git diff --check` 通过，其中新增 `shading-material-store.test.mjs` 覆盖 record/route/generation/abort/release，RenderWorld 用例覆盖同材质 Program 0/1 分槽。`MainRenderPipeline` 尚未消费该 summary，旧 MaterialTile consumer 与 shading payload 内的过渡 KernelClass 仍待后续原子 composition cutover 删除；本项未重跑浏览器，剩余 L4 lifecycle 与 L5 PERF 继续保持 open。
  `23fa6da` 进一步把 production scene truth 固化为 `GpuRenderWorldRuntime.shadingPublication`：一个 revision 原子持有 material/geometry/instance publication source、实际 material/texture generation、由 `GpuAssetStore` stable slot/generation 得到的 geometry identity 与同一 `ActiveShadingSummary` 对象。material/Active patch 在当前主命令中发布新 immutable source，abort 同时恢复 instance association、summary 和 snapshot；transform-only/stable frame 保持旧引用且不扫描 Scene。changed paths 为 `GpuAssetStore.ts`、`GpuRenderWorld.ts` 与两组 residency/RenderWorld tests，命中 ADR 第 10、14、17.3 节的唯一 truth、generation、rollback 与 stable-frame 要求。L0/L1 runner 为 OEngine `typecheck`、`build:test`、`packed-render-world-contract`、`runtime-geometry-instance-residency-v2`、`shading-bin-publication`，53/53 通过；artifact id 为 `adr-0013-production-shading-scene-source-v1`，content hash/tree 为 `232a0882bf7767827bdb9f446c13d22314d63880`。本切片未运行浏览器、GPU timestamp 或 PERF，因为它只发布 CPU scene source；render-context snapshot 派生与 live revision consumer 仍为下一实现项，L4/L5 保持 open。
- ADR-0013 Step 4 为 `implementation-complete; producer-l3-validated; production-mrt-integrated`（基础 implementation commit `057c0fb6111ed1f321eb29272475df319ff4f7ce`，WebGPU 2026 fixes `6808b05`，validation owner `c8aae737a7af734fdbabe7104806b4501773dace`，production MRT integration commit `2f8bc3c`）：`r8uint` ShadingBinId attachment/双 MRT shader、CPU raster ownership oracle、64×64 macro/8×8 microtile 的 256-lane width-agnostic subgroup→groupshared classifier、每 macro/bin 一次 bounded CAS reservation、fixed scatter、独立 64-lane fail-closed finalizer，以及 exact heap/768 B indirect resource owner 已落地。`PackedVisibilityPass` 现在无条件创建 `VisibilityFrame.shadingBinId`，唯一 `MeshletBucketRaster` pipeline 同一 depth winner 写 `r32uint VisibilityKey + r8uint ShadingBinId` 并以 `0xffffffff/0xff` 分别 clear；单 MRT shader、pipeline-cache 维度、candidate-only raster method 与别名 shader 已删除。真实 Chrome 153.0.8010.37 在 NVIDIA/Turing（`subgroupMinSize..subgroupMaxSize = 32..32`）既有 component evidence 编译 production/diagnostics WGSL并运行 65×67 partial-macro deterministic `r8uint` 输入；原生 dispatch 与 limit=16 的二维矩形尾部逐 bin counters/layouts/record sets/64 组 args 均与 CPU oracle 相等，zero/one work 成立。invalid/inactive/revision/counter/reservation/dispatch 六类独立故障均在 consumer 前把全部 64 组 args 写为 `(0,1,1)`，GPU validation/uncaptured error 为零。production MRT integration 的 typecheck、validation typecheck 及命中 ownership/ABI/shader tests 68/68 通过；按当前 implementation-first 节奏未为本次接线重跑浏览器，因此 live Renderer 的 classifier/resolve consumer 与最终 MILESTONE 仍为 open。
- ADR-0013 Step 5 为 `implementation-complete; consumer-l3-validated; production-material-abi-integrated`（基础实现 commit `723d878d1be3c9030c0d6b7c9c61b8c14ec1f4af`；WebGPU/Surface ABI 修正及 validation owner commit `c81d266cef8154b61a9a82527657c8a6cb08254e`，tree `c73edfd54d6517a7f33c606530b7e381c4eb7d9b`；production material publication commit `fe021f8`）：272 B versioned shading-material record/16 B texture-route record、三 storage-buffer packed light/cluster input、16 个 creation-time `ShadingProgramId` family、8 种 output mask、checked pipeline owner 与 GPU-authored indirect consumer 已落地；生产 `GpuMaterialStore` 现已成为该 record/route ABI 的唯一 owner，旧过渡 shader 仅从其中读取 payload，不再拥有另一种物理 material table。真实 Chrome 153.0.8010.37 在 NVIDIA/Turing（subgroup 32..32）编译 16 个 production 与 16 个 diagnostics widest variants，并以 33×33 deterministic mixed-material/mixed-TextureBindingSet 输入执行 classifier→finalizer→indirect resolve；五张输出逐像素与 CPU/numeric oracle 相等，HDR f16 最大误差 `0.0009844413`，normal、unlit diffuse、packed metallic/roughness/flags、RGB9E5 emissive、AO 与 velocity 均通过冻结 ABI。program 0 的 10 个 microtile 真实产生 `(5,2,1)` dispatch，48 个 inactive bins 的 dispatch X 全为 0；production/diagnostics 正常输出 hash 同为 `3899b331`。meshlet/geometry mismatch 均抑制 1089 lanes，material/texture generation fault 分别在 64 lanes 首次设置 frame-invalid 并使 Final Output 不可展示；独立 diagnostics 捕获完整 8×8 tile 的 64 duplicate 与 64 unassigned，而 production 场景不创建 claims resource/pass/readback。accepted run `2026-09-13T02-53-55-744Z-shading-resolve-component-de8385e2-d8be-4e71-b09d-910adec840df` 的 result/readback/events SHA-256 为 `073b16ab2b79a488b8044d99cb29017c06a7be42fb5d6bbcf417a57bde68b12c`、`07f04d89fd282a3b003493f05339ea717f01c74bf66caaace055f9b61d2c1ffd`、`d2b5c706f7e88d645627df2cb4fe04a18753721c014aa2d3d0b551e34ef4e5d9`；GPU/browser errors 为零，dispose 后 buffer/texture/resolve owner/device/listener 均为零。该 component 不验证 Step 6 FrameGraph/lifecycle、下游 GI/AO/SSR/Temporal/Post 组合或 PERF，candidate 仍未接入 live `MainRenderPipeline`；`fe021f8` 后按 implementation-first 节奏只运行 DEV/static 验证，浏览器证据仍引用上述冻结 revision，不冒充当前 revision。
- ADR-0013 Step 6 为 `implementation-complete; production-revision-owner-ready; partial-l4-validated; runtime-gates-open`（DEV composition commits `173d54e11438a2f18bf36db9ba6ba7f71bca02c4`、`2140304f6d61f032a7f0c3afeb6cafb4a12bb55f`、`0bf00ab7db9185dc3fec4db7ea669b0d5b6a6542`；真实 downstream 子图扩展 seam commit `03d6327`；HDR-only clear correction `d50ce76`；首个 L4 slice commits `c829cde`、`2931fd9`、`364b7a6`；sparse-output clear correction `96e32da`；BasicCubeNear/Far commit `361ed93`；UnlitVertexColor commit `8bcd1ce`；UnlitTexture commit `8ba5d29`；RenderingLabFixed commit `76ba0bf` 及 validation binding/history/oracle fixes `ef812f7`、`38c152d`、`e2b0393`；resize GPU revision owner commit `c8b3fea`、browser workload commit `cd015ee` 与 generation fixes `6e9dd91`、`64190b7`；production owner/FrameProduct promotion commit `bb3da75`）：未公开导出的 candidate FrameGraph/runtime/executor 把 Visibility MRT、独立 bin clear+classifier、独立 finalizer、按 output dependency 创建的 sparse outputs、active-bin indirect resolve 与按需 SSGI/GTAO/SSR/Temporal/Post/history/diagnostics 边界编入同一外部提供的 `ShadeGPUCommandContext`；classifier→finalizer→output clear→resolve 以及 light/shadow producer→lit resolve 均为显式图边。downstream 现在提供 graph-build-time composition seam，使生产 feature owner 能把真实多 Pass 子图直接展开进同一 FrameGraph；不再要求用一个合成 compute dispatch 冒充 GTAO/SSGI/SSR/Temporal。output clear 只以无 draw render pass 初始化必有 HDR；Surface/Velocity background texel 遵循未定义合同，由 consumer 先检查 depth/visibility validity，不增加无需求的全屏 clear，也不为已关闭的输出创建 attachment。最初 candidate-only 的双 MRT seam 已由 `2f8bc3c` 收敛为唯一 production `MeshletBucketRaster`；candidate 与主管线现在共享同一 portable/primitive-index ShadingBin shader、`r32uint + r8uint` targets 和双 attachment clear，不再存在单 MRT backend。`SparseShadingGpuRevisionOwner` 已从 candidate 命名提升为 production owner，在 CPU publication commit 前完整准备 heap、768 B indirect args、256 B immutable settings 与 resolve pipeline closure；publish 后旧 closure 只在引用它的 submission 完成后退役，abort/device loss 同时销毁全部资源，stable `active()` 不分配。验证 fixture 不再拥有或逐帧重写另一份 settings。finalizer 的版本化 heap/args 现在发布 ADR 第 9 节正式 `ShadingBinFrame`，携带 ABI、generation、active masks、8×8 microtile 与 `internal-full` domain；resolve 在消费前核对 FrameProduct 句柄及由 active specialization 导出的完整 64-bin mask。无 opaque 时 revision resource 与 FrameProduct 均为 `null`。独立 diagnostics pipeline 的 shader/pipeline/buffer/pass 在 diagnostics-off topology 物理不存在。candidate runtime 以 frame ticket 保证 abort/submit exactly-once、history 只在成功 submit 推进、旧 publication 按 completed serial 退役。Final Output 新增 creation-time `shading-bin` validity ABI，直接读取 `ShadingBinControl.frame_flags`，不再把旧 `MaterialClassificationControl.frame_invalid` offset 套到新 control；capture 还显式记录真实 encoder-work 数量，并只在 capture boundary 存在时为 VisibilityKey/ShadingBinId 增加 `COPY_SRC` usage。`bb3da75` 的 OEngine/validation typecheck、build:test、63 个命中 FrameProduct/FrameGraph/visibility/ownership/registry tests 与 `git diff --check` 已通过；本切片未重跑浏览器，既有 L4 artifact 仍只证明其冻结 commit，且该 owner 尚未接入 live `MainRenderPipeline`。

  `sparse-shading-candidate/correctness-v4` 已在 clean commit `8ba5d29028e47e94eda8d04bd2f4bb41e7c671aa`、tree `42a6e6e65138b294ff79001a803f4e99edaf6cd4` 上通过 Chrome 153.0.8010.37 的 NVIDIA/Turing adapter（subgroup 32..32）。`MixedBins` 继续以 256×256 Hardware Visibility 覆盖全部 65,536 像素和 16 program/16 active bins；每 bin 256 个 microtile record，总 `attempted=written=4096`，48 个 inactive bin dispatch X、overflow/frame error 均为 0；16 个 GPU-authored indirect resolve 加 directional direct lighting 的 HDR 对 CPU oracle 最大误差 `0.001707996`。`BasicCubeNear/Far` 使用同一单 Meshlet、8 顶点/12 三角形、单 UnlitFactor material 和 immutable publication，只把 reverse-Z 60° 透视相机从距离 4 更新到 8；逐像素解析 silhouette 与 VisibilityKey/ShadingBinId/HDR readback 完全一致，近/远分别覆盖 21,904/4,096 像素和 400/64 microtiles，queue attempted/written 与 indirect dispatch X 同值且 overflow 为 0，coverage ratio `5.34765625` 与 oracle 相等。`UnlitVertexColor` 独立发布 Program 1/bin 1，以 4 个不同 authored color 顶点和 2 个三角形覆盖 12,100 像素/196 microtiles；逐像素按 VisibilityKey primitive 重建重心插值，HDR 最大误差 `0.000971776`，Final Output 有 11,105 种 RGB。`UnlitTexture` 独立发布 Program 2/bin 50/TextureBindingSet 3，在 4×4 texture layer 1 上覆盖全部 16 个非均匀 texel；逐像素重建 UV、nearest-clamp sample 与材质因子，12,100 像素/196 microtiles 的 HDR 最大误差 `0.000399050`。全部 unlit 帧背景的 bin sentinel/HDR 非零、identity、inactive dispatch、overflow 与 diagnostic magenta 均为 0，且图中不创建 light/cluster/environment resource/pass；最终输出只保留合同内一 LSB SDR triangle dither。五个采样帧各自 graph build/compile/execute 1、`Renderer/main-0` submit 1；fixture 间与最终 dispose 后 buffer/texture/runtime/graphics/device/listener 均为 0。accepted run `2026-09-13T05-06-57-758Z-sparse-shading-candidate-c1415156-97eb-4801-87e3-d70c8645b43a` 的 result/readback/events/screenshot SHA-256 为 `e9ac503cf0fdf035ff63739fbad8a7f2d2ab53f0500e96e8b20ca4301a13d085`、`81f60982d95b794f81e32db515196a34c9b7f464e4af75dde15a3e2b207b6f7a`、`b7b856c72823d3da3ab23c33aa629456157f88a672aedbf5ecaa6446f4b8b7dd`、`6246599b0665316d5d702805872dc7fc1417f990028841f5add78ca69e499eee`。该 Case 只关闭 MixedBins、BasicCubeNear/Far、UnlitVertexColor 与 UnlitTexture 明确覆盖的 L4 项；RenderingLabFixed、完整 resize/patch/toggle/replace/cut/abort/device-loss matrix 与当前指定 `NVIDIA GeForce RTX 2060 SUPER` 的 L5 formal PERF 仍为 `open`。因此 Step 6 Exit Gate 未关闭并继续阻塞 Step 7 production cutover；其他 adapter 不阻塞本 ADR。
  `sparse-shading-candidate/correctness-v5` 随后在 clean commit `e2b0393beca55ca2850a750146aba86a96f397b3`、tree `7879d3741a2e1f199ee9c9448563bc5bd9a6d89b` 上加入并通过 `RenderingLabFixed`。该 workload 发布 programs `[0,2,4,12]`，以四个有实际 transform 的 cube/floor instance 同时覆盖 lit、unlit 与 textured 路径；cold/warm/stable 三帧均解析 24,685 个可见像素，四个 active bin 分别写入 `[299,1821,18426,4139]` 个 work pixel，microtile queue 每帧 `attempted=written=462`、overflow 0。256² shadow atlas 由同一 MeshletWork/scene/geometry/meshlet/vertex buffer 的真实 GPU reverse-Z raster 生成，覆盖 20,682 texel且深度范围为 `0.398348..0.634544`；不是常量或 CPU 伪造 shadow 输入。candidate graph 每帧展开 28 个 pass，包含 production HZB、Occlusion Confidence、Temporal Validity、GI、Opaque Lighting、Three.js r186-derived SSGI 全链、shared color pyramid、SSR trace/shading/temporal/denoise/correction、Final TAA/TAAU、history copy 与 Tonemap；不存在合成 `SparseShading/downstream/*` pass。三帧 HDR 全部 65,536 像素有限，24,685 个 lit pixel 的最大亮度从 `7.3422` 收敛到 `7.2654`，最终 presentation 分别有 4,355/4,354/4,347 种 RGB 且 diagnostic magenta 为零。runtime 记录 `frameEncodes=3`、`submittedFrames=3`、`abortedFrames=0`、`pendingFrames=0`、`historyGeneration=4`、`lastSubmittedSerial=3`；每帧恰有 `Renderer/main-0` submit 1，private submit 0，最终 error 与未释放 owner 均为零。accepted run `2026-09-13T05-48-40-737Z-sparse-shading-candidate-3751f40d-90ed-4461-abd2-b9d0e61c3ba7` 的 result/readback/events/screenshot SHA-256 为 `e356bdf8628ef7db4b6fdea2ce00630ffa2802559e69f31f448a8b9cbd7bea3c`、`b71e4348677bc37c93426a0620d7b9faaf31c9ac342a7c0b1fca3873e826d888`、`91e704fc6e8d3710de196c4fa2d4d6f3f23756a3c7e41bdd4137365288e25d50`、`49fc846c2495cfec2ba5d05b4e7e88cc2bc1c20e70cfcf3960c793b5e72dd937`。这关闭了 ADR-0013 列出的五个 L4 correctness workload；完整 resize/patch/toggle/replace/cut/abort/device-loss matrix 与当前指定 `NVIDIA GeForce RTX 2060 SUPER` 的 L5 formal PERF 仍为 `open`。因此 Step 6 Exit Gate 未关闭并继续阻塞 Step 7 production cutover；其他 adapter 不阻塞本 ADR。
  `sparse-shading-candidate/correctness-v6` 在 clean commit `64190b763f05d0b85a344bd50b90986ef31495a6`、tree `d9c2fe5a38f0228c6b9f6d0eaf923d855a094dfe` 上关闭 resize retirement 项。`LifecycleResize` 先以 revision 2 执行 256×256 BasicCube，再在该 submission 的完成边界前准备并发布 revision 3 的 256×128 heap/args/layout、settings、MeshletWork generation、material/texture publication records 与 consumer；此时旧 revision 2 仍明确列于 `retiringRevisions=[2]`、`retiringBytes=7168`、`retireCount=0`。submission 1 完成后它才变为 `retiringRevisions=[]`、`retiringBytes=0`、`retireCount=1`；active heap 从 6,400 B 缩为 4,352 B，indirect 维持 768 B。初始、resized、reuse 三帧分别覆盖 21,904/5,476/5,476 pixels 与 400/100/100 microtiles，全部 `attempted=written`、overflow/frame/identity/background error 为零；后两帧 HDR/presentation hash 相同，reuse 没有创建第三个 GPU revision。全 Case 11 帧各自 main submit 1、private submit 0，最终 browser/GPU error 和 buffer/texture/runtime/graphics/device/listener 泄漏均为零。accepted run `2026-09-13T06-12-07-943Z-sparse-shading-candidate-1c683931-f6ec-41a3-bafe-2d4ae5fb1492` 的 result/readback/events/screenshot SHA-256 为 `fe064b11760e43ec23acb3c53ab99376ee5611539f8ef9cc158f758768d8dde9`、`317c5130ec527ca76803dd0733fb19bd369c1bfb3b22aae4470b5c743076ea22`、`03eea94104b5b743f3fe00c8b54965e5d334319c3f049989bbd96adbd89cca51`、`8a12aabd94f00b817fd22970da894759733821fb2e92db5b208241343cae7795`。其余 material/association patch、TextureBindingSet relocation、feature toggle、scene replace、camera cut、aborted submit、device-loss recovery 与本机 L5 formal PERF 仍为 `open`，Step 6 Exit Gate 继续阻塞 Step 7。
  `c27c403` 将 classifier/finalizer 与全部 active-bin specialized resolve 的执行期 bind-group 创建收敛到 revision-local resource-tuple cache：key 使用真实 GPU resource identity 与 buffer offset/size，不受临时 `GPUBufferBinding` 包装对象影响；同 tuple 重用，资源/range 改变才创建，失败创建不污染 cache，revision destroy 清除全部引用。`SparseShadingGpuRevisionOwner.evidence()` 现在分别发布 producer/resolve request 与 creation 计数，供后续 stable-frame browser Gate 证明 warm 后 creation delta 为零；两个 validation consumer 已删除各自的 sparse resolve bind-group 工厂并统一调用 owner API。实现 commit 的 OEngine/validation typecheck、build:test、41 个命中 classifier/specialization/FrameGraph/ownership tests 以及新增 resource-tuple cache 的 2 个 fault/identity tests 均通过；本项只关闭 DEV 结构与 CPU fake-device Gate，未重跑浏览器，因此不把 stable-frame L4 Gate 标成通过。
  `7ad01c1`（tree/content hash `a638e305882104275f902cd02b28fc5dfae70d8a`，artifact id `adr-0013-production-publication-coordinator-v1`）新增 production-only `SparseShadingPublicationCoordinator`，把 `GpuRenderWorldRuntime.shadingPublication` 与 internal extent、output dependency、shadow specialization、capability/sizing limits 组合成唯一 `GpuShadingPublicationSnapshot`，并在同步 frame loop 外完成 pipeline/heap/args/settings preparation。CPU transaction 只在完整 GPU closure 成功后 commit；source/summary 或 material generation 不一致在 GPU 创建前失败，stable scene/context 返回同一 active revision，并发同请求复用同一 promise。resize/scene replacement 的旧 CPU/GPU revision 共用 submitted-work boundary；该 serial 在异步 preparation 完成后才读取，避免编译期间的新 submission 被提前退役。`SparseShadingGpuRevisionOwner` 另加 lifecycle epoch，使 destroy/device loss 与 in-flight factory 竞争时销毁 late output 而不发布失效 revision。changed paths 为 coordinator、revision owner 与专用 transaction/lifecycle test；L0/L1 运行 OEngine typecheck、build:test 及 publication/FrameGraph/RenderWorld 63-test matrix 全绿，专用 4/4 fault/lifecycle tests 复跑通过。未运行浏览器/GPU timestamp/PERF；该 coordinator 尚未由 `SurfaceFeature/MainRenderPipeline` 消费，L4/L5 和旧 owner 删除仍保持 open。

  `30aac52f7defba9cb5e25b4364bec65da3a1536d`（tree/content hash `71dc46c715b96afdbbbf9415aaffe8506dbe2367`，artifact id `adr-0013-instance-geometry-generation-v1`）把 production Instance ABI 升至 V6，并用 64 B static record 中原保留的 `bounds_min.w` lane 保存每个 geometry association 的非零 AssetHandle generation；总 stride 仍为 176 B。`GpuScene` bulk publication 从 `GpuAssetStore.publicationIdentity()` 同时写 slot/generation，稀疏 triangle reconstruction 改为把资产 metadata generation table 与命中实例的期望 generation 逐条比较，不再使用无法表达混合代际资产的 scene-global geometry generation。component/candidate fixture 同步到新 ABI，geometry fault 仍通过篡改资产 generation table 触发 fail-closed。L0/L1/L2 已运行 OEngine typecheck、build:test 与 residency/instance/specialization/binding 29-test matrix，29/29 通过；未运行浏览器、GPU timestamp 或 PERF，真实 production asset metadata heap 与主管线 consumer 仍是后续实现项。

  `6aec1dde437adf02269ab2b0002b8b2bd98dfa94`（tree/content hash `1889c60cf9a9da669f092e7135cd6057f765a242`，artifact id `adr-0013-production-asset-heaps-v1`）让 `GpuAssetStore` 在原 resident/release command transaction 中发布 production `asset-metadata-heap` 与 `vertex-payload-heap`：前者由 geometry records、meshlet records、逐 stable slot generation table 组成，后者由 meshlet vertex indices、triangle bytes、canonical vertex stream data 组成；布局发布六个 word base、geometry/meshlet high-water count、byte size 与 heap epoch，供 Group 1 在 MeshletWork/Instance 之外只占两个 storage binding。构建只记录五段 GPU copy 和一次 generation staging copy，无 private submit/readback；任一容量/创建/command 失败回滚全部 source cursor、slot 与 heap identity，release-abort 恢复旧 heap，commit/reuse 把 generation 从 1 推进到 2，旧 heap 经 submitted-work completion 才销毁。两块 resolve-oriented 派生表示的 resident/allocated/retiring/peak bytes 已纳入 `AssetResidencyEvidence`，并计入 512 MiB residency budget；不能把当前额外物理字节描述成 alias 或免费。L0/L1 已运行 OEngine typecheck/build:test、validation typecheck 与 RenderWorld/publication/residency/specialization 71-test matrix，71/71 通过。未运行浏览器、GPU timestamp 或 PERF；`SurfaceFeature/MainRenderPipeline` 尚未绑定这些 heap，实际内存与性能判断保持 open。

- ADR-0013 Step 7.1 checkpoint 为 `implementation-complete`（implementation commit `dd14eef804989627d3280eaf064762be4dcc94ed`，tree/content hash `41ae8a8cde1e219c03c7bf4c4776fe64d1cd026f`，artifact id `adr-0013-production-sparse-lighting-abi-v1`）：新增唯一 240 B `GpuSparseShadingFrameAbi`，把 extent、material/texture/publication generation、资产 heap word bases、frame/PreExposure/upscale、camera 与 current/previous VP 固定为 checked CPU/WGSL 合同；scene-global geometry generation lane 明确保留为零，逐实例 generation 仍与资产 table 核对。Lit sparse specialization 已删除 candidate-only `GpuSparseShadingLightAbi` 及其简化 16×16 cluster/3×3 projected shadow，自此直接读取 production paged `LightDatabase`，并从 `lighting_direct.ts` 的同一源码权威派生 Filament BRDF、32×32×24 cluster、active-list fallback、5×5 optimized gather、directional CSM blend、point contact-hardening 与 spot shadow；旧过渡 Lighting consumer 同时继续使用该权威，没有复制第二套算法。为守住 ADR §8.6 的 10-storage ceiling，ClusterData 升为 V2：32 B header 内发布 `active_written`，固定 tuple prefix 保存 GPU filtered active-list snapshot，普通 cluster references 从 prefix 后分配；sparse lighting group 仍只有 `LightDatabase + lookup + data` 三个 storage binding。Direct-only resolve 不读取 environment/IBL，因此相应 uniform、三 texture 与 sampler 已物理删除；当前 concrete widest 使用 13 sampled textures、7 samplers、10 storage buffers、5 storage textures、3 uniforms，未修改 ADR 冻结 16/8/10/5/4 上限。两个 browser fixture 已改用真实 paged database bytes、ClusterData V2 与统一 view packer，但本切片按 implementation-first 节奏只运行 OEngine typecheck/build:test、74 个命中 ABI/binding/specialization/FrameGraph assertions、validation typecheck 与 `git diff --check`，均通过；未运行浏览器、截图、GPU timestamp 或 PERF，旧 L3/L4 artifact 不代表当前 revision。本段只记录 Step 7.1 checkpoint；当前 production 状态见下一条。

- ADR-0013 Step 7.2 为 `implementation-complete; 7.3 deletion-open; runtime/perf-gates-open`（implementation commit `cbdf97281ab3608e35b21f3288b9ce1214f6a103`，tree/content hash `edd11ae60bb6561677616809212a5117a1e80f7b`）：Renderer 只在精确满足 ADR-0013 sparse capability 后创建 owner；`GpuRenderWorld` publication preview 与 `SparseShadingPublicationCoordinator` 在 frame encode 前准备、提交并按 completion boundary 退役 revision，upload failure 回滚已提交 residency，scene release 发布 empty closure。`SurfaceFeature` 已成为 classify/finalize、HDR-only 初始化、active-bin indirect specialized resolve 与按 output dependency 创建 compact Surface 的生产 owner；lit specialization 在同一次 resolve 中消费 production shadow/light/cluster ABI，`LightingFeature` 只保留这些 producer 与背景，旧 `shadow-update` FramePlan stage 已删除。`ShadingBinFrame`/`SpecializedShadingFrame` 取代旧 compute-material FrameProduct，GTAO/SSGI/SSR/Temporal/debug consumer 均先按 depth/visibility validity 判断未定义的 compact background。该 revision 的 OEngine typecheck、build:test 与 9 个命中测试文件共 140/140 assertions 已通过，`git diff --check` 通过；未运行 clean `npm ci`、validation typecheck、shader source audit、真实浏览器、截图、GPU timestamp 或 PERF。Step 6 formal Gate 并未因 implementation-first 推进而豁免；Step 7.3 的旧 MaterialTile/KernelClass owner 删除以及本机 `NVIDIA GeForce RTX 2060 SUPER` 上剩余 lifecycle L4 与 L5 baseline/candidate 仍为 open，因此不得声明 Runtime Validated、Performance Improved、Pipeline Feature Complete 或 ADR Complete。

- WebGPU 目标能力线已升级为 [WebGPU 2026 Desktop](./WEBGPU.md)。当前 Renderer 强制 `core-features-and-limits`、`indirect-first-instance`、`float32-blendable`、`texture-formats-tier1` 与 ADR-0013 所需 `subgroups`，并分别验证 WGSL `texture_formats_tier1`、在 HZB/NSS/Velocity storage Shader 中显式声明 `requires`；`timestamp-query`、`primitive-index` 和一族纹理压缩能力仍为机会性能力，并冻结 adapter/device feature、关键 limit、WGSL/API probe 与 texture specialization record。
- Runtime Package V2 与 Encoded Texture Variant 已冻结确定性 manifest/chunk、exact format/block extent、完整 mip、codec provenance 与 capability selection。GPU-native package 直接进入生产 residency；KTX2 UASTC/ETC1S 由惰性有界 Worker pool 和固定 Khronos KTX-Software v4.4.2 libktx WASM 转为同一 package，再经普通 `GpuRenderWorld → TextureResidency` 进入 Material Resolve、Visibility MASK、Shadow MASK 与 Transparency。Production source graph 不 import reference/self block codec；Cooked runtime mip、runtime recompression、RGBA expansion 和 private submit 为零。
- Texture Residency 使用 exact-format immutable segment 与最多 4 个有界 `TextureBindingSet`，每 set 保留 5 个共享 RGBA8 development size class 和 4 个 Cooked package slot；同一 material 的全部语义必须在发布前 colocate，无法表达时原子失败。生产 Material ABI 已统一为 association-specific 272 B shading record + 四条 texture route，header 携带 `ShadingProgramId`、canonical `TextureBindingSetId` 与 publication generation；主管线已只按 `ShadingProgramId × TextureBindingSetId` specialization 消费该记录，旧 `KernelClassId` payload 已不可达但其源码和测试仍待 Step 7.3 删除。TextureRef V2 与业务 handle 保持 stable slot/generation；证据已区分 exact format、direct/Worker/uncompressed、transfer/upload/transcode/resident/retiring/transaction bytes、set utilization 与 preflight failure。
- ADR-0011 的唯一综合 final PERF 保存在 `OEngine/benchmarks/asset-codec-texture-v3-final.json`：clean implementation commit 的三次独立 1920×1080 浏览器运行实际覆盖 30 张常驻纹理、5 张 direct Cooked BC 纹理和 2 个 `TextureBindingSet`，Cooked runtime mip、preflight failure、overflow、WebGPU diagnostics 均为零。第二 set 相对受控单 set 基线使 GPU frame P50 变化 -0.23%，但 CPU frame P50 增加 33.33%、Material Resolve P50 增加 98.40%、Shadow P50 增加 26.82%；这是被如实接受的 V3 有界基线，不是性能提升结论。
- Geometry 默认生产路径已切到 `static-pbr-compact-v2`：position/normal/tangent/UV/color 使用有界紧凑编码，bounds 保守覆盖 quantization error，Runtime Package manifest/profile/hash 与目录互证；float32 generic 只保留显式 fallback。
- Instance ABI V6 已拆为 64 B static 与 112 B dynamic region，总 stride 为 176 B；static identity 同时保存 geometry slot 与逐资产 generation，支持混合代际资产的 consumer 防陈旧核对。static/transform/material/visibility/lifecycle 分流，transform、material 与 visibility 只上传命中 region/field，CPU shadow 与 patch bytes 由 owner/Profiler 计数。
- Runtime Asset 已有无 scheduler 的 chunk/page seam：stable identity、logical/physical resident range、request state、budget hook、原子 commit/abort、retire 与 device-loss reset；Geometry/Texture upload 已接入且不改变 stable asset/material handle。
- ADR-0008 Step 0 已冻结 Geometry truth counter ABI 并接入生产 GPU 阶段：hierarchy nodes、accepted clusters、selected meshlets、MeshletWork、candidate/risky/exact/raster triangles、padding、visible pixels 与 queue payload bytes 可分别观测；切换前正式综合基线保存在 `OEngine/benchmarks/gpu-driven-geometry-v2-baseline.json`。
- ADR-0008 Step 1–7 已冻结 24 B `GpuMeshletRasterWork`、32 B correctness-critical queue header 与 VisibilityKey V2 的 `24-bit work slot + 8-bit local primitive`；GPU projection-risk classifier 将 normal 与 selective-exact meshlet 互斥排入 32+32 个有界 bucket，固定 64 次 `drawIndirect` 写同一 production `r32uint` key/reverse-Z depth。Material Resolve、MaterialClassDepth 与 Visibility debug 只经 MeshletWork 恢复 material/geometry；主视图不再分配或消费 per-triangle RasterWork/ExactRasterWork，旧 filter、ABI、shader、parity target 和死 counter 已删除，阴影/透明仅保留各自独立的 SecondaryRasterWork。queue overflow/invalid 会清零全部 bucket indirect args，禁止呈现部分几何。独立、默认关闭的 `LargeTriangleSetupCache` 以 `workSlot * 128 + localPrimitive` 建立有界可裁剪 cache，overflow 逐像素 fallback。Geometry cooker 以 one-hot directory flags 写入 Flat/Shallow/Full hint，GPU traversal 逐 geometry 消费；`GeometryWorkBudget` 统一约束 nodes/MeshletWork/raster vertices/risk/setup，fixed 模式确定性运行，adaptive SSE 带 dead zone、慢恢复、quality floor 与 camera-cut reset。Step 2/3/5/6 clean PERF 分别保存在 `OEngine/benchmarks/gpu-driven-geometry-v2-step2.json`、`OEngine/benchmarks/gpu-driven-geometry-v2-step3.json`、`OEngine/benchmarks/gpu-driven-geometry-v2-step5.json` 与 `OEngine/benchmarks/gpu-driven-geometry-v2-step6.json`；Step 7 final PERF 保存在 `OEngine/benchmarks/gpu-driven-geometry-v2-step7.json`：clean implementation commit 上三独立浏览器上下文的 GPU frame P50/P95 为 1.517024/1.7959424 ms，相对 Step 6 P50 降低 29.27%，geometry queue bytes P50 降低 97.94%，overflow/invalid 与 WebGPU diagnostics 均为零。ADR-0008 已完成。
- ADR-0009 Step 0 已冻结 FrameProducts、`ScreenSpaceDiffuseMode`、PreExposure/source stage、receiver-validity GI precedence、SSR baseline replacement、28-class `MaterialTileWork` ABI 与四组 Compute ShadeLighting binding budget。Step 1 的 8×8 portable GPU classifier 生成 per-class queue 和 12 B indirect args。Step 2 已完成 production cutover：唯一 opaque full-material evaluator 由 VisibilityKey V2 恢复 canonical compact vertex，计算 perspective-correct barycentric/显式 gradient，以一个共享 compute pipeline、静态 dynamic-uniform class table和固定 28 次 `dispatchWorkgroupsIndirect` 采样 Material/TextureBindingSet；退化 gradient 明确走 LOD 0 fallback。第二个共享 compute consumer执行 clustered direct lighting/shadow，复用原 direct-lighting WGSL core，不重复材质采样。Material evaluator 写 consumed/exactly-once claim，lighting finalizer验证 valid/shaded、unassigned、duplicate、overflow/generation 并写 `frameInvalid`，Tonemap 以 diagnostic magenta fail-visible。旧 MaterialClassDepth probe/pass、class-discard 和 fullscreen raster material/direct-lighting production owner 已删除。clean PERF 保存在 `OEngine/benchmarks/advanced-frame-pipeline-v2-step2.json`：三个独立 Chrome context 的综合 workload 均为 14,690 records、874,227 valid/shaded，unassigned/duplicate/overflow/frame-invalid/gradient-fallback 全为 0；GPU frame P50/P95 中位数为 1.834/2.184 ms，classifier、compute material indirect、direct-lighting indirect P50 分别为 0.089/1.737/1.180 ms。
- ADR-0009 Step 3 已完成：Surface V1、纯格式 bridge、旧 `SurfaceFrame` 公共兼容类型、`GpuSurfaceAbi` 权威与 26 B/pixel attachments 已删除；GI/AO/SSR/Temporal/debug consumers 直接消费 named compact products。当前 working-set profile 在 velocity-off/on topology 分别为 20/24 B/pixel，MaterialId 由 VisibilityKey/MeshletWork 恢复。Velocity-off shader module 和 bind layout 静态移除 binding/texture store，FrameGraph 不创建资源。主管线/颜色 history 冻结为独立 8 B/pixel `pre-exposed-rgba16float-v1` HDR ABI。IBL/Probe Volume 使用 fused baseline pass，Brick4 在 SSR off 时走 fused path；真实 Chrome `surface.lpv-baseline-pruning` 证明 LPV SSR-off 的 baseline-specular/SSR pass 为 0、transient resource 为 21，SSR-on 恰有一个 baseline-specular、SSR consumer 闭环且 transient resource 为 30，WebGPU diagnostics 为零。clean PERF 保存在 `OEngine/benchmarks/advanced-frame-pipeline-v2-step3.json`：三个独立 Chrome context 的 24 B/pixel 实测 attachment 均为 49,766,400 B，相对 Step 2 迁移态减少 58.62%；Surface ABI 顶层 gate 为 `required` 且 conversion pass、unassigned/duplicate/overflow/frame-invalid 均为 0；GPU frame P50/P95 中位数为 1.623/1.959 ms，相对 Step 2 分别降低 11.49%/10.29%。
- ADR-0009 Step 4 的 Three.js r186-derived GTAO production replacement已落地并重新通过corrected MILESTONE，PERF/运动视觉仍open：唯一 `mode=gtao` owner保留pinned `interleavedGradientNoise + rand`、magic-square/temporal rotation、quadratic horizon integration、physical radius/thickness/falloff，并从同一trace输出AO与bent normal。原分辨率视觉复核发现shared HZB保守footprint把薄遮挡物扩张成跨屏暗带，当前raw trace已删除HZB binding并固定读取主depth mip0精确texel；1600×900 contact矩阵确认宽暗带消失、局部接触遮蔽和间隙可辨。当前revision的Chrome `surface.gtao-replacement` 11/11通过，230,400 pixels、36 samples/pixel、四阶段、feature-off、one-submit均闭合且WebGPU diagnostics为0；旧Step 4 PERF只作历史证据。
- ADR-0009 Step 5 已完成SSGI与receiver-local Hybrid GI production cutover并通过MILESTONE：公开模式只有`off|gtao|ssgi`，Three.js r186-derived SSGI拥有near-field diffuse GI、screen AO、bent/confidence和四纹理history；唯一long-range producer逐receiver按`Brick4 > Probe > IBL > black`早返回。Chrome验证进一步修复了Brick4 storage binding最小48 B、逐帧SSGI counter注册和reverse-Z背景receiver污染；`surface.ssgi-production` 11/11通过，230,400 evaluated、26,137 accepted、204,263 rejected，104,570有效receiver全部唯一分配，stale Brick4 generation回退IBL，diagnostics零错误。world-radius、bent/temporal长序列与性能仍由扩展视觉/final PERF关闭。
- ADR-0009 Step 6 的Three.js r186-derived SSR production replacement已落地并重新通过corrected MILESTONE，PERF/运动视觉仍open：production链仍为bounded GGX VNDF/HZB trace→receiver-resolved hit shading→双候选TemporalReproject→8-tap Vogel RecurrentDenoise→conditional upscale→baseline correction；near-delta roughness `<=0.04`使用同shader内的deterministic mirror specialization。视觉复核暴露Three inverse-age alpha被OEngine replacement-confidence错误替代，当前已分离temporal history rate/recurrent history aggressivity/output confidence，并加入3×3 hit-evidence miss-history gate、`1..8` half-res quadratic Vogel footprint、invalid-center零权重、miss不反馈与confidence-aware upscale；默认recurrent history aggressivity修正为0.81，PBR debug uint binding和共享Filament specular AO也已修正。1600×900 road fixture的50-frame矩阵确认near-mirror连续、roughness 0.38反射只保留低幅细颗粒；当前revision Chrome `surface.ssr-replacement` 11/11通过，127,029 trace pixels、26,252 hits、2,401,880 steps、max 128，feature-off/temporal-off/one-submit均闭合且WebGPU diagnostics为0。camera-motion/exposure矩阵和统一clean final PERF仍open。
- ADR-0009 Step 7–8 已完成shared products/history与Temporal Reconstruction/DRS production重构并通过MILESTONE：一个`SharedColorPyramidPass`分别拥有不alias的Opaque/Final语义产品，统一history registry只在成功submit推进；TAAU使用9-bilinear-tap bounded reconstruction，fixed/adaptive DRS和internal→output合同集中在`RenderSettings`。Chrome `surface.shared-derived-products` 14/14与`surface.temporal-reconstruction` 11/11通过，含consumer pruning、camera/resize/mode invalidation、fixed 0.75、透明reactive双classifier、无透明单classifier复用与adaptive policy。真实浏览器发现并修复classification evidence必须读取最终`graphTopology`；扩展temporal visual与final PERF仍开放。
- ADR-0009 Step 9 已完成Final Output post fusion并通过当前SDR宿主MILESTONE：normal frame在唯一swapchain pass融合Bloom composite、Color Grading、Sharpen与display mapping，feature specialization物理裁剪bindings/loads；one-shot capture是有意的materialization例外。Chrome `surface.post-fusion` 12/12通过，normal frame零live full-resolution HDR intermediates，Exposure/debug/last-consumer裁剪、capture后恢复fusion、one-submit与diagnostics零错误成立。异步capture oracle现冻结准确capture帧证据；HDR显示宿主与final PERF仍开放。
- ADR-0009 Step 4–10 的旧 MILESTONE 汇总保留在对应提交和 ADR 中；它发生在示例库重置前，只证明当时 revision。上一 revision 的 `comprehensive-full` pipeline matrix 同样不能替代当前或后续 revision 的 clean formal PERF。
- ADR-0009 整体仍等待其新宿主 cases 接入后完成唯一综合 formal run、扩展 temporal/SSGI/SSR 视觉矩阵及 HDR 宿主检查；在 final PERF 通过前，normal graph 已无 consumer 的 `SharpenPass.ts`/`sharpen.ts`仍保留为明确 deletion target，不能提前把 ADR 标成 complete。

这些结构事实不等于 1080p/60 FPS、完整画质、内存上限或 feature-off Gate 已通过。

## 当前生产 Owner

- 总装：公开 `Renderer.ts` shell、唯一 `render/pipeline/MainRenderPipeline.ts` recipe owner 与 `render/features/*`。
- GPU 资产/场景：`GpuAssetStore`、`GpuScene`、`GpuRenderWorld`。
- 工作/可见性：`GpuWorkGenerationAbi`、`GpuVisibilityKeyAbi`、统一 visibility owners。
- Surface/HDR：`SparseShadingPublicationCoordinator`、`SparseShadingGpuRevisionOwner`、`ShadingBinFrame`、`SpecializedShadingFrame`、`GpuSparseShadingFrameAbi`、`GpuHdrAbi` 与 `SurfaceFeature`；旧 MaterialTile/KernelClass owner 只剩 Step 7.3 删除目标，不再属于生产主链。
- 效果：Lighting Feature，Render-owned Shadow Feature，AO/Reflection/GI Service，Transparency/Temporal/Post Feature。
- 证据 owner：`FrameProfiler`、GPU counters、resource accounting、shader source audit、已提交的冻结 benchmark，以及 ADR-0014 的独立 `validation/` protocol/registry/runner。宿主通用 smoke、ADR-0013 Step 4 `shading-bin-component`、Step 5 `shading-resolve-component` 与 Step 6 `sparse-shading-candidate/correctness-v6` 已有各自范围内的 clean browser evidence；Step 6 其余 lifecycle、Renderer cutover 与 PERF case 仍按 registry 逐项补齐。

## 开放 Gate 与风险

### Visibility 与 Surface

- Meshlet bucket Hardware Visibility 是主视图唯一 VisibilityKey V2 raster consumer；targeted basic、near/far、selective-risk 和 optional setup Case 已通过，queue closure、最终 visible pixel 与 WebGPU diagnostics 为零错误。旧 exact parity seam 已删除；若后续修改 raster ordering，必须用截图/数值回归重新建立正确性证据。
- MaterialClassDepth 与 class-discard 已退出 production 并删除；后续正确性以 compute evaluator 的浏览器 Surface fixture、exactly-once counter 和截图/数值回归为准，不恢复长期双 backend。
- TriangleSetup candidate cache 保持显式 opt-in；正确性、near-plane、off/on GPU 时间和内存组合证据不足以改变默认值。
- Surface ABI 只有一套生产合同；Step 3 的完整 consumer coverage、targeted readback、实际 attachment、综合 transient accounting 与三次独立运行 gate 已通过。当前证据只覆盖 NVIDIA Turing，第二 GPU vendor 仍缺失。
- Sparse Shading Bin 已形成 GPU Visibility producer → classifier/finalizer → active-bin indirect specialized material/fused direct-lighting consumer 闭环，full material evaluation 只发生一次；compact `SurfaceLite`/`Velocity` 只按 dependency 创建且背景未定义，所有下游 consumer 在读取前检查 depth/visibility validity。旧 MaterialTileWork/compute evaluator/第二轮 direct-lighting 源码尚待 Step 7.3 物理删除，当前 revision 也尚无 production-cutover Chrome topology、截图、GPU diagnostics 或 PERF 证据。
- ADR-0013 Step 1–7.2 的 DEV 实现已存在，唯一 `MainRenderPipeline` 已消费资产/instance identity、`GpuRenderWorld.ActiveShadingSummary`、association-specific shading material/texture-route publication、Visibility 双 MRT、production revision closure、`ShadingBinFrame` 与 `SpecializedShadingFrame`；live Renderer 将 `subgroups` 作为该主管线的 required capability。当前只完成静态/DEV 接线验证：旧 28-class/MaterialTile/KernelClass owner 的源码、shader、计数器、测试和文档删除仍是 Step 7.3；material/association patch、TextureBindingSet relocation、feature toggle、scene replace、camera cut、aborted submit、device-loss recovery 与本机 L5 formal PERF 仍未关闭。尤其 coordinator 目前绑定创建时 device，完整 device-loss/recovery 必须由剩余 lifecycle Gate 证明。上述开放项关闭前不得声明 Runtime Validated、Performance Improved、Pipeline Feature Complete 或 ADR Complete。

### 生命周期

- 旧对象场景 GPU runtime、双 ID visibility attachment、Material Expand、独立 Velocity、旧 OIT/Shadow/LPV update 路径及其无消费者 shader 已静态删除；生产图没有兼容分支。
- 普通 Scene 的当前产品 adapter 不支持 `SkinnedMesh`；完整蒙皮/动画仍为 Deferred，并显式报 unsupported。
- Shadow 的 device loss、resize、scene replace、feature toggle 与 camera cut 已有浏览器证据；其他 Feature 和提交失败后的 history/resource invalidation 仍需继续补齐。

### 性能、内存与来源

- 1920×1080、DPR 1、完整目标画质下 16.667 ms GPU 尚未在目标设备形成可复现发布基线。
- resident、transient、history、shadow、upload/readback 预算仍需目标 adapter 的同条件证据。
- one-main-submit 和 feature-off 接近零成本需要逐帧证据，不能只凭静态结构判断。
- Shader source audit 当前只有有生产 owner 的 authored shader；实际数量和名单以生成的 `OEngine/benchmarks/shader-source-audit.json` 为准。
- `shader-f16`、Immediate Data 与 Transient Attachment 尚无生产 consumer；lockfile 中的 `@webgpu/types` 0.1.71 还没有 2026-09 规范中的 `texture-compression-unaligned` 名称，因此 BC base dimensions 仍要求 block alignment；已对齐 base 的物理 mip subresource 仍离线保留并上传到 1×1 tail。
- TextureBindingSet 已完成最多 4 set 的生产接入与跨 consumer 浏览器验证；当前固定上限不是 streaming/eviction policy。通用 streaming、partial residency、Virtual Texturing 和内容数据库仍是明确非目标/后续架构，不得借 V3 名义隐式加入。
- `TextureBindingSet` 当前按 active set 固定执行 Material Resolve 与 MASK consumer；两 set 综合证据已显示 CPU、Material Resolve 与 Shadow 的可测增量。后续优化必须保留 GPU producer → consumer 闭环、固定有界调度和零 CPU 可见材质回读，并以同一 `comprehensive-full` workload 证明收益。

## 下一步

1. [ADR-0014](./adr/0014-browser-validation-and-performance-host.md)：在已落地的独立 `validation/` package 上补齐 freshness/error/dispose 故障矩阵、ADR-0013 Step 6 candidate workloads、生命周期矩阵与 formal PERF orchestration；不得把验证重新并入 `examples/`。
2. [ADR-0013](./adr/0013-sparse-shading-bin-pipeline.md)：Step 4 producer 与 Step 5 consumer L3 已关闭，Steps 1–7.2 的 production identity/publication、Visibility 双 MRT、revision closure、native light/cluster/shadow ABI 及 `SurfaceFeature`/`LightingFeature`/`MainRenderPipeline` 原子 composition 已落地。暂停后的精确续点是 Step 7.3：按 migration manifest 删除旧 MaterialTile/KernelClass/28-dispatch material 与第二轮 direct-lighting owner，并证明 public/source/shader/counter/test/doc 零命中；随后才进入 Step 8，在当前指定 `NVIDIA GeForce RTX 2060 SUPER` 上集中补齐剩余 lifecycle MILESTONE、production-cutover browser evidence、shader audit、clean full suite 与同条件 L5 baseline/candidate。所有 gate 关闭前保持 ADR 为 implementation open；其他 adapter 只作非阻塞补充，不保留兼容 backend。
3. [ADR-0010](./adr/0010-webgpu-2026-capability-contract.md)：将 ADR-0013 的 required `subgroups`、limits 与 capability record 接入真实初始化；`primitive-index` 的 production consumer 与 portable parity case 已落地；其他能力没有 consumer 前保持 record-only，运行 Gate 等新宿主恢复。
4. [ADR-0009](./adr/0009-compute-shading-and-advanced-frame-pipeline-v2.md)：未被 ADR-0013 替代的 GI/AO/SSR/Temporal/Post 实现保持 verification-open；新宿主恢复后统一完成 topology/history、camera-motion/exposure/HDR 视觉检查和唯一综合 clean PERF，所有 open Exit 关闭前不得把 ADR 整体记为完成。
5. Texture V3 follow-up：针对 active-set 固定编排的 CPU、Material Resolve 与 Shadow 成本做不改变 ABI/正确性合同的优化；新综合 workload 落地后才能用 clean commit A/B 改写当前冻结基线。
