# OEngine 当前状态

更新时间：2026-09-12。本文件只记录当前能力、开放风险和下一步；实施过程与旧结果从 Git 查询。

## 当前基线

- WebGPU Renderer、FrameGraph、FramePlan、Feature/Service 组合和公开入口已经存在。
- GPU-ready geometry package、`GpuAssetStore`、`GpuScene` 与 `GpuRenderWorld` 已形成资源边界。
- Packed source 与普通 Scene adapter 已汇入同一 GPU Render World；未注册 Scene 直接失败。
- Packed hierarchy/work generation、Hardware Visibility、直接 `VisibilityKey`、MaterialTileWork compute evaluation 和 compact SurfaceLite 已有生产 owner。
- 旧 Pixel Queue、ShadeWork 和可见像素 scan/scatter 生产链已退出生产路径。
- direct lighting、CSM、GI、AO、SSR、MBOIT、Temporal 与 HDR post 接入同一 Renderer 主流程。
- Performance Inspector 是共享的实时 Profiler/Timeline；Rendering Lab 是综合质量与性能 fixture。
- Browser Validation 已统一为 Registry + Source Domain Selector + 单一 ChromeRunner；Smoke、Visibility、Surface、Lifecycle 承担日常真实 WebGPU 验证，Rendering Lab 保留综合与 formal benchmark。公共证据强度统一为 DEV/MILESTONE/PERF，30+60 的 `profile:rendering-lab:dev` 只承担短 A/B 与编排检查。
- WebGPU 目标能力线已升级为 [WebGPU 2026 Desktop](./WEBGPU.md)。当前代码强制 `core-features-and-limits`、`indirect-first-instance`、`float32-blendable`、`texture-formats-tier1`，并分别验证 WGSL `texture_formats_tier1`、在 HZB/NSS/Velocity storage Shader 中显式声明 `requires`；机会性启用 `timestamp-query`、`subgroups`、`primitive-index` 和一族纹理压缩能力，并冻结 adapter/device feature、关键 limit、WGSL/API probe 与 texture specialization record。
- Runtime Package V2 与 Encoded Texture Variant 已冻结确定性 manifest/chunk、exact format/block extent、完整 mip、codec provenance 与 capability selection。GPU-native package 直接进入生产 residency；KTX2 UASTC/ETC1S 由惰性有界 Worker pool 和固定 Khronos KTX-Software v4.4.2 libktx WASM 转为同一 package，再经普通 `GpuRenderWorld → TextureResidency` 进入 Material Resolve、Visibility MASK、Shadow MASK 与 Transparency。Production source graph 不 import reference/self block codec；Cooked runtime mip、runtime recompression、RGBA expansion 和 private submit 为零。
- Texture Residency 使用 exact-format immutable segment 与最多 4 个有界 `TextureBindingSet`，每 set 保留 5 个共享 RGBA8 development size class 和 4 个 Cooked package slot；同一 material 的全部语义必须在发布前 colocate，无法表达时原子失败。Material ABI 携带 `TextureBindingSetId`，GPU consumer 按 `KernelClassId × TextureBindingSetId` 固定编排，不回读可见材质。TextureRef V2 与业务 handle 保持 stable slot/generation；证据已区分 exact format、direct/Worker/uncompressed、transfer/upload/transcode/resident/retiring/transaction bytes、set utilization 与 preflight failure。
- ADR-0011 的唯一综合 final PERF 保存在 `OEngine/benchmarks/asset-codec-texture-v3-final.json`：clean implementation commit 的三次独立 1920×1080 浏览器运行实际覆盖 30 张常驻纹理、5 张 direct Cooked BC 纹理和 2 个 `TextureBindingSet`，Cooked runtime mip、preflight failure、overflow、WebGPU diagnostics 均为零。第二 set 相对受控单 set 基线使 GPU frame P50 变化 -0.23%，但 CPU frame P50 增加 33.33%、Material Resolve P50 增加 98.40%、Shadow P50 增加 26.82%；这是被如实接受的 V3 有界基线，不是性能提升结论。
- Geometry 默认生产路径已切到 `static-pbr-compact-v2`：position/normal/tangent/UV/color 使用有界紧凑编码，bounds 保守覆盖 quantization error，Runtime Package manifest/profile/hash 与目录互证；float32 generic 只保留显式 fallback。
- Instance ABI 已拆为 64 B static 与 112 B dynamic region，总 stride 为 176 B；static/transform/material/visibility/lifecycle 分流，transform、material 与 visibility 只上传命中 region/field，CPU shadow 与 patch bytes 由 owner/Profiler 计数。
- Runtime Asset 已有无 scheduler 的 chunk/page seam：stable identity、logical/physical resident range、request state、budget hook、原子 commit/abort、retire 与 device-loss reset；Geometry/Texture upload 已接入且不改变 stable asset/material handle。
- ADR-0008 Step 0 已冻结 Geometry truth counter ABI 并接入生产 GPU 阶段：hierarchy nodes、accepted clusters、selected meshlets、MeshletWork、candidate/risky/exact/raster triangles、padding、visible pixels 与 queue payload bytes 可分别观测；切换前正式综合基线保存在 `OEngine/benchmarks/gpu-driven-geometry-v2-baseline.json`。
- ADR-0008 Step 1–7 已冻结 24 B `GpuMeshletRasterWork`、32 B correctness-critical queue header 与 VisibilityKey V2 的 `24-bit work slot + 8-bit local primitive`；GPU projection-risk classifier 将 normal 与 selective-exact meshlet 互斥排入 32+32 个有界 bucket，固定 64 次 `drawIndirect` 写同一 production `r32uint` key/reverse-Z depth。Material Resolve、MaterialClassDepth 与 Visibility debug 只经 MeshletWork 恢复 material/geometry；主视图不再分配或消费 per-triangle RasterWork/ExactRasterWork，旧 filter、ABI、shader、parity target 和死 counter 已删除，阴影/透明仅保留各自独立的 SecondaryRasterWork。queue overflow/invalid 会清零全部 bucket indirect args，禁止呈现部分几何。独立、默认关闭的 `LargeTriangleSetupCache` 以 `workSlot * 128 + localPrimitive` 建立有界可裁剪 cache，overflow 逐像素 fallback。Geometry cooker 以 one-hot directory flags 写入 Flat/Shallow/Full hint，GPU traversal 逐 geometry 消费；`GeometryWorkBudget` 统一约束 nodes/MeshletWork/raster vertices/risk/setup，fixed 模式确定性运行，adaptive SSE 带 dead zone、慢恢复、quality floor 与 camera-cut reset。Step 2/3/5/6 clean PERF 分别保存在 `OEngine/benchmarks/gpu-driven-geometry-v2-step2.json`、`OEngine/benchmarks/gpu-driven-geometry-v2-step3.json`、`OEngine/benchmarks/gpu-driven-geometry-v2-step5.json` 与 `OEngine/benchmarks/gpu-driven-geometry-v2-step6.json`；Step 7 final PERF 保存在 `OEngine/benchmarks/gpu-driven-geometry-v2-step7.json`：clean implementation commit 上三独立浏览器上下文的 GPU frame P50/P95 为 1.517024/1.7959424 ms，相对 Step 6 P50 降低 29.27%，geometry queue bytes P50 降低 97.94%，overflow/invalid 与 WebGPU diagnostics 均为零。ADR-0008 已完成。
- ADR-0009 Step 0 已冻结 FrameProducts、`ScreenSpaceDiffuseMode`、PreExposure/source stage、receiver-validity GI precedence、SSR baseline replacement、28-class `MaterialTileWork` ABI 与四组 Compute ShadeLighting binding budget。Step 1 的 8×8 portable GPU classifier 生成 per-class queue 和 12 B indirect args。Step 2 已完成 production cutover：唯一 opaque full-material evaluator 由 VisibilityKey V2 恢复 canonical compact vertex，计算 perspective-correct barycentric/显式 gradient，以一个共享 compute pipeline、静态 dynamic-uniform class table和固定 28 次 `dispatchWorkgroupsIndirect` 采样 Material/TextureBindingSet；退化 gradient 明确走 LOD 0 fallback。第二个共享 compute consumer执行 clustered direct lighting/shadow，复用原 direct-lighting WGSL core，不重复材质采样。Material evaluator 写 consumed/exactly-once claim，lighting finalizer验证 valid/shaded、unassigned、duplicate、overflow/generation 并写 `frameInvalid`，Tonemap 以 diagnostic magenta fail-visible。旧 MaterialClassDepth probe/pass、class-discard 和 fullscreen raster material/direct-lighting production owner 已删除。clean PERF 保存在 `OEngine/benchmarks/advanced-frame-pipeline-v2-step2.json`：三个独立 Chrome context 的综合 workload 均为 14,690 records、874,227 valid/shaded，unassigned/duplicate/overflow/frame-invalid/gradient-fallback 全为 0；GPU frame P50/P95 中位数为 1.834/2.184 ms，classifier、compute material indirect、direct-lighting indirect P50 分别为 0.089/1.737/1.180 ms。
- ADR-0009 Step 3 已完成：Surface V1、纯格式 bridge、旧 `SurfaceFrame` 公共兼容类型、`GpuSurfaceAbi` 权威与 26 B/pixel attachments 已删除；GI/AO/SSR/Temporal/debug consumers 直接消费 named compact products。当前 working-set profile 在 velocity-off/on topology 分别为 20/24 B/pixel，MaterialId 由 VisibilityKey/MeshletWork 恢复。Velocity-off shader module 和 bind layout 静态移除 binding/texture store，FrameGraph 不创建资源。主管线/颜色 history 冻结为独立 8 B/pixel `pre-exposed-rgba16float-v1` HDR ABI。IBL/Probe Volume 使用 fused baseline pass，Brick4 在 SSR off 时走 fused path；真实 Chrome `surface.lpv-baseline-pruning` 证明 LPV SSR-off 的 baseline-specular/SSR pass 为 0、transient resource 为 21，SSR-on 恰有一个 baseline-specular、SSR consumer 闭环且 transient resource 为 30，WebGPU diagnostics 为零。clean PERF 保存在 `OEngine/benchmarks/advanced-frame-pipeline-v2-step3.json`：三个独立 Chrome context 的 24 B/pixel 实测 attachment 均为 49,766,400 B，相对 Step 2 迁移态减少 58.62%；Surface ABI 顶层 gate 为 `required` 且 conversion pass、unassigned/duplicate/overflow/frame-invalid 均为 0；GPU frame P50/P95 中位数为 1.623/1.959 ms，相对 Step 2 分别降低 11.49%/10.29%。
- ADR-0009 Step 4 上游一致性修正已落地、验证重新打开：`mode=gtao` 的唯一 production AO 是 pinned three.js r186 `GTAONode` local WGSL port，保留 3/5 direction、quadratic stepping、magic-square、精确 Three.js `interleavedGradientNoise + rand` temporal jitter、view-space thickness、squared falloff 与 horizon integral；同一 trace 扩展 world-space bent normal，不存在第二个旧 AO 或 Three.js TRAA owner。AO/second moment/oct bent normal 以 `rgba16float` 共同做 spatial/temporal filter，最终 joint resolve 拆成 `r8unorm + rg16uint`。linear-depth、raw trace与joint resolve绑定的主管线`depth32float`已统一修正为`texture_depth_2d + sampleType: depth`，仅自产`r32float` view-depth继续按普通color texture读取。修正前真实 Chrome `surface.gtao-replacement` 与 `advanced-frame-pipeline-v2-step4.json` 曾证明单 trace、两张 history、one-submit、feature-off裁剪及 GPU P50/P95 1.962/2.284 ms；但当前 shader 已把本地 hash 换成 pinned Three.js `rand`，旧 artifact 只能作历史对照。按“不跑代码测试”约束，类型/WGSL、Chrome oracle、视觉与 `comprehensive-full` PERF 均未重跑，所以当前 Step 4 是 implementation-landed/verification-open，不能继续写“完成”。
- ADR-0009 Step 5 实现已落地但验证尚未关闭：公开配置使用单值 `screenSpaceDiffuseMode = off | gtao | ssgi`；pinned three.js r186 SSGI、AO/GI/bent/confidence joint resolve、四纹理 history、pre-SSGI source 与能量正确 resolve 已接入。上游一致性复核补齐双radius、Three.js rand/IGN、temporal-off direction/offset=1与出屏`break`。SSGI temporal现正确解释`SurfaceValidity.r=reactive, g=motionValid`，仅`motionValid && !reactive`并乘disocclusion后接受history；修复了稳定表面无history/reactive错误积累。bent normal在spatial、temporal、full-resolution resolve均改为oct-decode后方向加权/同半球/normalize/encode，不再直接插值oct坐标；方向ALU复用原AO fetch。full-resolution resolve同时开始真实消费已绑定normal texture，以center+4 tap normal weight补齐此前名义存在、实际缺失的normal edge stop，新增5次load等待PERF。单个`LongRangeDiffuseProviderPass`逐receiver早返回选择`Brick4 > Probe > IBL > black`，Brick4 generation原子驻留，旧scene-wide GI owner已删除；SSR-only物化receiver-resolved baseline，SSGI才写diffuse component。按“不跑代码测试”约束，类型/构建、真实Chrome双radius/Brick4/history oracle、feature matrix、bent/temporal visual与唯一`comprehensive-full` PERF均未执行，所以Step 5仍是implementation-landed/verification-open。
- ADR-0009 Step 6 实现已落地但验证尚未关闭：生产 `ReflectionService` 已切换到pinned three.js r186-derived `bounded GGX VNDF/HZB trace → receiver-resolved hit shading → TemporalReproject → 8-tap Vogel RecurrentDenoise → conditional upscale → OEngine replacement`。Trace/resolve复演同一GGX sample。Temporal保留receiver/reflected-hit双4-tap candidate；4-tap max/min confidence、3×3 ray stddev/hit probability、normal curvature共同恢复reflection-edge hit trust，HDR variance clip和Jacobian stretch按pinned结构执行。receiver/hit各自的disocclusion、motion-valid和reactive只拒绝对应history，raw current不再被上一帧重投影状态清零；新显露SSR可进入recurrent current分支。Recurrent恢复viewport mirror、5-tap ray statistic、frustum-normalized hit distance、GGX lobe falloff、pinned edge scale与`adapt=0.5` feedback，temporal/raw accumulator独立。新增读取不增加pass/attachment但等待PERF验收。packed diagnostics保留，raw alpha保存dominant ray length，history/final alpha保存OEngine综合confidence而非Three inverse frame count；frame-age/firefly/environment sentinel差异已登记。history由recurrent输出并受pre-exposure约束；temporal关闭时不分配history。旧48-neighbor resolve、pre-temporal blur、environment/LPV fallback与二次BRDF/AO correction已删除，最终执行 `HDR + confidence × (SSRSpecular - BaselineSpecular)`。canonical Chrome oracle及静态oracle已登记但未运行。按“不跑代码测试”约束，WGSL compile、oracles、真实Chrome visual、feature matrix和PERF未执行，所以Step 6仍为implementation-landed/verification-open。
- ADR-0009 Step 6 主深度 ABI 修正已落地但未验证：SSR trace、hit shading、TemporalReproject、RecurrentDenoise 与 conditional joint upscale 现在都以 depth-only view、WGSL `texture_depth_2d` 和 BGL `sampleType: depth` 消费主管线 `depth32float`，不再把 depth attachment 冒充普通 `unfilterable-float` color texture；shared `rg16float` HZB 仍保持独立 color binding。对应静态 oracle 已登记但按“不跑代码测试”约束未执行，Step 6 状态仍为 implementation-landed/verification-open。
- ADR-0009 Step 7 实现已落地但验证尚未关闭：`SharedColorPyramidPass` 成为唯一颜色 reduction owner，并分别生成最多 5 mip 的 post-screen-space-diffuse `OpaqueColorPyramid` 与最多 6 mip 的 post-transparency/temporal `FinalColorPyramid`，两个 source stage 不 alias。SSR 已删除私有 `ssr_prefilter` shader/pipeline/texture，直接消费共享 opaque pyramid；Bloom 删除独立 downsample chain，只保留从 Final mip1–5 提取和重建的专用结果；Automatic Exposure histogram 从 Final 最低 mip 采样，不再 full-resolution 扫描。SSGI 尚无 radiance-mip benchmark，因此继续读取 full-resolution pre-SSGI source，`ScreenSpaceDiffuseSourcePyramid` 明确不创建。统一 `TemporalHistoryRegistry` 现在声明 color/GTAO/SSGI/SSR/NSS-feedback/Exposure 六种 semantic/domain/format/count/pre-exposure policy，集中处理 submission-aware ping-pong、camera/resize/scale/feature/format/light/scene/view/representation/exposure/abort 失效；TAA/SSGI/SSR 兼容 history 按 exposure ratio 重标定，NSS feedback 与 Exposure 不再按 frame parity自行推进。`surface.shared-derived-products` 浏览器 oracle、pure contract tests、source-domain routing 和 `sharedPyramid.*` profiler counters 已登记但未运行。按“不跑代码测试”约束，类型/构建、WGSL、browser/MILESTONE、feature topology、visual 与 PERF 全部未执行，Step 7 只能记为 implementation-landed/verification-open，不代表 Exit 通过。
- ADR-0009 Step 8 实现已落地但验证尚未关闭：新增 typed `TemporalReconstructionFrame`，冻结 `internal-full → output-full`、post-transparency/temporal、PreExposure、history/representation revision 与 TAA alpha-lock/NSS feedback confidence语义；主管线在唯一 TAA/NSS输出处构造该产品。TAAU upscale将16-fetch cubic footprint合并为9个bilinear tap，history在采样前拒绝 invalid motion/reactive/disocclusion/out-of-bounds/incompatible exposure，再执行YCoCg clip、relative-luminance damping、motion/reactive/confidence-gated lock。TAA与NSS preprocess的closest-depth binding均已从错误的`texture_2d<f32> + unfilterable-float`修正为与主管线`depth32float`匹配的`texture_depth_2d + depth`，depth load直接返回reverse-Z标量且不再非法取`.r`。Temporal之后的Motion Blur现按internal extent生成tile/neighbor velocity，在output resolve中显式映射每个pixel/tap到internal depth/velocity、把velocity缩放到output-pixel单位，并修正reverse-Z前后景判断；不再用output坐标越界读取internal纹理。GPU rejection evidence现与同一RenderSettings阈值late-bind，CPU oracle同步公式。`RenderSettings.resolution`成为唯一 fixed/adaptive策略：formal/default/manual scale固定模式不消费timing，adaptive要求Temporal开启并限定`[0.67,0.75,0.8,0.9,1]` bucket、delayed GPU timestamp、warm-up/dead band/probe/lockout；旧公开mutable DRS side channel已删除。`surface.temporal-reconstruction`、pure contract/oracle、source routing与新增profiler evidence已登记但未运行。按“不跑代码测试”约束，类型/构建、WGSL、browser/MILESTONE、static/motion/MASK/transparency/SSR/cut/resize visual与PERF均未执行，Step 8只能记为implementation-landed/verification-open。

- ADR-0009 Step 8 shared disocclusion 深度 ABI 修正已落地但未验证：`OcclusionConfidencePass` 的当前/上一帧输入实际都是主管线 `depth32float`，现已改为 depth-only view、WGSL `texture_depth_2d` 与 BGL `sampleType: depth`；velocity 仍是独立普通 float texture。4-tap previous-depth 与3×3 current closest-depth helper直接消费 reverse-Z标量，不再使用非法颜色纹理声明/`.r`。其 `r8unorm` confidence 输出也从未生成/消费的完整 mip chain 收缩为单 mip，删除了理论上约等于 mip0 1/3 的额外 allocation；实际 bytes/GPU 收益未测。对应静态 oracle 已登记但按“不跑代码测试”约束未执行，Step 8状态不变。
- ADR-0009 Step 9 实现已落地但验证尚未关闭：`TonemapPass`是唯一`Final Output SDR/HDR` owner，normal frame在swapchain pass中融合Bloom composite、linear HDR Color Grading与可选Sharpen，再执行Exposure/display mapping；静态variant物理裁剪未启用的Bloom binding/sampler、Sharpen邻域load和effects uniform。Bloom只保留shared Final pyramid上的multi-resolution reconstruction，normal frame不物化composited HDR；其mip0从Final mip1开始，FrameGraph现以真实`output-half`标记该中间产品，只有capture exception生成的composited texture是`output-full`。`post-color-grading` one-shot capture是唯一物化例外；debug固定观察pre-post-effects HDR且无消费者Bloom可裁剪。按“不跑代码测试”约束，WGSL compile、oracles、SDR/HDR/debug/capture visual和PERF均未执行，所以Step 9仍为implementation-landed/verification-open。

这些结构事实不等于 1080p/60 FPS、完整画质、内存上限或 feature-off Gate 已通过。

## 当前生产 Owner

- 总装：公开 `Renderer.ts` shell、唯一 `render/pipeline/MainRenderPipeline.ts` recipe owner 与 `render/features/*`。
- GPU 资产/场景：`GpuAssetStore`、`GpuScene`、`GpuRenderWorld`。
- 工作/可见性：`GpuWorkGenerationAbi`、`GpuVisibilityKeyAbi`、统一 visibility owners。
- Surface/HDR：`MaterialTileWork`、compute material evaluator、`GpuComputeMaterialAbi`、`GpuHdrAbi` 与 `SurfaceFeature`。
- 效果：Lighting Feature，Render-owned Shadow Feature，AO/Reflection/GI Service，Transparency/Temporal/Post Feature。
- 证据：`FrameProfiler`、GPU counters、resource accounting、shader source audit 和 Rendering Lab diagnostics。

## 开放 Gate 与风险

### Visibility 与 Surface

- Meshlet bucket Hardware Visibility 是主视图唯一 VisibilityKey V2 raster consumer；targeted basic、near/far、selective-risk 和 optional setup Case 已通过，queue closure、最终 visible pixel 与 WebGPU diagnostics 为零错误。旧 exact parity seam 已删除；若后续修改 raster ordering，必须用截图/数值回归重新建立正确性证据。
- MaterialClassDepth 与 class-discard 已退出 production 并删除；后续正确性以 compute evaluator 的浏览器 Surface fixture、exactly-once counter 和截图/数值回归为准，不恢复长期双 backend。
- TriangleSetup candidate cache 保持显式 opt-in；正确性、near-plane、off/on GPU 时间和内存组合证据不足以改变默认值。
- Surface ABI 只有一套生产合同；Step 3 的完整 consumer coverage、targeted readback、实际 attachment、综合 transient accounting 与三次独立运行 gate 已通过。当前证据只覆盖 NVIDIA Turing，第二 GPU vendor 仍缺失。
- MaterialTileWork 已形成 GPU classifier → compute material evaluator → compute direct-lighting consumer 闭环，full material evaluation 只发生一次；生产 consumer、公开接口与 ABI 权威已没有 Surface V1 依赖。Step 5 让 `DiffuseSurfaceLite`、pre-SSGI source 与 component MRT 只在 `mode=ssgi` 被请求，把 receiver-level GI precedence 收敛到一个早返回 producer，并以 monolithic Brick4 V1 package/generation publication 关闭当前 residency 语义；legacy plumbing 删除和 Chrome topology/perf 证据仍缺。第二 GPU vendor 证据仍缺失。

### 生命周期

- 旧对象场景 GPU runtime、双 ID visibility attachment、Material Expand、独立 Velocity、旧 OIT/Shadow/LPV update 路径及其无消费者 shader 已静态删除；生产图没有兼容分支。
- 普通 Scene 的当前产品 adapter 不支持 `SkinnedMesh`；完整蒙皮/动画仍为 Deferred，并显式报 unsupported。
- Shadow 的 device loss、resize、scene replace、feature toggle 与 camera cut 已有浏览器证据；其他 Feature 和提交失败后的 history/resource invalidation 仍需继续补齐。

### 性能、内存与来源

- 1920×1080、DPR 1、完整目标画质下 16.667 ms GPU 尚未在目标设备形成可复现发布基线。
- resident、transient、history、shadow、upload/readback 预算仍需目标 adapter 的同条件证据。
- one-main-submit 和 feature-off 接近零成本需要逐帧证据，不能只凭静态结构判断。
- Shader source audit 当前只有有生产 owner 的 authored shader；实际数量和名单以生成的 `OEngine/benchmarks/shader-source-audit.json` 为准。
- `shader-f16`、Immediate Data 与 Transient Attachment 尚无生产 consumer；lockfile 中的 `@webgpu/types` 0.1.71 还没有 2026-09 规范中的 `texture-compression-unaligned` 名称，因此 BC base dimensions 仍要求 block alignment；已对齐 base 的物理 mip subresource 仍离线保留并上传到 1×1 tail。
- TextureBindingSet 已完成最多 4 set 的生产接入与跨 consumer 浏览器验证；当前固定上限不是 streaming/eviction policy。通用 streaming、partial residency、Virtual Texturing 和内容数据库仍是明确非目标/后续架构，不得借 V3 名义隐式加入。
- `TextureBindingSet` 当前按 active set 固定执行 Material Resolve 与 MASK consumer；两 set 综合证据已显示 CPU、Material Resolve 与 Shadow 的可测增量。后续优化必须保留 GPU producer → consumer 闭环、固定有界调度和零 CPU 可见材质回读，并以同一 `comprehensive-full` workload 证明收益。

## 下一步

1. [ADR-0010](./adr/0010-webgpu-2026-capability-contract.md)：`primitive-index` 的 production consumer 与 portable parity case 已落地；继续为 `shader-f16`、Immediate Data 与 Transient Attachment 增加实际 consumer/fallback，没有 consumer 前保持 record-only。
2. [ADR-0009](./adr/0009-compute-shading-and-advanced-frame-pipeline-v2.md)：Step 0–3 已完成；Step 4 因 Three.js rand 一致性修正而重新打开验证，Step 5–9 同为 implementation-landed/verification-open；Step 10已完成属于此前已过门禁范围的静态cutover cleanup（counter schema v22删除旧Pixel Queue/ShadeWork/MaterialClassDepth字段与zero publisher，删除单值`MaterialResolveBackend`类型壳），最终deletion Gate仍verification-open。允许执行验证后仍需统一完成类型/构建、GTAO corrected-rand、SSGI/Brick4、SSR replacement、shared-derived-products、temporal-reconstruction与post-fusion oracles、`off/gtao/ssgi × SSR off/on` topology matrix、history reset、GTAO/SSR/Temporal/Post视觉检查和唯一 `comprehensive-full` clean PERF；所有 open Exit关闭前不得删除未通过replacement的对照source、关闭Step 10或把ADR整体记为完成。
3. Texture V3 follow-up：针对 active-set 固定编排的 CPU、Material Resolve 与 Shadow 成本做不改变 ABI/正确性合同的优化；只能用同一 `comprehensive-full` workload 和 clean commit A/B 改写当前基线。
