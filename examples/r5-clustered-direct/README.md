# R5 FX-02 Clustered Direct

Production `Renderer.render()` fixture for the documented C-light sweep. Query parameters are
`count`, `layout=spread|overlap`, `kind=point|spot|directional`, and `profile=full|smoke`.
The automated runner owns pass/fail and records JSON, GPU counters/timestamps, diagnostics,
and canvas screenshots; manual inspection is not a Gate input.
