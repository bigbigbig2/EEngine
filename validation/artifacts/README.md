# Local validation artifacts

Runner output is written below this directory by unique `runId` and is ignored by Git. It may contain machine identity, screenshots, raw samples and traces. Do not commit raw runs.

Only a clean, accepted, reproducible summary may be copied to `OEngine/benchmarks/`; that summary must retain the raw artifact content hash.
